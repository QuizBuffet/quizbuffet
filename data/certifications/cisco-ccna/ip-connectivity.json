{
  "slug": "ip-connectivity",
  "name": "Ip Connectivity",
  "cert": "cisco-ccna",
  "version": 1,
  "questions": [
    {
      "id": 321,
      "domain": "3.0",
      "objective": "3.1",
      "keyword": "Routing table",
      "difficulty": "easy",
      "text": "Which description best matches Routing table in CCNA Objective 3.0 IP Connectivity?",
      "answers": [
        {
          "id": "a",
          "text": "A routing table lists known routes and the information a router uses to choose where to forward packets."
        },
        {
          "id": "b",
          "text": "A routing protocol code identifies how a route was learned, such as connected, static, OSPF, or another source."
        },
        {
          "id": "c",
          "text": "A route prefix identifies the destination network or subnet in a routing table entry."
        },
        {
          "id": "d",
          "text": "A network mask or prefix length identifies which bits represent the network portion of an address."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "A routing table lists known routes and the information a router uses to choose where to forward packets. This is the correct answer.",
        "b": "Routing protocol code refers to A routing protocol code identifies how a route was learned, such as connected, static, OSPF, or another source. Routing table is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor.",
        "c": "Route prefix refers to A route prefix identifies the destination network or subnet in a routing table entry. Routing table is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor.",
        "d": "Network mask refers to A network mask or prefix length identifies which bits represent the network portion of an address. Routing table is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor."
      }
    },
    {
      "id": 322,
      "domain": "3.0",
      "objective": "3.1",
      "keyword": "Routing table",
      "difficulty": "medium",
      "text": "A router is choosing a path using information related to Routing table. Which answer best describes the routing behavior?",
      "answers": [
        {
          "id": "a",
          "text": "IPv4 static route is the best match when the IP connectivity scenario requires this behavior: An IPv4 static route is a manually configured route for an IPv4 destination prefix."
        },
        {
          "id": "b",
          "text": "Routing table is the best match when the IP connectivity scenario requires this behavior: A routing table lists known routes and the information a router uses to choose where to forward packets."
        },
        {
          "id": "c",
          "text": "IPv6 static route is the best match when the IP connectivity scenario requires this behavior: An IPv6 static route is a manually configured route for an IPv6 destination prefix."
        },
        {
          "id": "d",
          "text": "Default route is the best match when the IP connectivity scenario requires this behavior: A default route matches destinations that do not match any more specific route."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "IPv4 static route refers to An IPv4 static route is a manually configured route for an IPv4 destination prefix. Routing table is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor.",
        "b": "A routing table lists known routes and the information a router uses to choose where to forward packets. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer.",
        "c": "IPv6 static route refers to An IPv6 static route is a manually configured route for an IPv6 destination prefix. Routing table is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor.",
        "d": "Default route refers to A default route matches destinations that do not match any more specific route. Routing table is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor."
      }
    },
    {
      "id": 323,
      "domain": "3.0",
      "objective": "3.1",
      "keyword": "Routing table",
      "difficulty": "medium-hard",
      "text": "A router has multiple possible routes to a destination. Which scenario best matches Routing table?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to Single-area OSPFv2 when the routing or OSPF evidence depends on this distinction: Single-area OSPFv2 runs OSPF for IPv4 within one area, commonly area 0, to exchange routes dynamically."
        },
        {
          "id": "b",
          "text": "A scenario points to OSPF neighbor adjacency when the routing or OSPF evidence depends on this distinction: An OSPF neighbor adjacency is the relationship formed between OSPF routers that successfully exchange hello packets and required parameters."
        },
        {
          "id": "c",
          "text": "A scenario points to Routing table when the routing or OSPF evidence depends on this distinction: A routing table lists known routes and the information a router uses to choose where to forward packets."
        },
        {
          "id": "d",
          "text": "A scenario points to OSPF hello packets when the routing or OSPF evidence depends on this distinction: OSPF hello packets discover neighbors and maintain OSPF neighbor relationships."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "Single-area OSPFv2 refers to Single-area OSPFv2 runs OSPF for IPv4 within one area, commonly area 0, to exchange routes dynamically. Routing table is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor.",
        "b": "OSPF neighbor adjacency refers to An OSPF neighbor adjacency is the relationship formed between OSPF routers that successfully exchange hello packets and required parameters. Routing table is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor.",
        "c": "A routing table lists known routes and the information a router uses to choose where to forward packets. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer.",
        "d": "OSPF hello packets refers to OSPF hello packets discover neighbors and maintain OSPF neighbor relationships. Routing table is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor."
      }
    },
    {
      "id": 324,
      "domain": "3.0",
      "objective": "3.1",
      "keyword": "Routing table",
      "difficulty": "hard",
      "text": "A Cisco administrator must predict exactly which route is installed or used for a packet. Which answer applies Routing table most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "BDR election is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: Backup designated router election selects a standby router to take over if the DR fails."
        },
        {
          "id": "b",
          "text": "OSPF cost is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: OSPF cost is the metric used by OSPF to choose preferred paths."
        },
        {
          "id": "c",
          "text": "OSPF adjacency verification is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: OSPF adjacency verification checks neighbor state, interface participation, area, timers, and reachability to confirm OSPF operation."
        },
        {
          "id": "d",
          "text": "Routing table is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A routing table lists known routes and the information a router uses to choose where to forward packets."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "BDR election refers to Backup designated router election selects a standby router to take over if the DR fails. Routing table is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor.",
        "b": "OSPF cost refers to OSPF cost is the metric used by OSPF to choose preferred paths. Routing table is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor.",
        "c": "OSPF adjacency verification refers to OSPF adjacency verification checks neighbor state, interface participation, area, timers, and reachability to confirm OSPF operation. Routing table is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor.",
        "d": "A routing table lists known routes and the information a router uses to choose where to forward packets. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer."
      }
    },
    {
      "id": 325,
      "domain": "3.0",
      "objective": "3.1",
      "keyword": "Routing protocol code",
      "difficulty": "easy",
      "text": "Which option correctly defines Routing protocol code?",
      "answers": [
        {
          "id": "a",
          "text": "Longest prefix match selects the route with the most specific matching prefix for the destination IP address."
        },
        {
          "id": "b",
          "text": "A routing protocol code identifies how a route was learned, such as connected, static, OSPF, or another source."
        },
        {
          "id": "c",
          "text": "A router forwarding decision uses the destination IP address, routing table, longest prefix match, and route attributes to choose an outgoing path."
        },
        {
          "id": "d",
          "text": "Route source preference uses administrative distance only when multiple sources advertise the same prefix length."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "Longest prefix match refers to Longest prefix match selects the route with the most specific matching prefix for the destination IP address. Routing protocol code is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor.",
        "b": "A routing protocol code identifies how a route was learned, such as connected, static, OSPF, or another source. This is the correct answer.",
        "c": "Forwarding decision refers to A router forwarding decision uses the destination IP address, routing table, longest prefix match, and route attributes to choose an outgoing path. Routing protocol code is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor.",
        "d": "Route source preference refers to Route source preference uses administrative distance only when multiple sources advertise the same prefix length. Routing protocol code is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor."
      }
    },
    {
      "id": 326,
      "domain": "3.0",
      "objective": "3.1",
      "keyword": "Routing protocol code",
      "difficulty": "medium",
      "text": "A router is choosing a path using information related to Routing protocol code. Which answer best describes the routing behavior?",
      "answers": [
        {
          "id": "a",
          "text": "Fully specified static route is the best match when the IP connectivity scenario requires this behavior: A fully specified static route includes both the exit interface and next-hop address."
        },
        {
          "id": "b",
          "text": "Exit-interface static route is the best match when the IP connectivity scenario requires this behavior: An exit-interface static route specifies the local outgoing interface used to forward traffic."
        },
        {
          "id": "c",
          "text": "Routing protocol code is the best match when the IP connectivity scenario requires this behavior: A routing protocol code identifies how a route was learned, such as connected, static, OSPF, or another source."
        },
        {
          "id": "d",
          "text": "Static route verification is the best match when the IP connectivity scenario requires this behavior: Static route verification confirms configured static routes appear in the routing table and forward traffic as intended."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "Fully specified static route refers to A fully specified static route includes both the exit interface and next-hop address. Routing protocol code is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor.",
        "b": "Exit-interface static route refers to An exit-interface static route specifies the local outgoing interface used to forward traffic. Routing protocol code is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor.",
        "c": "A routing protocol code identifies how a route was learned, such as connected, static, OSPF, or another source. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer.",
        "d": "Static route verification refers to Static route verification confirms configured static routes appear in the routing table and forward traffic as intended. Routing protocol code is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor."
      }
    },
    {
      "id": 327,
      "domain": "3.0",
      "objective": "3.1",
      "keyword": "Routing protocol code",
      "difficulty": "medium-hard",
      "text": "A router has multiple possible routes to a destination. Which scenario best matches Routing protocol code?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to OSPF router ID selection when the routing or OSPF evidence depends on this distinction: OSPF router ID selection uses a manually configured router ID first, then the highest loopback IP, then the highest active physical interface IP."
        },
        {
          "id": "b",
          "text": "A scenario points to OSPF point-to-point network when the routing or OSPF evidence depends on this distinction: An OSPF point-to-point network type connects two routers without DR or BDR election."
        },
        {
          "id": "c",
          "text": "A scenario points to OSPF broadcast network when the routing or OSPF evidence depends on this distinction: An OSPF broadcast network type supports multiaccess Ethernet segments and uses DR/BDR election."
        },
        {
          "id": "d",
          "text": "A scenario points to Routing protocol code when the routing or OSPF evidence depends on this distinction: A routing protocol code identifies how a route was learned, such as connected, static, OSPF, or another source."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "OSPF router ID selection refers to OSPF router ID selection uses a manually configured router ID first, then the highest loopback IP, then the highest active physical interface IP. Routing protocol code is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor.",
        "b": "OSPF point-to-point network refers to An OSPF point-to-point network type connects two routers without DR or BDR election. Routing protocol code is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor.",
        "c": "OSPF broadcast network refers to An OSPF broadcast network type supports multiaccess Ethernet segments and uses DR/BDR election. Routing protocol code is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor.",
        "d": "A routing protocol code identifies how a route was learned, such as connected, static, OSPF, or another source. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer."
      }
    },
    {
      "id": 328,
      "domain": "3.0",
      "objective": "3.1",
      "keyword": "Routing protocol code",
      "difficulty": "hard",
      "text": "A Cisco administrator must predict exactly which route is installed or used for a packet. Which answer applies Routing protocol code most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "Routing protocol code is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A routing protocol code identifies how a route was learned, such as connected, static, OSPF, or another source."
        },
        {
          "id": "b",
          "text": "OSPF wildcard mask is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: An OSPF wildcard mask in a network command identifies which interface addresses match for OSPF activation."
        },
        {
          "id": "c",
          "text": "OSPF process ID is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: The OSPF process ID locally identifies an OSPF routing process and does not need to match between routers."
        },
        {
          "id": "d",
          "text": "First hop redundancy protocols is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: First hop redundancy protocols provide a redundant default gateway for hosts on a LAN."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "A routing protocol code identifies how a route was learned, such as connected, static, OSPF, or another source. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer.",
        "b": "OSPF wildcard mask refers to An OSPF wildcard mask in a network command identifies which interface addresses match for OSPF activation. Routing protocol code is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor.",
        "c": "OSPF process ID refers to The OSPF process ID locally identifies an OSPF routing process and does not need to match between routers. Routing protocol code is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor.",
        "d": "First hop redundancy protocols refers to First hop redundancy protocols provide a redundant default gateway for hosts on a LAN. Routing protocol code is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor."
      }
    },
    {
      "id": 329,
      "domain": "3.0",
      "objective": "3.1",
      "keyword": "Route prefix",
      "difficulty": "easy",
      "text": "How is Route prefix best understood in an IP connectivity context?",
      "answers": [
        {
          "id": "a",
          "text": "A network route points to an entire subnet or network rather than one host."
        },
        {
          "id": "b",
          "text": "A host route points to one specific host address, typically using /32 for IPv4 or /128 for IPv6."
        },
        {
          "id": "c",
          "text": "A route prefix identifies the destination network or subnet in a routing table entry."
        },
        {
          "id": "d",
          "text": "A floating static route uses a higher administrative distance so it is installed only when a better route is unavailable."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "Network route refers to A network route points to an entire subnet or network rather than one host. Route prefix is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor.",
        "b": "Host route refers to A host route points to one specific host address, typically using /32 for IPv4 or /128 for IPv6. Route prefix is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor.",
        "c": "A route prefix identifies the destination network or subnet in a routing table entry. This is the correct answer.",
        "d": "Floating static route refers to A floating static route uses a higher administrative distance so it is installed only when a better route is unavailable. Route prefix is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor."
      }
    },
    {
      "id": 330,
      "domain": "3.0",
      "objective": "3.1",
      "keyword": "Route prefix",
      "difficulty": "medium",
      "text": "A router is choosing a path using information related to Route prefix. Which answer best describes the routing behavior?",
      "answers": [
        {
          "id": "a",
          "text": "OSPF area is the best match when the IP connectivity scenario requires this behavior: An OSPF area groups routers and link-state information; single-area CCNA scenarios commonly use area 0."
        },
        {
          "id": "b",
          "text": "OSPF network command is the best match when the IP connectivity scenario requires this behavior: The OSPF network command enables OSPF on matching interfaces and assigns them to an area."
        },
        {
          "id": "c",
          "text": "OSPF passive interface is the best match when the IP connectivity scenario requires this behavior: An OSPF passive interface advertises the connected network but does not send OSPF hello packets on that interface."
        },
        {
          "id": "d",
          "text": "Route prefix is the best match when the IP connectivity scenario requires this behavior: A route prefix identifies the destination network or subnet in a routing table entry."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "OSPF area refers to An OSPF area groups routers and link-state information; single-area CCNA scenarios commonly use area 0. Route prefix is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor.",
        "b": "OSPF network command refers to The OSPF network command enables OSPF on matching interfaces and assigns them to an area. Route prefix is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor.",
        "c": "OSPF passive interface refers to An OSPF passive interface advertises the connected network but does not send OSPF hello packets on that interface. Route prefix is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor.",
        "d": "A route prefix identifies the destination network or subnet in a routing table entry. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer."
      }
    },
    {
      "id": 331,
      "domain": "3.0",
      "objective": "3.1",
      "keyword": "Route prefix",
      "difficulty": "medium-hard",
      "text": "A router has multiple possible routes to a destination. Which scenario best matches Route prefix?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to Route prefix when the routing or OSPF evidence depends on this distinction: A route prefix identifies the destination network or subnet in a routing table entry."
        },
        {
          "id": "b",
          "text": "A scenario points to OSPF route verification when the routing or OSPF evidence depends on this distinction: OSPF route verification checks that OSPF-learned routes appear in the routing table and are used for forwarding."
        },
        {
          "id": "c",
          "text": "A scenario points to OSPF mismatched area when the routing or OSPF evidence depends on this distinction: An OSPF area mismatch prevents neighbors from forming because both ends of the link must agree on the area."
        },
        {
          "id": "d",
          "text": "A scenario points to OSPF mismatched timers when the routing or OSPF evidence depends on this distinction: OSPF hello and dead timer mismatches can prevent neighbor adjacency from forming."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "A route prefix identifies the destination network or subnet in a routing table entry. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer.",
        "b": "OSPF route verification refers to OSPF route verification checks that OSPF-learned routes appear in the routing table and are used for forwarding. Route prefix is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor.",
        "c": "OSPF mismatched area refers to An OSPF area mismatch prevents neighbors from forming because both ends of the link must agree on the area. Route prefix is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor.",
        "d": "OSPF mismatched timers refers to OSPF hello and dead timer mismatches can prevent neighbor adjacency from forming. Route prefix is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor."
      }
    },
    {
      "id": 332,
      "domain": "3.0",
      "objective": "3.1",
      "keyword": "Route prefix",
      "difficulty": "hard",
      "text": "A Cisco administrator must predict exactly which route is installed or used for a packet. Which answer applies Route prefix most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "Standby router is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A standby router waits to take over forwarding if the active first-hop device fails."
        },
        {
          "id": "b",
          "text": "Route prefix is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A route prefix identifies the destination network or subnet in a routing table entry."
        },
        {
          "id": "c",
          "text": "FHRP failover is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: FHRP failover moves the active gateway role to another device when the current active device becomes unavailable."
        },
        {
          "id": "d",
          "text": "Routing table is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A routing table lists known routes and the information a router uses to choose where to forward packets."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "Standby router refers to A standby router waits to take over forwarding if the active first-hop device fails. Route prefix is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor.",
        "b": "A route prefix identifies the destination network or subnet in a routing table entry. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer.",
        "c": "FHRP failover refers to FHRP failover moves the active gateway role to another device when the current active device becomes unavailable. Route prefix is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor.",
        "d": "Routing table refers to A routing table lists known routes and the information a router uses to choose where to forward packets. Route prefix is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor."
      }
    },
    {
      "id": 333,
      "domain": "3.0",
      "objective": "3.1",
      "keyword": "Network mask",
      "difficulty": "easy",
      "text": "Select the description that best identifies Network mask.",
      "answers": [
        {
          "id": "a",
          "text": "Static route prefix specificity determines whether a default, network, or host route is the most specific match for a destination."
        },
        {
          "id": "b",
          "text": "Single-area OSPFv2 runs OSPF for IPv4 within one area, commonly area 0, to exchange routes dynamically."
        },
        {
          "id": "c",
          "text": "An OSPF neighbor adjacency is the relationship formed between OSPF routers that successfully exchange hello packets and required parameters."
        },
        {
          "id": "d",
          "text": "A network mask or prefix length identifies which bits represent the network portion of an address."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "Static route prefix specificity refers to Static route prefix specificity determines whether a default, network, or host route is the most specific match for a destination. Network mask is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor.",
        "b": "Single-area OSPFv2 refers to Single-area OSPFv2 runs OSPF for IPv4 within one area, commonly area 0, to exchange routes dynamically. Network mask is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor.",
        "c": "OSPF neighbor adjacency refers to An OSPF neighbor adjacency is the relationship formed between OSPF routers that successfully exchange hello packets and required parameters. Network mask is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor.",
        "d": "A network mask or prefix length identifies which bits represent the network portion of an address. This is the correct answer."
      }
    },
    {
      "id": 334,
      "domain": "3.0",
      "objective": "3.1",
      "keyword": "Network mask",
      "difficulty": "medium",
      "text": "A router is choosing a path using information related to Network mask. Which answer best describes the routing behavior?",
      "answers": [
        {
          "id": "a",
          "text": "Network mask is the best match when the IP connectivity scenario requires this behavior: A network mask or prefix length identifies which bits represent the network portion of an address."
        },
        {
          "id": "b",
          "text": "DR election is the best match when the IP connectivity scenario requires this behavior: Designated router election selects a router on an OSPF broadcast network to reduce adjacency overhead."
        },
        {
          "id": "c",
          "text": "BDR election is the best match when the IP connectivity scenario requires this behavior: Backup designated router election selects a standby router to take over if the DR fails."
        },
        {
          "id": "d",
          "text": "OSPF cost is the best match when the IP connectivity scenario requires this behavior: OSPF cost is the metric used by OSPF to choose preferred paths."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "A network mask or prefix length identifies which bits represent the network portion of an address. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer.",
        "b": "DR election refers to Designated router election selects a router on an OSPF broadcast network to reduce adjacency overhead. Network mask is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor.",
        "c": "BDR election refers to Backup designated router election selects a standby router to take over if the DR fails. Network mask is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor.",
        "d": "OSPF cost refers to OSPF cost is the metric used by OSPF to choose preferred paths. Network mask is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor."
      }
    },
    {
      "id": 335,
      "domain": "3.0",
      "objective": "3.1",
      "keyword": "Network mask",
      "difficulty": "medium-hard",
      "text": "A router has multiple possible routes to a destination. Which scenario best matches Network mask?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to Default gateway redundancy when the routing or OSPF evidence depends on this distinction: Default gateway redundancy lets hosts keep using a gateway address even if one physical router or Layer 3 switch fails."
        },
        {
          "id": "b",
          "text": "A scenario points to Network mask when the routing or OSPF evidence depends on this distinction: A network mask or prefix length identifies which bits represent the network portion of an address."
        },
        {
          "id": "c",
          "text": "A scenario points to Virtual IP address when the routing or OSPF evidence depends on this distinction: A virtual IP address is the shared gateway address used by hosts in a first hop redundancy design."
        },
        {
          "id": "d",
          "text": "A scenario points to Virtual MAC address when the routing or OSPF evidence depends on this distinction: A virtual MAC address is used by the active first-hop device so hosts can forward frames to a redundant gateway."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "Default gateway redundancy refers to Default gateway redundancy lets hosts keep using a gateway address even if one physical router or Layer 3 switch fails. Network mask is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor.",
        "b": "A network mask or prefix length identifies which bits represent the network portion of an address. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer.",
        "c": "Virtual IP address refers to A virtual IP address is the shared gateway address used by hosts in a first hop redundancy design. Network mask is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor.",
        "d": "Virtual MAC address refers to A virtual MAC address is used by the active first-hop device so hosts can forward frames to a redundant gateway. Network mask is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor."
      }
    },
    {
      "id": 336,
      "domain": "3.0",
      "objective": "3.1",
      "keyword": "Network mask",
      "difficulty": "hard",
      "text": "A Cisco administrator must predict exactly which route is installed or used for a packet. Which answer applies Network mask most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "Routing metric is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A routing metric ranks paths within the same routing protocol or route source."
        },
        {
          "id": "b",
          "text": "Gateway of last resort is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: The gateway of last resort is the default route used when no more specific route matches the destination."
        },
        {
          "id": "c",
          "text": "Network mask is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A network mask or prefix length identifies which bits represent the network portion of an address."
        },
        {
          "id": "d",
          "text": "Longest prefix match is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: Longest prefix match selects the route with the most specific matching prefix for the destination IP address."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "Routing metric refers to A routing metric ranks paths within the same routing protocol or route source. Network mask is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor.",
        "b": "Gateway of last resort refers to The gateway of last resort is the default route used when no more specific route matches the destination. Network mask is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor.",
        "c": "A network mask or prefix length identifies which bits represent the network portion of an address. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer.",
        "d": "Longest prefix match refers to Longest prefix match selects the route with the most specific matching prefix for the destination IP address. Network mask is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor."
      }
    },
    {
      "id": 337,
      "domain": "3.0",
      "objective": "3.1",
      "keyword": "Next hop",
      "difficulty": "easy",
      "text": "Which description best matches Next hop in CCNA Objective 3.0 IP Connectivity?",
      "answers": [
        {
          "id": "a",
          "text": "A next hop is the next router IP address to which a packet is sent on the way to the destination."
        },
        {
          "id": "b",
          "text": "An OSPF router ID uniquely identifies an OSPF router in the OSPF domain."
        },
        {
          "id": "c",
          "text": "OSPF router ID selection uses a manually configured router ID first, then the highest loopback IP, then the highest active physical interface IP."
        },
        {
          "id": "d",
          "text": "An OSPF point-to-point network type connects two routers without DR or BDR election."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "A next hop is the next router IP address to which a packet is sent on the way to the destination. This is the correct answer.",
        "b": "OSPF router ID refers to An OSPF router ID uniquely identifies an OSPF router in the OSPF domain. Next hop is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor.",
        "c": "OSPF router ID selection refers to OSPF router ID selection uses a manually configured router ID first, then the highest loopback IP, then the highest active physical interface IP. Next hop is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor.",
        "d": "OSPF point-to-point network refers to An OSPF point-to-point network type connects two routers without DR or BDR election. Next hop is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor."
      }
    },
    {
      "id": 338,
      "domain": "3.0",
      "objective": "3.1",
      "keyword": "Next hop",
      "difficulty": "medium",
      "text": "A router is choosing a path using information related to Next hop. Which answer best describes the routing behavior?",
      "answers": [
        {
          "id": "a",
          "text": "OSPF network type mismatch is the best match when the IP connectivity scenario requires this behavior: OSPF network type mismatch can affect adjacency behavior, DR/BDR expectations, or route exchange."
        },
        {
          "id": "b",
          "text": "Next hop is the best match when the IP connectivity scenario requires this behavior: A next hop is the next router IP address to which a packet is sent on the way to the destination."
        },
        {
          "id": "c",
          "text": "OSPF wildcard mask is the best match when the IP connectivity scenario requires this behavior: An OSPF wildcard mask in a network command identifies which interface addresses match for OSPF activation."
        },
        {
          "id": "d",
          "text": "OSPF process ID is the best match when the IP connectivity scenario requires this behavior: The OSPF process ID locally identifies an OSPF routing process and does not need to match between routers."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "OSPF network type mismatch refers to OSPF network type mismatch can affect adjacency behavior, DR/BDR expectations, or route exchange. Next hop is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor.",
        "b": "A next hop is the next router IP address to which a packet is sent on the way to the destination. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer.",
        "c": "OSPF wildcard mask refers to An OSPF wildcard mask in a network command identifies which interface addresses match for OSPF activation. Next hop is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor.",
        "d": "OSPF process ID refers to The OSPF process ID locally identifies an OSPF routing process and does not need to match between routers. Next hop is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor."
      }
    },
    {
      "id": 339,
      "domain": "3.0",
      "objective": "3.1",
      "keyword": "Next hop",
      "difficulty": "medium-hard",
      "text": "A router has multiple possible routes to a destination. Which scenario best matches Next hop?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to Routing protocol code when the routing or OSPF evidence depends on this distinction: A routing protocol code identifies how a route was learned, such as connected, static, OSPF, or another source."
        },
        {
          "id": "b",
          "text": "A scenario points to Route prefix when the routing or OSPF evidence depends on this distinction: A route prefix identifies the destination network or subnet in a routing table entry."
        },
        {
          "id": "c",
          "text": "A scenario points to Next hop when the routing or OSPF evidence depends on this distinction: A next hop is the next router IP address to which a packet is sent on the way to the destination."
        },
        {
          "id": "d",
          "text": "A scenario points to Network mask when the routing or OSPF evidence depends on this distinction: A network mask or prefix length identifies which bits represent the network portion of an address."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "Routing protocol code refers to A routing protocol code identifies how a route was learned, such as connected, static, OSPF, or another source. Next hop is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor.",
        "b": "Route prefix refers to A route prefix identifies the destination network or subnet in a routing table entry. Next hop is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor.",
        "c": "A next hop is the next router IP address to which a packet is sent on the way to the destination. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer.",
        "d": "Network mask refers to A network mask or prefix length identifies which bits represent the network portion of an address. Next hop is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor."
      }
    },
    {
      "id": 340,
      "domain": "3.0",
      "objective": "3.1",
      "keyword": "Next hop",
      "difficulty": "hard",
      "text": "A Cisco administrator must predict exactly which route is installed or used for a packet. Which answer applies Next hop most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "IPv6 static route is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: An IPv6 static route is a manually configured route for an IPv6 destination prefix."
        },
        {
          "id": "b",
          "text": "Default route is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A default route matches destinations that do not match any more specific route."
        },
        {
          "id": "c",
          "text": "Network route is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A network route points to an entire subnet or network rather than one host."
        },
        {
          "id": "d",
          "text": "Next hop is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A next hop is the next router IP address to which a packet is sent on the way to the destination."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "IPv6 static route refers to An IPv6 static route is a manually configured route for an IPv6 destination prefix. Next hop is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor.",
        "b": "Default route refers to A default route matches destinations that do not match any more specific route. Next hop is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor.",
        "c": "Network route refers to A network route points to an entire subnet or network rather than one host. Next hop is different because the scenario depends on interpreting a routing-table field rather than configuring a route or forming an OSPF neighbor.",
        "d": "A next hop is the next router IP address to which a packet is sent on the way to the destination. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer."
      }
    },
    {
      "id": 341,
      "domain": "3.0",
      "objective": "3.1",
      "keyword": "Administrative distance",
      "difficulty": "easy",
      "text": "Which option correctly defines Administrative distance?",
      "answers": [
        {
          "id": "a",
          "text": "OSPF adjacency verification checks neighbor state, interface participation, area, timers, and reachability to confirm OSPF operation."
        },
        {
          "id": "b",
          "text": "Administrative distance ranks the trustworthiness of route sources when the same prefix is learned from multiple sources."
        },
        {
          "id": "c",
          "text": "OSPF route verification checks that OSPF-learned routes appear in the routing table and are used for forwarding."
        },
        {
          "id": "d",
          "text": "An OSPF area mismatch prevents neighbors from forming because both ends of the link must agree on the area."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "OSPF adjacency verification refers to OSPF adjacency verification checks neighbor state, interface participation, area, timers, and reachability to confirm OSPF operation. Administrative distance is different because the scenario depends on route preference, route source trust, default routing, or metric comparison.",
        "b": "Administrative distance ranks the trustworthiness of route sources when the same prefix is learned from multiple sources. This is the correct answer.",
        "c": "OSPF route verification refers to OSPF route verification checks that OSPF-learned routes appear in the routing table and are used for forwarding. Administrative distance is different because the scenario depends on route preference, route source trust, default routing, or metric comparison.",
        "d": "OSPF mismatched area refers to An OSPF area mismatch prevents neighbors from forming because both ends of the link must agree on the area. Administrative distance is different because the scenario depends on route preference, route source trust, default routing, or metric comparison."
      }
    },
    {
      "id": 342,
      "domain": "3.0",
      "objective": "3.1",
      "keyword": "Administrative distance",
      "difficulty": "medium",
      "text": "A router is choosing a path using information related to Administrative distance. Which answer best describes the routing behavior?",
      "answers": [
        {
          "id": "a",
          "text": "Active router is the best match when the IP connectivity scenario requires this behavior: An active router in an FHRP group forwards traffic sent to the virtual gateway."
        },
        {
          "id": "b",
          "text": "Standby router is the best match when the IP connectivity scenario requires this behavior: A standby router waits to take over forwarding if the active first-hop device fails."
        },
        {
          "id": "c",
          "text": "Administrative distance is the best match when the IP connectivity scenario requires this behavior: Administrative distance ranks the trustworthiness of route sources when the same prefix is learned from multiple sources."
        },
        {
          "id": "d",
          "text": "FHRP failover is the best match when the IP connectivity scenario requires this behavior: FHRP failover moves the active gateway role to another device when the current active device becomes unavailable."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "Active router refers to An active router in an FHRP group forwards traffic sent to the virtual gateway. Administrative distance is different because the scenario depends on route preference, route source trust, default routing, or metric comparison.",
        "b": "Standby router refers to A standby router waits to take over forwarding if the active first-hop device fails. Administrative distance is different because the scenario depends on route preference, route source trust, default routing, or metric comparison.",
        "c": "Administrative distance ranks the trustworthiness of route sources when the same prefix is learned from multiple sources. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer.",
        "d": "FHRP failover refers to FHRP failover moves the active gateway role to another device when the current active device becomes unavailable. Administrative distance is different because the scenario depends on route preference, route source trust, default routing, or metric comparison."
      }
    },
    {
      "id": 343,
      "domain": "3.0",
      "objective": "3.1",
      "keyword": "Administrative distance",
      "difficulty": "medium-hard",
      "text": "A router has multiple possible routes to a destination. Which scenario best matches Administrative distance?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to Next hop when the routing or OSPF evidence depends on this distinction: A next hop is the next router IP address to which a packet is sent on the way to the destination."
        },
        {
          "id": "b",
          "text": "A scenario points to Longest prefix match when the routing or OSPF evidence depends on this distinction: Longest prefix match selects the route with the most specific matching prefix for the destination IP address."
        },
        {
          "id": "c",
          "text": "A scenario points to Forwarding decision when the routing or OSPF evidence depends on this distinction: A router forwarding decision uses the destination IP address, routing table, longest prefix match, and route attributes to choose an outgoing path."
        },
        {
          "id": "d",
          "text": "A scenario points to Administrative distance when the routing or OSPF evidence depends on this distinction: Administrative distance ranks the trustworthiness of route sources when the same prefix is learned from multiple sources."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "Next hop refers to A next hop is the next router IP address to which a packet is sent on the way to the destination. Administrative distance is different because the scenario depends on route preference, route source trust, default routing, or metric comparison.",
        "b": "Longest prefix match refers to Longest prefix match selects the route with the most specific matching prefix for the destination IP address. Administrative distance is different because the scenario depends on route preference, route source trust, default routing, or metric comparison.",
        "c": "Forwarding decision refers to A router forwarding decision uses the destination IP address, routing table, longest prefix match, and route attributes to choose an outgoing path. Administrative distance is different because the scenario depends on route preference, route source trust, default routing, or metric comparison.",
        "d": "Administrative distance ranks the trustworthiness of route sources when the same prefix is learned from multiple sources. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer."
      }
    },
    {
      "id": 344,
      "domain": "3.0",
      "objective": "3.1",
      "keyword": "Administrative distance",
      "difficulty": "hard",
      "text": "A Cisco administrator must predict exactly which route is installed or used for a packet. Which answer applies Administrative distance most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "Administrative distance is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: Administrative distance ranks the trustworthiness of route sources when the same prefix is learned from multiple sources."
        },
        {
          "id": "b",
          "text": "Exit-interface static route is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: An exit-interface static route specifies the local outgoing interface used to forward traffic."
        },
        {
          "id": "c",
          "text": "Static route verification is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: Static route verification confirms configured static routes appear in the routing table and forward traffic as intended."
        },
        {
          "id": "d",
          "text": "Static route prefix specificity is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: Static route prefix specificity determines whether a default, network, or host route is the most specific match for a destination."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "Administrative distance ranks the trustworthiness of route sources when the same prefix is learned from multiple sources. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer.",
        "b": "Exit-interface static route refers to An exit-interface static route specifies the local outgoing interface used to forward traffic. Administrative distance is different because the scenario depends on route preference, route source trust, default routing, or metric comparison.",
        "c": "Static route verification refers to Static route verification confirms configured static routes appear in the routing table and forward traffic as intended. Administrative distance is different because the scenario depends on route preference, route source trust, default routing, or metric comparison.",
        "d": "Static route prefix specificity refers to Static route prefix specificity determines whether a default, network, or host route is the most specific match for a destination. Administrative distance is different because the scenario depends on route preference, route source trust, default routing, or metric comparison."
      }
    },
    {
      "id": 345,
      "domain": "3.0",
      "objective": "3.1",
      "keyword": "Routing metric",
      "difficulty": "easy",
      "text": "How is Routing metric best understood in an IP connectivity context?",
      "answers": [
        {
          "id": "a",
          "text": "First hop redundancy protocols provide a redundant default gateway for hosts on a LAN."
        },
        {
          "id": "b",
          "text": "Default gateway redundancy lets hosts keep using a gateway address even if one physical router or Layer 3 switch fails."
        },
        {
          "id": "c",
          "text": "A routing metric ranks paths within the same routing protocol or route source."
        },
        {
          "id": "d",
          "text": "A virtual IP address is the shared gateway address used by hosts in a first hop redundancy design."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "First hop redundancy protocols refers to First hop redundancy protocols provide a redundant default gateway for hosts on a LAN. Routing metric is different because the scenario depends on route preference, route source trust, default routing, or metric comparison.",
        "b": "Default gateway redundancy refers to Default gateway redundancy lets hosts keep using a gateway address even if one physical router or Layer 3 switch fails. Routing metric is different because the scenario depends on route preference, route source trust, default routing, or metric comparison.",
        "c": "A routing metric ranks paths within the same routing protocol or route source. This is the correct answer.",
        "d": "Virtual IP address refers to A virtual IP address is the shared gateway address used by hosts in a first hop redundancy design. Routing metric is different because the scenario depends on route preference, route source trust, default routing, or metric comparison."
      }
    },
    {
      "id": 346,
      "domain": "3.0",
      "objective": "3.1",
      "keyword": "Routing metric",
      "difficulty": "medium",
      "text": "A router is choosing a path using information related to Routing metric. Which answer best describes the routing behavior?",
      "answers": [
        {
          "id": "a",
          "text": "Routing table is the best match when the IP connectivity scenario requires this behavior: A routing table lists known routes and the information a router uses to choose where to forward packets."
        },
        {
          "id": "b",
          "text": "Routing protocol code is the best match when the IP connectivity scenario requires this behavior: A routing protocol code identifies how a route was learned, such as connected, static, OSPF, or another source."
        },
        {
          "id": "c",
          "text": "Route prefix is the best match when the IP connectivity scenario requires this behavior: A route prefix identifies the destination network or subnet in a routing table entry."
        },
        {
          "id": "d",
          "text": "Routing metric is the best match when the IP connectivity scenario requires this behavior: A routing metric ranks paths within the same routing protocol or route source."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "Routing table refers to A routing table lists known routes and the information a router uses to choose where to forward packets. Routing metric is different because the scenario depends on route preference, route source trust, default routing, or metric comparison.",
        "b": "Routing protocol code refers to A routing protocol code identifies how a route was learned, such as connected, static, OSPF, or another source. Routing metric is different because the scenario depends on route preference, route source trust, default routing, or metric comparison.",
        "c": "Route prefix refers to A route prefix identifies the destination network or subnet in a routing table entry. Routing metric is different because the scenario depends on route preference, route source trust, default routing, or metric comparison.",
        "d": "A routing metric ranks paths within the same routing protocol or route source. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer."
      }
    },
    {
      "id": 347,
      "domain": "3.0",
      "objective": "3.1",
      "keyword": "Routing metric",
      "difficulty": "medium-hard",
      "text": "A router has multiple possible routes to a destination. Which scenario best matches Routing metric?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to Routing metric when the routing or OSPF evidence depends on this distinction: A routing metric ranks paths within the same routing protocol or route source."
        },
        {
          "id": "b",
          "text": "A scenario points to Host route when the routing or OSPF evidence depends on this distinction: A host route points to one specific host address, typically using /32 for IPv4 or /128 for IPv6."
        },
        {
          "id": "c",
          "text": "A scenario points to Floating static route when the routing or OSPF evidence depends on this distinction: A floating static route uses a higher administrative distance so it is installed only when a better route is unavailable."
        },
        {
          "id": "d",
          "text": "A scenario points to Recursive static route when the routing or OSPF evidence depends on this distinction: A recursive static route points to a next-hop IP address that must itself be resolved through another route."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "A routing metric ranks paths within the same routing protocol or route source. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer.",
        "b": "Host route refers to A host route points to one specific host address, typically using /32 for IPv4 or /128 for IPv6. Routing metric is different because the scenario depends on route preference, route source trust, default routing, or metric comparison.",
        "c": "Floating static route refers to A floating static route uses a higher administrative distance so it is installed only when a better route is unavailable. Routing metric is different because the scenario depends on route preference, route source trust, default routing, or metric comparison.",
        "d": "Recursive static route refers to A recursive static route points to a next-hop IP address that must itself be resolved through another route. Routing metric is different because the scenario depends on route preference, route source trust, default routing, or metric comparison."
      }
    },
    {
      "id": 348,
      "domain": "3.0",
      "objective": "3.1",
      "keyword": "Routing metric",
      "difficulty": "hard",
      "text": "A Cisco administrator must predict exactly which route is installed or used for a packet. Which answer applies Routing metric most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "OSPF network command is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: The OSPF network command enables OSPF on matching interfaces and assigns them to an area."
        },
        {
          "id": "b",
          "text": "Routing metric is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A routing metric ranks paths within the same routing protocol or route source."
        },
        {
          "id": "c",
          "text": "OSPF passive interface is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: An OSPF passive interface advertises the connected network but does not send OSPF hello packets on that interface."
        },
        {
          "id": "d",
          "text": "OSPF router ID is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: An OSPF router ID uniquely identifies an OSPF router in the OSPF domain."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "OSPF network command refers to The OSPF network command enables OSPF on matching interfaces and assigns them to an area. Routing metric is different because the scenario depends on route preference, route source trust, default routing, or metric comparison.",
        "b": "A routing metric ranks paths within the same routing protocol or route source. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer.",
        "c": "OSPF passive interface refers to An OSPF passive interface advertises the connected network but does not send OSPF hello packets on that interface. Routing metric is different because the scenario depends on route preference, route source trust, default routing, or metric comparison.",
        "d": "OSPF router ID refers to An OSPF router ID uniquely identifies an OSPF router in the OSPF domain. Routing metric is different because the scenario depends on route preference, route source trust, default routing, or metric comparison."
      }
    },
    {
      "id": 349,
      "domain": "3.0",
      "objective": "3.1",
      "keyword": "Gateway of last resort",
      "difficulty": "easy",
      "text": "Select the description that best identifies Gateway of last resort.",
      "answers": [
        {
          "id": "a",
          "text": "Administrative distance ranks the trustworthiness of route sources when the same prefix is learned from multiple sources."
        },
        {
          "id": "b",
          "text": "A routing metric ranks paths within the same routing protocol or route source."
        },
        {
          "id": "c",
          "text": "Route source preference uses administrative distance only when multiple sources advertise the same prefix length."
        },
        {
          "id": "d",
          "text": "The gateway of last resort is the default route used when no more specific route matches the destination."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "Administrative distance refers to Administrative distance ranks the trustworthiness of route sources when the same prefix is learned from multiple sources. Gateway of last resort is different because the scenario depends on route preference, route source trust, default routing, or metric comparison.",
        "b": "Routing metric refers to A routing metric ranks paths within the same routing protocol or route source. Gateway of last resort is different because the scenario depends on route preference, route source trust, default routing, or metric comparison.",
        "c": "Route source preference refers to Route source preference uses administrative distance only when multiple sources advertise the same prefix length. Gateway of last resort is different because the scenario depends on route preference, route source trust, default routing, or metric comparison.",
        "d": "The gateway of last resort is the default route used when no more specific route matches the destination. This is the correct answer."
      }
    },
    {
      "id": 350,
      "domain": "3.0",
      "objective": "3.1",
      "keyword": "Gateway of last resort",
      "difficulty": "medium",
      "text": "A router is choosing a path using information related to Gateway of last resort. Which answer best describes the routing behavior?",
      "answers": [
        {
          "id": "a",
          "text": "Gateway of last resort is the best match when the IP connectivity scenario requires this behavior: The gateway of last resort is the default route used when no more specific route matches the destination."
        },
        {
          "id": "b",
          "text": "IPv4 static route is the best match when the IP connectivity scenario requires this behavior: An IPv4 static route is a manually configured route for an IPv4 destination prefix."
        },
        {
          "id": "c",
          "text": "IPv6 static route is the best match when the IP connectivity scenario requires this behavior: An IPv6 static route is a manually configured route for an IPv6 destination prefix."
        },
        {
          "id": "d",
          "text": "Default route is the best match when the IP connectivity scenario requires this behavior: A default route matches destinations that do not match any more specific route."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "The gateway of last resort is the default route used when no more specific route matches the destination. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer.",
        "b": "IPv4 static route refers to An IPv4 static route is a manually configured route for an IPv4 destination prefix. Gateway of last resort is different because the scenario depends on route preference, route source trust, default routing, or metric comparison.",
        "c": "IPv6 static route refers to An IPv6 static route is a manually configured route for an IPv6 destination prefix. Gateway of last resort is different because the scenario depends on route preference, route source trust, default routing, or metric comparison.",
        "d": "Default route refers to A default route matches destinations that do not match any more specific route. Gateway of last resort is different because the scenario depends on route preference, route source trust, default routing, or metric comparison."
      }
    },
    {
      "id": 351,
      "domain": "3.0",
      "objective": "3.1",
      "keyword": "Gateway of last resort",
      "difficulty": "medium-hard",
      "text": "A router has multiple possible routes to a destination. Which scenario best matches Gateway of last resort?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to Single-area OSPFv2 when the routing or OSPF evidence depends on this distinction: Single-area OSPFv2 runs OSPF for IPv4 within one area, commonly area 0, to exchange routes dynamically."
        },
        {
          "id": "b",
          "text": "A scenario points to Gateway of last resort when the routing or OSPF evidence depends on this distinction: The gateway of last resort is the default route used when no more specific route matches the destination."
        },
        {
          "id": "c",
          "text": "A scenario points to OSPF neighbor adjacency when the routing or OSPF evidence depends on this distinction: An OSPF neighbor adjacency is the relationship formed between OSPF routers that successfully exchange hello packets and required parameters."
        },
        {
          "id": "d",
          "text": "A scenario points to OSPF hello packets when the routing or OSPF evidence depends on this distinction: OSPF hello packets discover neighbors and maintain OSPF neighbor relationships."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "Single-area OSPFv2 refers to Single-area OSPFv2 runs OSPF for IPv4 within one area, commonly area 0, to exchange routes dynamically. Gateway of last resort is different because the scenario depends on route preference, route source trust, default routing, or metric comparison.",
        "b": "The gateway of last resort is the default route used when no more specific route matches the destination. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer.",
        "c": "OSPF neighbor adjacency refers to An OSPF neighbor adjacency is the relationship formed between OSPF routers that successfully exchange hello packets and required parameters. Gateway of last resort is different because the scenario depends on route preference, route source trust, default routing, or metric comparison.",
        "d": "OSPF hello packets refers to OSPF hello packets discover neighbors and maintain OSPF neighbor relationships. Gateway of last resort is different because the scenario depends on route preference, route source trust, default routing, or metric comparison."
      }
    },
    {
      "id": 352,
      "domain": "3.0",
      "objective": "3.1",
      "keyword": "Gateway of last resort",
      "difficulty": "hard",
      "text": "A Cisco administrator must predict exactly which route is installed or used for a packet. Which answer applies Gateway of last resort most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "BDR election is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: Backup designated router election selects a standby router to take over if the DR fails."
        },
        {
          "id": "b",
          "text": "OSPF cost is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: OSPF cost is the metric used by OSPF to choose preferred paths."
        },
        {
          "id": "c",
          "text": "Gateway of last resort is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: The gateway of last resort is the default route used when no more specific route matches the destination."
        },
        {
          "id": "d",
          "text": "OSPF adjacency verification is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: OSPF adjacency verification checks neighbor state, interface participation, area, timers, and reachability to confirm OSPF operation."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "BDR election refers to Backup designated router election selects a standby router to take over if the DR fails. Gateway of last resort is different because the scenario depends on route preference, route source trust, default routing, or metric comparison.",
        "b": "OSPF cost refers to OSPF cost is the metric used by OSPF to choose preferred paths. Gateway of last resort is different because the scenario depends on route preference, route source trust, default routing, or metric comparison.",
        "c": "The gateway of last resort is the default route used when no more specific route matches the destination. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer.",
        "d": "OSPF adjacency verification refers to OSPF adjacency verification checks neighbor state, interface participation, area, timers, and reachability to confirm OSPF operation. Gateway of last resort is different because the scenario depends on route preference, route source trust, default routing, or metric comparison."
      }
    },
    {
      "id": 353,
      "domain": "3.0",
      "objective": "3.2",
      "keyword": "Longest prefix match",
      "difficulty": "easy",
      "text": "Which description best matches Longest prefix match in CCNA Objective 3.0 IP Connectivity?",
      "answers": [
        {
          "id": "a",
          "text": "Longest prefix match selects the route with the most specific matching prefix for the destination IP address."
        },
        {
          "id": "b",
          "text": "A next hop is the next router IP address to which a packet is sent on the way to the destination."
        },
        {
          "id": "c",
          "text": "Administrative distance ranks the trustworthiness of route sources when the same prefix is learned from multiple sources."
        },
        {
          "id": "d",
          "text": "A routing metric ranks paths within the same routing protocol or route source."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "Longest prefix match selects the route with the most specific matching prefix for the destination IP address. This is the correct answer.",
        "b": "Next hop refers to A next hop is the next router IP address to which a packet is sent on the way to the destination. Longest prefix match is different because the scenario depends on how a router chooses the actual forwarding path for a destination packet.",
        "c": "Administrative distance refers to Administrative distance ranks the trustworthiness of route sources when the same prefix is learned from multiple sources. Longest prefix match is different because the scenario depends on how a router chooses the actual forwarding path for a destination packet.",
        "d": "Routing metric refers to A routing metric ranks paths within the same routing protocol or route source. Longest prefix match is different because the scenario depends on how a router chooses the actual forwarding path for a destination packet."
      }
    },
    {
      "id": 354,
      "domain": "3.0",
      "objective": "3.2",
      "keyword": "Longest prefix match",
      "difficulty": "medium",
      "text": "A router is choosing a path using information related to Longest prefix match. Which answer best describes the routing behavior?",
      "answers": [
        {
          "id": "a",
          "text": "Fully specified static route is the best match when the IP connectivity scenario requires this behavior: A fully specified static route includes both the exit interface and next-hop address."
        },
        {
          "id": "b",
          "text": "Longest prefix match is the best match when the IP connectivity scenario requires this behavior: Longest prefix match selects the route with the most specific matching prefix for the destination IP address."
        },
        {
          "id": "c",
          "text": "Exit-interface static route is the best match when the IP connectivity scenario requires this behavior: An exit-interface static route specifies the local outgoing interface used to forward traffic."
        },
        {
          "id": "d",
          "text": "Static route verification is the best match when the IP connectivity scenario requires this behavior: Static route verification confirms configured static routes appear in the routing table and forward traffic as intended."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "Fully specified static route refers to A fully specified static route includes both the exit interface and next-hop address. Longest prefix match is different because the scenario depends on how a router chooses the actual forwarding path for a destination packet.",
        "b": "Longest prefix match selects the route with the most specific matching prefix for the destination IP address. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer.",
        "c": "Exit-interface static route refers to An exit-interface static route specifies the local outgoing interface used to forward traffic. Longest prefix match is different because the scenario depends on how a router chooses the actual forwarding path for a destination packet.",
        "d": "Static route verification refers to Static route verification confirms configured static routes appear in the routing table and forward traffic as intended. Longest prefix match is different because the scenario depends on how a router chooses the actual forwarding path for a destination packet."
      }
    },
    {
      "id": 355,
      "domain": "3.0",
      "objective": "3.2",
      "keyword": "Longest prefix match",
      "difficulty": "medium-hard",
      "text": "A router has multiple possible routes to a destination. Which scenario best matches Longest prefix match?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to OSPF router ID selection when the routing or OSPF evidence depends on this distinction: OSPF router ID selection uses a manually configured router ID first, then the highest loopback IP, then the highest active physical interface IP."
        },
        {
          "id": "b",
          "text": "A scenario points to OSPF point-to-point network when the routing or OSPF evidence depends on this distinction: An OSPF point-to-point network type connects two routers without DR or BDR election."
        },
        {
          "id": "c",
          "text": "A scenario points to Longest prefix match when the routing or OSPF evidence depends on this distinction: Longest prefix match selects the route with the most specific matching prefix for the destination IP address."
        },
        {
          "id": "d",
          "text": "A scenario points to OSPF broadcast network when the routing or OSPF evidence depends on this distinction: An OSPF broadcast network type supports multiaccess Ethernet segments and uses DR/BDR election."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "OSPF router ID selection refers to OSPF router ID selection uses a manually configured router ID first, then the highest loopback IP, then the highest active physical interface IP. Longest prefix match is different because the scenario depends on how a router chooses the actual forwarding path for a destination packet.",
        "b": "OSPF point-to-point network refers to An OSPF point-to-point network type connects two routers without DR or BDR election. Longest prefix match is different because the scenario depends on how a router chooses the actual forwarding path for a destination packet.",
        "c": "Longest prefix match selects the route with the most specific matching prefix for the destination IP address. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer.",
        "d": "OSPF broadcast network refers to An OSPF broadcast network type supports multiaccess Ethernet segments and uses DR/BDR election. Longest prefix match is different because the scenario depends on how a router chooses the actual forwarding path for a destination packet."
      }
    },
    {
      "id": 356,
      "domain": "3.0",
      "objective": "3.2",
      "keyword": "Longest prefix match",
      "difficulty": "hard",
      "text": "A Cisco administrator must predict exactly which route is installed or used for a packet. Which answer applies Longest prefix match most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "OSPF wildcard mask is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: An OSPF wildcard mask in a network command identifies which interface addresses match for OSPF activation."
        },
        {
          "id": "b",
          "text": "OSPF process ID is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: The OSPF process ID locally identifies an OSPF routing process and does not need to match between routers."
        },
        {
          "id": "c",
          "text": "First hop redundancy protocols is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: First hop redundancy protocols provide a redundant default gateway for hosts on a LAN."
        },
        {
          "id": "d",
          "text": "Longest prefix match is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: Longest prefix match selects the route with the most specific matching prefix for the destination IP address."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "OSPF wildcard mask refers to An OSPF wildcard mask in a network command identifies which interface addresses match for OSPF activation. Longest prefix match is different because the scenario depends on how a router chooses the actual forwarding path for a destination packet.",
        "b": "OSPF process ID refers to The OSPF process ID locally identifies an OSPF routing process and does not need to match between routers. Longest prefix match is different because the scenario depends on how a router chooses the actual forwarding path for a destination packet.",
        "c": "First hop redundancy protocols refers to First hop redundancy protocols provide a redundant default gateway for hosts on a LAN. Longest prefix match is different because the scenario depends on how a router chooses the actual forwarding path for a destination packet.",
        "d": "Longest prefix match selects the route with the most specific matching prefix for the destination IP address. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer."
      }
    },
    {
      "id": 357,
      "domain": "3.0",
      "objective": "3.2",
      "keyword": "Forwarding decision",
      "difficulty": "easy",
      "text": "Which option correctly defines Forwarding decision?",
      "answers": [
        {
          "id": "a",
          "text": "A network route points to an entire subnet or network rather than one host."
        },
        {
          "id": "b",
          "text": "A router forwarding decision uses the destination IP address, routing table, longest prefix match, and route attributes to choose an outgoing path."
        },
        {
          "id": "c",
          "text": "A host route points to one specific host address, typically using /32 for IPv4 or /128 for IPv6."
        },
        {
          "id": "d",
          "text": "A floating static route uses a higher administrative distance so it is installed only when a better route is unavailable."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "Network route refers to A network route points to an entire subnet or network rather than one host. Forwarding decision is different because the scenario depends on how a router chooses the actual forwarding path for a destination packet.",
        "b": "A router forwarding decision uses the destination IP address, routing table, longest prefix match, and route attributes to choose an outgoing path. This is the correct answer.",
        "c": "Host route refers to A host route points to one specific host address, typically using /32 for IPv4 or /128 for IPv6. Forwarding decision is different because the scenario depends on how a router chooses the actual forwarding path for a destination packet.",
        "d": "Floating static route refers to A floating static route uses a higher administrative distance so it is installed only when a better route is unavailable. Forwarding decision is different because the scenario depends on how a router chooses the actual forwarding path for a destination packet."
      }
    },
    {
      "id": 358,
      "domain": "3.0",
      "objective": "3.2",
      "keyword": "Forwarding decision",
      "difficulty": "medium",
      "text": "A router is choosing a path using information related to Forwarding decision. Which answer best describes the routing behavior?",
      "answers": [
        {
          "id": "a",
          "text": "OSPF area is the best match when the IP connectivity scenario requires this behavior: An OSPF area groups routers and link-state information; single-area CCNA scenarios commonly use area 0."
        },
        {
          "id": "b",
          "text": "OSPF network command is the best match when the IP connectivity scenario requires this behavior: The OSPF network command enables OSPF on matching interfaces and assigns them to an area."
        },
        {
          "id": "c",
          "text": "Forwarding decision is the best match when the IP connectivity scenario requires this behavior: A router forwarding decision uses the destination IP address, routing table, longest prefix match, and route attributes to choose an outgoing path."
        },
        {
          "id": "d",
          "text": "OSPF passive interface is the best match when the IP connectivity scenario requires this behavior: An OSPF passive interface advertises the connected network but does not send OSPF hello packets on that interface."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "OSPF area refers to An OSPF area groups routers and link-state information; single-area CCNA scenarios commonly use area 0. Forwarding decision is different because the scenario depends on how a router chooses the actual forwarding path for a destination packet.",
        "b": "OSPF network command refers to The OSPF network command enables OSPF on matching interfaces and assigns them to an area. Forwarding decision is different because the scenario depends on how a router chooses the actual forwarding path for a destination packet.",
        "c": "A router forwarding decision uses the destination IP address, routing table, longest prefix match, and route attributes to choose an outgoing path. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer.",
        "d": "OSPF passive interface refers to An OSPF passive interface advertises the connected network but does not send OSPF hello packets on that interface. Forwarding decision is different because the scenario depends on how a router chooses the actual forwarding path for a destination packet."
      }
    },
    {
      "id": 359,
      "domain": "3.0",
      "objective": "3.2",
      "keyword": "Forwarding decision",
      "difficulty": "medium-hard",
      "text": "A router has multiple possible routes to a destination. Which scenario best matches Forwarding decision?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to OSPF route verification when the routing or OSPF evidence depends on this distinction: OSPF route verification checks that OSPF-learned routes appear in the routing table and are used for forwarding."
        },
        {
          "id": "b",
          "text": "A scenario points to OSPF mismatched area when the routing or OSPF evidence depends on this distinction: An OSPF area mismatch prevents neighbors from forming because both ends of the link must agree on the area."
        },
        {
          "id": "c",
          "text": "A scenario points to OSPF mismatched timers when the routing or OSPF evidence depends on this distinction: OSPF hello and dead timer mismatches can prevent neighbor adjacency from forming."
        },
        {
          "id": "d",
          "text": "A scenario points to Forwarding decision when the routing or OSPF evidence depends on this distinction: A router forwarding decision uses the destination IP address, routing table, longest prefix match, and route attributes to choose an outgoing path."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "OSPF route verification refers to OSPF route verification checks that OSPF-learned routes appear in the routing table and are used for forwarding. Forwarding decision is different because the scenario depends on how a router chooses the actual forwarding path for a destination packet.",
        "b": "OSPF mismatched area refers to An OSPF area mismatch prevents neighbors from forming because both ends of the link must agree on the area. Forwarding decision is different because the scenario depends on how a router chooses the actual forwarding path for a destination packet.",
        "c": "OSPF mismatched timers refers to OSPF hello and dead timer mismatches can prevent neighbor adjacency from forming. Forwarding decision is different because the scenario depends on how a router chooses the actual forwarding path for a destination packet.",
        "d": "A router forwarding decision uses the destination IP address, routing table, longest prefix match, and route attributes to choose an outgoing path. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer."
      }
    },
    {
      "id": 360,
      "domain": "3.0",
      "objective": "3.2",
      "keyword": "Forwarding decision",
      "difficulty": "hard",
      "text": "A Cisco administrator must predict exactly which route is installed or used for a packet. Which answer applies Forwarding decision most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "Forwarding decision is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A router forwarding decision uses the destination IP address, routing table, longest prefix match, and route attributes to choose an outgoing path."
        },
        {
          "id": "b",
          "text": "Standby router is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A standby router waits to take over forwarding if the active first-hop device fails."
        },
        {
          "id": "c",
          "text": "FHRP failover is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: FHRP failover moves the active gateway role to another device when the current active device becomes unavailable."
        },
        {
          "id": "d",
          "text": "Longest prefix match is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: Longest prefix match selects the route with the most specific matching prefix for the destination IP address."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "A router forwarding decision uses the destination IP address, routing table, longest prefix match, and route attributes to choose an outgoing path. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer.",
        "b": "Standby router refers to A standby router waits to take over forwarding if the active first-hop device fails. Forwarding decision is different because the scenario depends on how a router chooses the actual forwarding path for a destination packet.",
        "c": "FHRP failover refers to FHRP failover moves the active gateway role to another device when the current active device becomes unavailable. Forwarding decision is different because the scenario depends on how a router chooses the actual forwarding path for a destination packet.",
        "d": "Longest prefix match refers to Longest prefix match selects the route with the most specific matching prefix for the destination IP address. Forwarding decision is different because the scenario depends on how a router chooses the actual forwarding path for a destination packet."
      }
    },
    {
      "id": 361,
      "domain": "3.0",
      "objective": "3.2",
      "keyword": "Route source preference",
      "difficulty": "easy",
      "text": "How is Route source preference best understood in an IP connectivity context?",
      "answers": [
        {
          "id": "a",
          "text": "Static route prefix specificity determines whether a default, network, or host route is the most specific match for a destination."
        },
        {
          "id": "b",
          "text": "Single-area OSPFv2 runs OSPF for IPv4 within one area, commonly area 0, to exchange routes dynamically."
        },
        {
          "id": "c",
          "text": "Route source preference uses administrative distance only when multiple sources advertise the same prefix length."
        },
        {
          "id": "d",
          "text": "An OSPF neighbor adjacency is the relationship formed between OSPF routers that successfully exchange hello packets and required parameters."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "Static route prefix specificity refers to Static route prefix specificity determines whether a default, network, or host route is the most specific match for a destination. Route source preference is different because the scenario depends on route preference, route source trust, default routing, or metric comparison.",
        "b": "Single-area OSPFv2 refers to Single-area OSPFv2 runs OSPF for IPv4 within one area, commonly area 0, to exchange routes dynamically. Route source preference is different because the scenario depends on route preference, route source trust, default routing, or metric comparison.",
        "c": "Route source preference uses administrative distance only when multiple sources advertise the same prefix length. This is the correct answer.",
        "d": "OSPF neighbor adjacency refers to An OSPF neighbor adjacency is the relationship formed between OSPF routers that successfully exchange hello packets and required parameters. Route source preference is different because the scenario depends on route preference, route source trust, default routing, or metric comparison."
      }
    },
    {
      "id": 362,
      "domain": "3.0",
      "objective": "3.2",
      "keyword": "Route source preference",
      "difficulty": "medium",
      "text": "A router is choosing a path using information related to Route source preference. Which answer best describes the routing behavior?",
      "answers": [
        {
          "id": "a",
          "text": "DR election is the best match when the IP connectivity scenario requires this behavior: Designated router election selects a router on an OSPF broadcast network to reduce adjacency overhead."
        },
        {
          "id": "b",
          "text": "BDR election is the best match when the IP connectivity scenario requires this behavior: Backup designated router election selects a standby router to take over if the DR fails."
        },
        {
          "id": "c",
          "text": "OSPF cost is the best match when the IP connectivity scenario requires this behavior: OSPF cost is the metric used by OSPF to choose preferred paths."
        },
        {
          "id": "d",
          "text": "Route source preference is the best match when the IP connectivity scenario requires this behavior: Route source preference uses administrative distance only when multiple sources advertise the same prefix length."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "DR election refers to Designated router election selects a router on an OSPF broadcast network to reduce adjacency overhead. Route source preference is different because the scenario depends on route preference, route source trust, default routing, or metric comparison.",
        "b": "BDR election refers to Backup designated router election selects a standby router to take over if the DR fails. Route source preference is different because the scenario depends on route preference, route source trust, default routing, or metric comparison.",
        "c": "OSPF cost refers to OSPF cost is the metric used by OSPF to choose preferred paths. Route source preference is different because the scenario depends on route preference, route source trust, default routing, or metric comparison.",
        "d": "Route source preference uses administrative distance only when multiple sources advertise the same prefix length. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer."
      }
    },
    {
      "id": 363,
      "domain": "3.0",
      "objective": "3.2",
      "keyword": "Route source preference",
      "difficulty": "medium-hard",
      "text": "A router has multiple possible routes to a destination. Which scenario best matches Route source preference?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to Route source preference when the routing or OSPF evidence depends on this distinction: Route source preference uses administrative distance only when multiple sources advertise the same prefix length."
        },
        {
          "id": "b",
          "text": "A scenario points to Default gateway redundancy when the routing or OSPF evidence depends on this distinction: Default gateway redundancy lets hosts keep using a gateway address even if one physical router or Layer 3 switch fails."
        },
        {
          "id": "c",
          "text": "A scenario points to Virtual IP address when the routing or OSPF evidence depends on this distinction: A virtual IP address is the shared gateway address used by hosts in a first hop redundancy design."
        },
        {
          "id": "d",
          "text": "A scenario points to Virtual MAC address when the routing or OSPF evidence depends on this distinction: A virtual MAC address is used by the active first-hop device so hosts can forward frames to a redundant gateway."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "Route source preference uses administrative distance only when multiple sources advertise the same prefix length. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer.",
        "b": "Default gateway redundancy refers to Default gateway redundancy lets hosts keep using a gateway address even if one physical router or Layer 3 switch fails. Route source preference is different because the scenario depends on route preference, route source trust, default routing, or metric comparison.",
        "c": "Virtual IP address refers to A virtual IP address is the shared gateway address used by hosts in a first hop redundancy design. Route source preference is different because the scenario depends on route preference, route source trust, default routing, or metric comparison.",
        "d": "Virtual MAC address refers to A virtual MAC address is used by the active first-hop device so hosts can forward frames to a redundant gateway. Route source preference is different because the scenario depends on route preference, route source trust, default routing, or metric comparison."
      }
    },
    {
      "id": 364,
      "domain": "3.0",
      "objective": "3.2",
      "keyword": "Route source preference",
      "difficulty": "hard",
      "text": "A Cisco administrator must predict exactly which route is installed or used for a packet. Which answer applies Route source preference most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "Forwarding decision is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A router forwarding decision uses the destination IP address, routing table, longest prefix match, and route attributes to choose an outgoing path."
        },
        {
          "id": "b",
          "text": "Route source preference is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: Route source preference uses administrative distance only when multiple sources advertise the same prefix length."
        },
        {
          "id": "c",
          "text": "Routing table is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A routing table lists known routes and the information a router uses to choose where to forward packets."
        },
        {
          "id": "d",
          "text": "Routing protocol code is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A routing protocol code identifies how a route was learned, such as connected, static, OSPF, or another source."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "Forwarding decision refers to A router forwarding decision uses the destination IP address, routing table, longest prefix match, and route attributes to choose an outgoing path. Route source preference is different because the scenario depends on route preference, route source trust, default routing, or metric comparison.",
        "b": "Route source preference uses administrative distance only when multiple sources advertise the same prefix length. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer.",
        "c": "Routing table refers to A routing table lists known routes and the information a router uses to choose where to forward packets. Route source preference is different because the scenario depends on route preference, route source trust, default routing, or metric comparison.",
        "d": "Routing protocol code refers to A routing protocol code identifies how a route was learned, such as connected, static, OSPF, or another source. Route source preference is different because the scenario depends on route preference, route source trust, default routing, or metric comparison."
      }
    },
    {
      "id": 365,
      "domain": "3.0",
      "objective": "3.2",
      "keyword": "Metric comparison",
      "difficulty": "easy",
      "text": "Select the description that best identifies Metric comparison.",
      "answers": [
        {
          "id": "a",
          "text": "An OSPF router ID uniquely identifies an OSPF router in the OSPF domain."
        },
        {
          "id": "b",
          "text": "OSPF router ID selection uses a manually configured router ID first, then the highest loopback IP, then the highest active physical interface IP."
        },
        {
          "id": "c",
          "text": "An OSPF point-to-point network type connects two routers without DR or BDR election."
        },
        {
          "id": "d",
          "text": "Metric comparison chooses the best path among routes from the same routing protocol or route source."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "OSPF router ID refers to An OSPF router ID uniquely identifies an OSPF router in the OSPF domain. Metric comparison is different because the scenario depends on route preference, route source trust, default routing, or metric comparison.",
        "b": "OSPF router ID selection refers to OSPF router ID selection uses a manually configured router ID first, then the highest loopback IP, then the highest active physical interface IP. Metric comparison is different because the scenario depends on route preference, route source trust, default routing, or metric comparison.",
        "c": "OSPF point-to-point network refers to An OSPF point-to-point network type connects two routers without DR or BDR election. Metric comparison is different because the scenario depends on route preference, route source trust, default routing, or metric comparison.",
        "d": "Metric comparison chooses the best path among routes from the same routing protocol or route source. This is the correct answer."
      }
    },
    {
      "id": 366,
      "domain": "3.0",
      "objective": "3.2",
      "keyword": "Metric comparison",
      "difficulty": "medium",
      "text": "A router is choosing a path using information related to Metric comparison. Which answer best describes the routing behavior?",
      "answers": [
        {
          "id": "a",
          "text": "Metric comparison is the best match when the IP connectivity scenario requires this behavior: Metric comparison chooses the best path among routes from the same routing protocol or route source."
        },
        {
          "id": "b",
          "text": "OSPF network type mismatch is the best match when the IP connectivity scenario requires this behavior: OSPF network type mismatch can affect adjacency behavior, DR/BDR expectations, or route exchange."
        },
        {
          "id": "c",
          "text": "OSPF wildcard mask is the best match when the IP connectivity scenario requires this behavior: An OSPF wildcard mask in a network command identifies which interface addresses match for OSPF activation."
        },
        {
          "id": "d",
          "text": "OSPF process ID is the best match when the IP connectivity scenario requires this behavior: The OSPF process ID locally identifies an OSPF routing process and does not need to match between routers."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "Metric comparison chooses the best path among routes from the same routing protocol or route source. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer.",
        "b": "OSPF network type mismatch refers to OSPF network type mismatch can affect adjacency behavior, DR/BDR expectations, or route exchange. Metric comparison is different because the scenario depends on route preference, route source trust, default routing, or metric comparison.",
        "c": "OSPF wildcard mask refers to An OSPF wildcard mask in a network command identifies which interface addresses match for OSPF activation. Metric comparison is different because the scenario depends on route preference, route source trust, default routing, or metric comparison.",
        "d": "OSPF process ID refers to The OSPF process ID locally identifies an OSPF routing process and does not need to match between routers. Metric comparison is different because the scenario depends on route preference, route source trust, default routing, or metric comparison."
      }
    },
    {
      "id": 367,
      "domain": "3.0",
      "objective": "3.2",
      "keyword": "Metric comparison",
      "difficulty": "medium-hard",
      "text": "A router has multiple possible routes to a destination. Which scenario best matches Metric comparison?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to Routing metric when the routing or OSPF evidence depends on this distinction: A routing metric ranks paths within the same routing protocol or route source."
        },
        {
          "id": "b",
          "text": "A scenario points to Metric comparison when the routing or OSPF evidence depends on this distinction: Metric comparison chooses the best path among routes from the same routing protocol or route source."
        },
        {
          "id": "c",
          "text": "A scenario points to Gateway of last resort when the routing or OSPF evidence depends on this distinction: The gateway of last resort is the default route used when no more specific route matches the destination."
        },
        {
          "id": "d",
          "text": "A scenario points to Route source preference when the routing or OSPF evidence depends on this distinction: Route source preference uses administrative distance only when multiple sources advertise the same prefix length."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "Routing metric refers to A routing metric ranks paths within the same routing protocol or route source. Metric comparison is different because the scenario depends on route preference, route source trust, default routing, or metric comparison.",
        "b": "Metric comparison chooses the best path among routes from the same routing protocol or route source. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer.",
        "c": "Gateway of last resort refers to The gateway of last resort is the default route used when no more specific route matches the destination. Metric comparison is different because the scenario depends on route preference, route source trust, default routing, or metric comparison.",
        "d": "Route source preference refers to Route source preference uses administrative distance only when multiple sources advertise the same prefix length. Metric comparison is different because the scenario depends on route preference, route source trust, default routing, or metric comparison."
      }
    },
    {
      "id": 368,
      "domain": "3.0",
      "objective": "3.2",
      "keyword": "Metric comparison",
      "difficulty": "hard",
      "text": "A Cisco administrator must predict exactly which route is installed or used for a packet. Which answer applies Metric comparison most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "IPv6 static route is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: An IPv6 static route is a manually configured route for an IPv6 destination prefix."
        },
        {
          "id": "b",
          "text": "Default route is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A default route matches destinations that do not match any more specific route."
        },
        {
          "id": "c",
          "text": "Metric comparison is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: Metric comparison chooses the best path among routes from the same routing protocol or route source."
        },
        {
          "id": "d",
          "text": "Network route is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A network route points to an entire subnet or network rather than one host."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "IPv6 static route refers to An IPv6 static route is a manually configured route for an IPv6 destination prefix. Metric comparison is different because the scenario depends on route preference, route source trust, default routing, or metric comparison.",
        "b": "Default route refers to A default route matches destinations that do not match any more specific route. Metric comparison is different because the scenario depends on route preference, route source trust, default routing, or metric comparison.",
        "c": "Metric comparison chooses the best path among routes from the same routing protocol or route source. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer.",
        "d": "Network route refers to A network route points to an entire subnet or network rather than one host. Metric comparison is different because the scenario depends on route preference, route source trust, default routing, or metric comparison."
      }
    },
    {
      "id": 369,
      "domain": "3.0",
      "objective": "3.3",
      "keyword": "IPv4 static route",
      "difficulty": "easy",
      "text": "Which description best matches IPv4 static route in CCNA Objective 3.0 IP Connectivity?",
      "answers": [
        {
          "id": "a",
          "text": "An IPv4 static route is a manually configured route for an IPv4 destination prefix."
        },
        {
          "id": "b",
          "text": "OSPF adjacency verification checks neighbor state, interface participation, area, timers, and reachability to confirm OSPF operation."
        },
        {
          "id": "c",
          "text": "OSPF route verification checks that OSPF-learned routes appear in the routing table and are used for forwarding."
        },
        {
          "id": "d",
          "text": "An OSPF area mismatch prevents neighbors from forming because both ends of the link must agree on the area."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "An IPv4 static route is a manually configured route for an IPv4 destination prefix. This is the correct answer.",
        "b": "OSPF adjacency verification refers to OSPF adjacency verification checks neighbor state, interface participation, area, timers, and reachability to confirm OSPF operation. IPv4 static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "c": "OSPF route verification refers to OSPF route verification checks that OSPF-learned routes appear in the routing table and are used for forwarding. IPv4 static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "d": "OSPF mismatched area refers to An OSPF area mismatch prevents neighbors from forming because both ends of the link must agree on the area. IPv4 static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step."
      }
    },
    {
      "id": 370,
      "domain": "3.0",
      "objective": "3.3",
      "keyword": "IPv4 static route",
      "difficulty": "medium",
      "text": "A static route must be configured or verified using IPv4 static route. Which answer best describes the concept?",
      "answers": [
        {
          "id": "a",
          "text": "Active router is the best match when the IP connectivity scenario requires this behavior: An active router in an FHRP group forwards traffic sent to the virtual gateway."
        },
        {
          "id": "b",
          "text": "IPv4 static route is the best match when the IP connectivity scenario requires this behavior: An IPv4 static route is a manually configured route for an IPv4 destination prefix."
        },
        {
          "id": "c",
          "text": "Standby router is the best match when the IP connectivity scenario requires this behavior: A standby router waits to take over forwarding if the active first-hop device fails."
        },
        {
          "id": "d",
          "text": "FHRP failover is the best match when the IP connectivity scenario requires this behavior: FHRP failover moves the active gateway role to another device when the current active device becomes unavailable."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "Active router refers to An active router in an FHRP group forwards traffic sent to the virtual gateway. IPv4 static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "b": "An IPv4 static route is a manually configured route for an IPv4 destination prefix. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer.",
        "c": "Standby router refers to A standby router waits to take over forwarding if the active first-hop device fails. IPv4 static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "d": "FHRP failover refers to FHRP failover moves the active gateway role to another device when the current active device becomes unavailable. IPv4 static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step."
      }
    },
    {
      "id": 371,
      "domain": "3.0",
      "objective": "3.3",
      "keyword": "IPv4 static route",
      "difficulty": "medium-hard",
      "text": "A manually configured route must behave correctly during normal operation or failover. Which scenario best matches IPv4 static route?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to Static route verification when the routing or OSPF evidence depends on this distinction: Static route verification confirms configured static routes appear in the routing table and forward traffic as intended."
        },
        {
          "id": "b",
          "text": "A scenario points to Static route prefix specificity when the routing or OSPF evidence depends on this distinction: Static route prefix specificity determines whether a default, network, or host route is the most specific match for a destination."
        },
        {
          "id": "c",
          "text": "A scenario points to IPv4 static route when the routing or OSPF evidence depends on this distinction: An IPv4 static route is a manually configured route for an IPv4 destination prefix."
        },
        {
          "id": "d",
          "text": "A scenario points to Routing table when the routing or OSPF evidence depends on this distinction: A routing table lists known routes and the information a router uses to choose where to forward packets."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "Static route verification refers to Static route verification confirms configured static routes appear in the routing table and forward traffic as intended. IPv4 static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "b": "Static route prefix specificity refers to Static route prefix specificity determines whether a default, network, or host route is the most specific match for a destination. IPv4 static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "c": "An IPv4 static route is a manually configured route for an IPv4 destination prefix. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer.",
        "d": "Routing table refers to A routing table lists known routes and the information a router uses to choose where to forward packets. IPv4 static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step."
      }
    },
    {
      "id": 372,
      "domain": "3.0",
      "objective": "3.3",
      "keyword": "IPv4 static route",
      "difficulty": "hard",
      "text": "A static route design must support specific prefixes, backup paths, and correct verification. Which answer applies IPv4 static route most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "Forwarding decision is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A router forwarding decision uses the destination IP address, routing table, longest prefix match, and route attributes to choose an outgoing path."
        },
        {
          "id": "b",
          "text": "Route source preference is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: Route source preference uses administrative distance only when multiple sources advertise the same prefix length."
        },
        {
          "id": "c",
          "text": "Metric comparison is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: Metric comparison chooses the best path among routes from the same routing protocol or route source."
        },
        {
          "id": "d",
          "text": "IPv4 static route is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: An IPv4 static route is a manually configured route for an IPv4 destination prefix."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "Forwarding decision refers to A router forwarding decision uses the destination IP address, routing table, longest prefix match, and route attributes to choose an outgoing path. IPv4 static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "b": "Route source preference refers to Route source preference uses administrative distance only when multiple sources advertise the same prefix length. IPv4 static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "c": "Metric comparison refers to Metric comparison chooses the best path among routes from the same routing protocol or route source. IPv4 static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "d": "An IPv4 static route is a manually configured route for an IPv4 destination prefix. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer."
      }
    },
    {
      "id": 373,
      "domain": "3.0",
      "objective": "3.3",
      "keyword": "IPv6 static route",
      "difficulty": "easy",
      "text": "Which option correctly defines IPv6 static route?",
      "answers": [
        {
          "id": "a",
          "text": "First hop redundancy protocols provide a redundant default gateway for hosts on a LAN."
        },
        {
          "id": "b",
          "text": "An IPv6 static route is a manually configured route for an IPv6 destination prefix."
        },
        {
          "id": "c",
          "text": "Default gateway redundancy lets hosts keep using a gateway address even if one physical router or Layer 3 switch fails."
        },
        {
          "id": "d",
          "text": "A virtual IP address is the shared gateway address used by hosts in a first hop redundancy design."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "First hop redundancy protocols refers to First hop redundancy protocols provide a redundant default gateway for hosts on a LAN. IPv6 static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "b": "An IPv6 static route is a manually configured route for an IPv6 destination prefix. This is the correct answer.",
        "c": "Default gateway redundancy refers to Default gateway redundancy lets hosts keep using a gateway address even if one physical router or Layer 3 switch fails. IPv6 static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "d": "Virtual IP address refers to A virtual IP address is the shared gateway address used by hosts in a first hop redundancy design. IPv6 static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step."
      }
    },
    {
      "id": 374,
      "domain": "3.0",
      "objective": "3.3",
      "keyword": "IPv6 static route",
      "difficulty": "medium",
      "text": "A static route must be configured or verified using IPv6 static route. Which answer best describes the concept?",
      "answers": [
        {
          "id": "a",
          "text": "Floating static route is the best match when the IP connectivity scenario requires this behavior: A floating static route uses a higher administrative distance so it is installed only when a better route is unavailable."
        },
        {
          "id": "b",
          "text": "Recursive static route is the best match when the IP connectivity scenario requires this behavior: A recursive static route points to a next-hop IP address that must itself be resolved through another route."
        },
        {
          "id": "c",
          "text": "IPv6 static route is the best match when the IP connectivity scenario requires this behavior: An IPv6 static route is a manually configured route for an IPv6 destination prefix."
        },
        {
          "id": "d",
          "text": "Fully specified static route is the best match when the IP connectivity scenario requires this behavior: A fully specified static route includes both the exit interface and next-hop address."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "Floating static route refers to A floating static route uses a higher administrative distance so it is installed only when a better route is unavailable. IPv6 static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "b": "Recursive static route refers to A recursive static route points to a next-hop IP address that must itself be resolved through another route. IPv6 static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "c": "An IPv6 static route is a manually configured route for an IPv6 destination prefix. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer.",
        "d": "Fully specified static route refers to A fully specified static route includes both the exit interface and next-hop address. IPv6 static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step."
      }
    },
    {
      "id": 375,
      "domain": "3.0",
      "objective": "3.3",
      "keyword": "IPv6 static route",
      "difficulty": "medium-hard",
      "text": "A manually configured route must behave correctly during normal operation or failover. Which scenario best matches IPv6 static route?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to Administrative distance when the routing or OSPF evidence depends on this distinction: Administrative distance ranks the trustworthiness of route sources when the same prefix is learned from multiple sources."
        },
        {
          "id": "b",
          "text": "A scenario points to Routing metric when the routing or OSPF evidence depends on this distinction: A routing metric ranks paths within the same routing protocol or route source."
        },
        {
          "id": "c",
          "text": "A scenario points to Gateway of last resort when the routing or OSPF evidence depends on this distinction: The gateway of last resort is the default route used when no more specific route matches the destination."
        },
        {
          "id": "d",
          "text": "A scenario points to IPv6 static route when the routing or OSPF evidence depends on this distinction: An IPv6 static route is a manually configured route for an IPv6 destination prefix."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "Administrative distance refers to Administrative distance ranks the trustworthiness of route sources when the same prefix is learned from multiple sources. IPv6 static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "b": "Routing metric refers to A routing metric ranks paths within the same routing protocol or route source. IPv6 static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "c": "Gateway of last resort refers to The gateway of last resort is the default route used when no more specific route matches the destination. IPv6 static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "d": "An IPv6 static route is a manually configured route for an IPv6 destination prefix. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer."
      }
    },
    {
      "id": 376,
      "domain": "3.0",
      "objective": "3.3",
      "keyword": "IPv6 static route",
      "difficulty": "hard",
      "text": "A static route design must support specific prefixes, backup paths, and correct verification. Which answer applies IPv6 static route most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "IPv6 static route is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: An IPv6 static route is a manually configured route for an IPv6 destination prefix."
        },
        {
          "id": "b",
          "text": "OSPF network command is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: The OSPF network command enables OSPF on matching interfaces and assigns them to an area."
        },
        {
          "id": "c",
          "text": "OSPF passive interface is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: An OSPF passive interface advertises the connected network but does not send OSPF hello packets on that interface."
        },
        {
          "id": "d",
          "text": "OSPF router ID is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: An OSPF router ID uniquely identifies an OSPF router in the OSPF domain."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "An IPv6 static route is a manually configured route for an IPv6 destination prefix. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer.",
        "b": "OSPF network command refers to The OSPF network command enables OSPF on matching interfaces and assigns them to an area. IPv6 static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "c": "OSPF passive interface refers to An OSPF passive interface advertises the connected network but does not send OSPF hello packets on that interface. IPv6 static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "d": "OSPF router ID refers to An OSPF router ID uniquely identifies an OSPF router in the OSPF domain. IPv6 static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step."
      }
    },
    {
      "id": 377,
      "domain": "3.0",
      "objective": "3.3",
      "keyword": "Default route",
      "difficulty": "easy",
      "text": "How is Default route best understood in an IP connectivity context?",
      "answers": [
        {
          "id": "a",
          "text": "An IPv4 static route is a manually configured route for an IPv4 destination prefix."
        },
        {
          "id": "b",
          "text": "An IPv6 static route is a manually configured route for an IPv6 destination prefix."
        },
        {
          "id": "c",
          "text": "A default route matches destinations that do not match any more specific route."
        },
        {
          "id": "d",
          "text": "A network route points to an entire subnet or network rather than one host."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "IPv4 static route refers to An IPv4 static route is a manually configured route for an IPv4 destination prefix. Default route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "b": "IPv6 static route refers to An IPv6 static route is a manually configured route for an IPv6 destination prefix. Default route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "c": "A default route matches destinations that do not match any more specific route. This is the correct answer.",
        "d": "Network route refers to A network route points to an entire subnet or network rather than one host. Default route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step."
      }
    },
    {
      "id": 378,
      "domain": "3.0",
      "objective": "3.3",
      "keyword": "Default route",
      "difficulty": "medium",
      "text": "A static route must be configured or verified using Default route. Which answer best describes the concept?",
      "answers": [
        {
          "id": "a",
          "text": "Routing protocol code is the best match when the IP connectivity scenario requires this behavior: A routing protocol code identifies how a route was learned, such as connected, static, OSPF, or another source."
        },
        {
          "id": "b",
          "text": "Route prefix is the best match when the IP connectivity scenario requires this behavior: A route prefix identifies the destination network or subnet in a routing table entry."
        },
        {
          "id": "c",
          "text": "Network mask is the best match when the IP connectivity scenario requires this behavior: A network mask or prefix length identifies which bits represent the network portion of an address."
        },
        {
          "id": "d",
          "text": "Default route is the best match when the IP connectivity scenario requires this behavior: A default route matches destinations that do not match any more specific route."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "Routing protocol code refers to A routing protocol code identifies how a route was learned, such as connected, static, OSPF, or another source. Default route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "b": "Route prefix refers to A route prefix identifies the destination network or subnet in a routing table entry. Default route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "c": "Network mask refers to A network mask or prefix length identifies which bits represent the network portion of an address. Default route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "d": "A default route matches destinations that do not match any more specific route. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer."
      }
    },
    {
      "id": 379,
      "domain": "3.0",
      "objective": "3.3",
      "keyword": "Default route",
      "difficulty": "medium-hard",
      "text": "A manually configured route must behave correctly during normal operation or failover. Which scenario best matches Default route?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to Default route when the routing or OSPF evidence depends on this distinction: A default route matches destinations that do not match any more specific route."
        },
        {
          "id": "b",
          "text": "A scenario points to Single-area OSPFv2 when the routing or OSPF evidence depends on this distinction: Single-area OSPFv2 runs OSPF for IPv4 within one area, commonly area 0, to exchange routes dynamically."
        },
        {
          "id": "c",
          "text": "A scenario points to OSPF neighbor adjacency when the routing or OSPF evidence depends on this distinction: An OSPF neighbor adjacency is the relationship formed between OSPF routers that successfully exchange hello packets and required parameters."
        },
        {
          "id": "d",
          "text": "A scenario points to OSPF hello packets when the routing or OSPF evidence depends on this distinction: OSPF hello packets discover neighbors and maintain OSPF neighbor relationships."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "A default route matches destinations that do not match any more specific route. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer.",
        "b": "Single-area OSPFv2 refers to Single-area OSPFv2 runs OSPF for IPv4 within one area, commonly area 0, to exchange routes dynamically. Default route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "c": "OSPF neighbor adjacency refers to An OSPF neighbor adjacency is the relationship formed between OSPF routers that successfully exchange hello packets and required parameters. Default route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "d": "OSPF hello packets refers to OSPF hello packets discover neighbors and maintain OSPF neighbor relationships. Default route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step."
      }
    },
    {
      "id": 380,
      "domain": "3.0",
      "objective": "3.3",
      "keyword": "Default route",
      "difficulty": "hard",
      "text": "A static route design must support specific prefixes, backup paths, and correct verification. Which answer applies Default route most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "BDR election is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: Backup designated router election selects a standby router to take over if the DR fails."
        },
        {
          "id": "b",
          "text": "Default route is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A default route matches destinations that do not match any more specific route."
        },
        {
          "id": "c",
          "text": "OSPF cost is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: OSPF cost is the metric used by OSPF to choose preferred paths."
        },
        {
          "id": "d",
          "text": "OSPF adjacency verification is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: OSPF adjacency verification checks neighbor state, interface participation, area, timers, and reachability to confirm OSPF operation."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "BDR election refers to Backup designated router election selects a standby router to take over if the DR fails. Default route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "b": "A default route matches destinations that do not match any more specific route. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer.",
        "c": "OSPF cost refers to OSPF cost is the metric used by OSPF to choose preferred paths. Default route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "d": "OSPF adjacency verification refers to OSPF adjacency verification checks neighbor state, interface participation, area, timers, and reachability to confirm OSPF operation. Default route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step."
      }
    },
    {
      "id": 381,
      "domain": "3.0",
      "objective": "3.3",
      "keyword": "Network route",
      "difficulty": "easy",
      "text": "Select the description that best identifies Network route.",
      "answers": [
        {
          "id": "a",
          "text": "An exit-interface static route specifies the local outgoing interface used to forward traffic."
        },
        {
          "id": "b",
          "text": "Static route verification confirms configured static routes appear in the routing table and forward traffic as intended."
        },
        {
          "id": "c",
          "text": "Static route prefix specificity determines whether a default, network, or host route is the most specific match for a destination."
        },
        {
          "id": "d",
          "text": "A network route points to an entire subnet or network rather than one host."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "Exit-interface static route refers to An exit-interface static route specifies the local outgoing interface used to forward traffic. Network route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "b": "Static route verification refers to Static route verification confirms configured static routes appear in the routing table and forward traffic as intended. Network route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "c": "Static route prefix specificity refers to Static route prefix specificity determines whether a default, network, or host route is the most specific match for a destination. Network route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "d": "A network route points to an entire subnet or network rather than one host. This is the correct answer."
      }
    },
    {
      "id": 382,
      "domain": "3.0",
      "objective": "3.3",
      "keyword": "Network route",
      "difficulty": "medium",
      "text": "A static route must be configured or verified using Network route. Which answer best describes the concept?",
      "answers": [
        {
          "id": "a",
          "text": "Network route is the best match when the IP connectivity scenario requires this behavior: A network route points to an entire subnet or network rather than one host."
        },
        {
          "id": "b",
          "text": "Longest prefix match is the best match when the IP connectivity scenario requires this behavior: Longest prefix match selects the route with the most specific matching prefix for the destination IP address."
        },
        {
          "id": "c",
          "text": "Forwarding decision is the best match when the IP connectivity scenario requires this behavior: A router forwarding decision uses the destination IP address, routing table, longest prefix match, and route attributes to choose an outgoing path."
        },
        {
          "id": "d",
          "text": "Route source preference is the best match when the IP connectivity scenario requires this behavior: Route source preference uses administrative distance only when multiple sources advertise the same prefix length."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "A network route points to an entire subnet or network rather than one host. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer.",
        "b": "Longest prefix match refers to Longest prefix match selects the route with the most specific matching prefix for the destination IP address. Network route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "c": "Forwarding decision refers to A router forwarding decision uses the destination IP address, routing table, longest prefix match, and route attributes to choose an outgoing path. Network route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "d": "Route source preference refers to Route source preference uses administrative distance only when multiple sources advertise the same prefix length. Network route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step."
      }
    },
    {
      "id": 383,
      "domain": "3.0",
      "objective": "3.3",
      "keyword": "Network route",
      "difficulty": "medium-hard",
      "text": "A manually configured route must behave correctly during normal operation or failover. Which scenario best matches Network route?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to OSPF router ID selection when the routing or OSPF evidence depends on this distinction: OSPF router ID selection uses a manually configured router ID first, then the highest loopback IP, then the highest active physical interface IP."
        },
        {
          "id": "b",
          "text": "A scenario points to Network route when the routing or OSPF evidence depends on this distinction: A network route points to an entire subnet or network rather than one host."
        },
        {
          "id": "c",
          "text": "A scenario points to OSPF point-to-point network when the routing or OSPF evidence depends on this distinction: An OSPF point-to-point network type connects two routers without DR or BDR election."
        },
        {
          "id": "d",
          "text": "A scenario points to OSPF broadcast network when the routing or OSPF evidence depends on this distinction: An OSPF broadcast network type supports multiaccess Ethernet segments and uses DR/BDR election."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "OSPF router ID selection refers to OSPF router ID selection uses a manually configured router ID first, then the highest loopback IP, then the highest active physical interface IP. Network route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "b": "A network route points to an entire subnet or network rather than one host. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer.",
        "c": "OSPF point-to-point network refers to An OSPF point-to-point network type connects two routers without DR or BDR election. Network route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "d": "OSPF broadcast network refers to An OSPF broadcast network type supports multiaccess Ethernet segments and uses DR/BDR election. Network route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step."
      }
    },
    {
      "id": 384,
      "domain": "3.0",
      "objective": "3.3",
      "keyword": "Network route",
      "difficulty": "hard",
      "text": "A static route design must support specific prefixes, backup paths, and correct verification. Which answer applies Network route most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "OSPF wildcard mask is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: An OSPF wildcard mask in a network command identifies which interface addresses match for OSPF activation."
        },
        {
          "id": "b",
          "text": "OSPF process ID is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: The OSPF process ID locally identifies an OSPF routing process and does not need to match between routers."
        },
        {
          "id": "c",
          "text": "Network route is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A network route points to an entire subnet or network rather than one host."
        },
        {
          "id": "d",
          "text": "First hop redundancy protocols is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: First hop redundancy protocols provide a redundant default gateway for hosts on a LAN."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "OSPF wildcard mask refers to An OSPF wildcard mask in a network command identifies which interface addresses match for OSPF activation. Network route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "b": "OSPF process ID refers to The OSPF process ID locally identifies an OSPF routing process and does not need to match between routers. Network route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "c": "A network route points to an entire subnet or network rather than one host. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer.",
        "d": "First hop redundancy protocols refers to First hop redundancy protocols provide a redundant default gateway for hosts on a LAN. Network route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step."
      }
    },
    {
      "id": 385,
      "domain": "3.0",
      "objective": "3.3",
      "keyword": "Host route",
      "difficulty": "easy",
      "text": "Which description best matches Host route in CCNA Objective 3.0 IP Connectivity?",
      "answers": [
        {
          "id": "a",
          "text": "A host route points to one specific host address, typically using /32 for IPv4 or /128 for IPv6."
        },
        {
          "id": "b",
          "text": "A next hop is the next router IP address to which a packet is sent on the way to the destination."
        },
        {
          "id": "c",
          "text": "Administrative distance ranks the trustworthiness of route sources when the same prefix is learned from multiple sources."
        },
        {
          "id": "d",
          "text": "A routing metric ranks paths within the same routing protocol or route source."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "A host route points to one specific host address, typically using /32 for IPv4 or /128 for IPv6. This is the correct answer.",
        "b": "Next hop refers to A next hop is the next router IP address to which a packet is sent on the way to the destination. Host route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "c": "Administrative distance refers to Administrative distance ranks the trustworthiness of route sources when the same prefix is learned from multiple sources. Host route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "d": "Routing metric refers to A routing metric ranks paths within the same routing protocol or route source. Host route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step."
      }
    },
    {
      "id": 386,
      "domain": "3.0",
      "objective": "3.3",
      "keyword": "Host route",
      "difficulty": "medium",
      "text": "A static route must be configured or verified using Host route. Which answer best describes the concept?",
      "answers": [
        {
          "id": "a",
          "text": "OSPF area is the best match when the IP connectivity scenario requires this behavior: An OSPF area groups routers and link-state information; single-area CCNA scenarios commonly use area 0."
        },
        {
          "id": "b",
          "text": "Host route is the best match when the IP connectivity scenario requires this behavior: A host route points to one specific host address, typically using /32 for IPv4 or /128 for IPv6."
        },
        {
          "id": "c",
          "text": "OSPF network command is the best match when the IP connectivity scenario requires this behavior: The OSPF network command enables OSPF on matching interfaces and assigns them to an area."
        },
        {
          "id": "d",
          "text": "OSPF passive interface is the best match when the IP connectivity scenario requires this behavior: An OSPF passive interface advertises the connected network but does not send OSPF hello packets on that interface."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "OSPF area refers to An OSPF area groups routers and link-state information; single-area CCNA scenarios commonly use area 0. Host route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "b": "A host route points to one specific host address, typically using /32 for IPv4 or /128 for IPv6. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer.",
        "c": "OSPF network command refers to The OSPF network command enables OSPF on matching interfaces and assigns them to an area. Host route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "d": "OSPF passive interface refers to An OSPF passive interface advertises the connected network but does not send OSPF hello packets on that interface. Host route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step."
      }
    },
    {
      "id": 387,
      "domain": "3.0",
      "objective": "3.3",
      "keyword": "Host route",
      "difficulty": "medium-hard",
      "text": "A manually configured route must behave correctly during normal operation or failover. Which scenario best matches Host route?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to OSPF route verification when the routing or OSPF evidence depends on this distinction: OSPF route verification checks that OSPF-learned routes appear in the routing table and are used for forwarding."
        },
        {
          "id": "b",
          "text": "A scenario points to OSPF mismatched area when the routing or OSPF evidence depends on this distinction: An OSPF area mismatch prevents neighbors from forming because both ends of the link must agree on the area."
        },
        {
          "id": "c",
          "text": "A scenario points to Host route when the routing or OSPF evidence depends on this distinction: A host route points to one specific host address, typically using /32 for IPv4 or /128 for IPv6."
        },
        {
          "id": "d",
          "text": "A scenario points to OSPF mismatched timers when the routing or OSPF evidence depends on this distinction: OSPF hello and dead timer mismatches can prevent neighbor adjacency from forming."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "OSPF route verification refers to OSPF route verification checks that OSPF-learned routes appear in the routing table and are used for forwarding. Host route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "b": "OSPF mismatched area refers to An OSPF area mismatch prevents neighbors from forming because both ends of the link must agree on the area. Host route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "c": "A host route points to one specific host address, typically using /32 for IPv4 or /128 for IPv6. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer.",
        "d": "OSPF mismatched timers refers to OSPF hello and dead timer mismatches can prevent neighbor adjacency from forming. Host route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step."
      }
    },
    {
      "id": 388,
      "domain": "3.0",
      "objective": "3.3",
      "keyword": "Host route",
      "difficulty": "hard",
      "text": "A static route design must support specific prefixes, backup paths, and correct verification. Which answer applies Host route most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "Standby router is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A standby router waits to take over forwarding if the active first-hop device fails."
        },
        {
          "id": "b",
          "text": "FHRP failover is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: FHRP failover moves the active gateway role to another device when the current active device becomes unavailable."
        },
        {
          "id": "c",
          "text": "IPv4 static route is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: An IPv4 static route is a manually configured route for an IPv4 destination prefix."
        },
        {
          "id": "d",
          "text": "Host route is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A host route points to one specific host address, typically using /32 for IPv4 or /128 for IPv6."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "Standby router refers to A standby router waits to take over forwarding if the active first-hop device fails. Host route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "b": "FHRP failover refers to FHRP failover moves the active gateway role to another device when the current active device becomes unavailable. Host route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "c": "IPv4 static route refers to An IPv4 static route is a manually configured route for an IPv4 destination prefix. Host route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "d": "A host route points to one specific host address, typically using /32 for IPv4 or /128 for IPv6. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer."
      }
    },
    {
      "id": 389,
      "domain": "3.0",
      "objective": "3.3",
      "keyword": "Floating static route",
      "difficulty": "easy",
      "text": "Which option correctly defines Floating static route?",
      "answers": [
        {
          "id": "a",
          "text": "Metric comparison chooses the best path among routes from the same routing protocol or route source."
        },
        {
          "id": "b",
          "text": "A floating static route uses a higher administrative distance so it is installed only when a better route is unavailable."
        },
        {
          "id": "c",
          "text": "Single-area OSPFv2 runs OSPF for IPv4 within one area, commonly area 0, to exchange routes dynamically."
        },
        {
          "id": "d",
          "text": "An OSPF neighbor adjacency is the relationship formed between OSPF routers that successfully exchange hello packets and required parameters."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "Metric comparison refers to Metric comparison chooses the best path among routes from the same routing protocol or route source. Floating static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "b": "A floating static route uses a higher administrative distance so it is installed only when a better route is unavailable. This is the correct answer.",
        "c": "Single-area OSPFv2 refers to Single-area OSPFv2 runs OSPF for IPv4 within one area, commonly area 0, to exchange routes dynamically. Floating static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "d": "OSPF neighbor adjacency refers to An OSPF neighbor adjacency is the relationship formed between OSPF routers that successfully exchange hello packets and required parameters. Floating static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step."
      }
    },
    {
      "id": 390,
      "domain": "3.0",
      "objective": "3.3",
      "keyword": "Floating static route",
      "difficulty": "medium",
      "text": "A static route must be configured or verified using Floating static route. Which answer best describes the concept?",
      "answers": [
        {
          "id": "a",
          "text": "DR election is the best match when the IP connectivity scenario requires this behavior: Designated router election selects a router on an OSPF broadcast network to reduce adjacency overhead."
        },
        {
          "id": "b",
          "text": "BDR election is the best match when the IP connectivity scenario requires this behavior: Backup designated router election selects a standby router to take over if the DR fails."
        },
        {
          "id": "c",
          "text": "Floating static route is the best match when the IP connectivity scenario requires this behavior: A floating static route uses a higher administrative distance so it is installed only when a better route is unavailable."
        },
        {
          "id": "d",
          "text": "OSPF cost is the best match when the IP connectivity scenario requires this behavior: OSPF cost is the metric used by OSPF to choose preferred paths."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "DR election refers to Designated router election selects a router on an OSPF broadcast network to reduce adjacency overhead. Floating static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "b": "BDR election refers to Backup designated router election selects a standby router to take over if the DR fails. Floating static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "c": "A floating static route uses a higher administrative distance so it is installed only when a better route is unavailable. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer.",
        "d": "OSPF cost refers to OSPF cost is the metric used by OSPF to choose preferred paths. Floating static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step."
      }
    },
    {
      "id": 391,
      "domain": "3.0",
      "objective": "3.3",
      "keyword": "Floating static route",
      "difficulty": "medium-hard",
      "text": "A manually configured route must behave correctly during normal operation or failover. Which scenario best matches Floating static route?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to Default gateway redundancy when the routing or OSPF evidence depends on this distinction: Default gateway redundancy lets hosts keep using a gateway address even if one physical router or Layer 3 switch fails."
        },
        {
          "id": "b",
          "text": "A scenario points to Virtual IP address when the routing or OSPF evidence depends on this distinction: A virtual IP address is the shared gateway address used by hosts in a first hop redundancy design."
        },
        {
          "id": "c",
          "text": "A scenario points to Virtual MAC address when the routing or OSPF evidence depends on this distinction: A virtual MAC address is used by the active first-hop device so hosts can forward frames to a redundant gateway."
        },
        {
          "id": "d",
          "text": "A scenario points to Floating static route when the routing or OSPF evidence depends on this distinction: A floating static route uses a higher administrative distance so it is installed only when a better route is unavailable."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "Default gateway redundancy refers to Default gateway redundancy lets hosts keep using a gateway address even if one physical router or Layer 3 switch fails. Floating static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "b": "Virtual IP address refers to A virtual IP address is the shared gateway address used by hosts in a first hop redundancy design. Floating static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "c": "Virtual MAC address refers to A virtual MAC address is used by the active first-hop device so hosts can forward frames to a redundant gateway. Floating static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "d": "A floating static route uses a higher administrative distance so it is installed only when a better route is unavailable. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer."
      }
    },
    {
      "id": 392,
      "domain": "3.0",
      "objective": "3.3",
      "keyword": "Floating static route",
      "difficulty": "hard",
      "text": "A static route design must support specific prefixes, backup paths, and correct verification. Which answer applies Floating static route most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "Floating static route is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A floating static route uses a higher administrative distance so it is installed only when a better route is unavailable."
        },
        {
          "id": "b",
          "text": "Recursive static route is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A recursive static route points to a next-hop IP address that must itself be resolved through another route."
        },
        {
          "id": "c",
          "text": "Fully specified static route is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A fully specified static route includes both the exit interface and next-hop address."
        },
        {
          "id": "d",
          "text": "Exit-interface static route is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: An exit-interface static route specifies the local outgoing interface used to forward traffic."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "A floating static route uses a higher administrative distance so it is installed only when a better route is unavailable. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer.",
        "b": "Recursive static route refers to A recursive static route points to a next-hop IP address that must itself be resolved through another route. Floating static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "c": "Fully specified static route refers to A fully specified static route includes both the exit interface and next-hop address. Floating static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "d": "Exit-interface static route refers to An exit-interface static route specifies the local outgoing interface used to forward traffic. Floating static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step."
      }
    },
    {
      "id": 393,
      "domain": "3.0",
      "objective": "3.3",
      "keyword": "Recursive static route",
      "difficulty": "easy",
      "text": "How is Recursive static route best understood in an IP connectivity context?",
      "answers": [
        {
          "id": "a",
          "text": "An OSPF router ID uniquely identifies an OSPF router in the OSPF domain."
        },
        {
          "id": "b",
          "text": "OSPF router ID selection uses a manually configured router ID first, then the highest loopback IP, then the highest active physical interface IP."
        },
        {
          "id": "c",
          "text": "A recursive static route points to a next-hop IP address that must itself be resolved through another route."
        },
        {
          "id": "d",
          "text": "An OSPF point-to-point network type connects two routers without DR or BDR election."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "OSPF router ID refers to An OSPF router ID uniquely identifies an OSPF router in the OSPF domain. Recursive static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "b": "OSPF router ID selection refers to OSPF router ID selection uses a manually configured router ID first, then the highest loopback IP, then the highest active physical interface IP. Recursive static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "c": "A recursive static route points to a next-hop IP address that must itself be resolved through another route. This is the correct answer.",
        "d": "OSPF point-to-point network refers to An OSPF point-to-point network type connects two routers without DR or BDR election. Recursive static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step."
      }
    },
    {
      "id": 394,
      "domain": "3.0",
      "objective": "3.3",
      "keyword": "Recursive static route",
      "difficulty": "medium",
      "text": "A static route must be configured or verified using Recursive static route. Which answer best describes the concept?",
      "answers": [
        {
          "id": "a",
          "text": "OSPF network type mismatch is the best match when the IP connectivity scenario requires this behavior: OSPF network type mismatch can affect adjacency behavior, DR/BDR expectations, or route exchange."
        },
        {
          "id": "b",
          "text": "OSPF wildcard mask is the best match when the IP connectivity scenario requires this behavior: An OSPF wildcard mask in a network command identifies which interface addresses match for OSPF activation."
        },
        {
          "id": "c",
          "text": "OSPF process ID is the best match when the IP connectivity scenario requires this behavior: The OSPF process ID locally identifies an OSPF routing process and does not need to match between routers."
        },
        {
          "id": "d",
          "text": "Recursive static route is the best match when the IP connectivity scenario requires this behavior: A recursive static route points to a next-hop IP address that must itself be resolved through another route."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "OSPF network type mismatch refers to OSPF network type mismatch can affect adjacency behavior, DR/BDR expectations, or route exchange. Recursive static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "b": "OSPF wildcard mask refers to An OSPF wildcard mask in a network command identifies which interface addresses match for OSPF activation. Recursive static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "c": "OSPF process ID refers to The OSPF process ID locally identifies an OSPF routing process and does not need to match between routers. Recursive static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "d": "A recursive static route points to a next-hop IP address that must itself be resolved through another route. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer."
      }
    },
    {
      "id": 395,
      "domain": "3.0",
      "objective": "3.3",
      "keyword": "Recursive static route",
      "difficulty": "medium-hard",
      "text": "A manually configured route must behave correctly during normal operation or failover. Which scenario best matches Recursive static route?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to Recursive static route when the routing or OSPF evidence depends on this distinction: A recursive static route points to a next-hop IP address that must itself be resolved through another route."
        },
        {
          "id": "b",
          "text": "A scenario points to IPv6 static route when the routing or OSPF evidence depends on this distinction: An IPv6 static route is a manually configured route for an IPv6 destination prefix."
        },
        {
          "id": "c",
          "text": "A scenario points to Default route when the routing or OSPF evidence depends on this distinction: A default route matches destinations that do not match any more specific route."
        },
        {
          "id": "d",
          "text": "A scenario points to Network route when the routing or OSPF evidence depends on this distinction: A network route points to an entire subnet or network rather than one host."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "A recursive static route points to a next-hop IP address that must itself be resolved through another route. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer.",
        "b": "IPv6 static route refers to An IPv6 static route is a manually configured route for an IPv6 destination prefix. Recursive static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "c": "Default route refers to A default route matches destinations that do not match any more specific route. Recursive static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "d": "Network route refers to A network route points to an entire subnet or network rather than one host. Recursive static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step."
      }
    },
    {
      "id": 396,
      "domain": "3.0",
      "objective": "3.3",
      "keyword": "Recursive static route",
      "difficulty": "hard",
      "text": "A static route design must support specific prefixes, backup paths, and correct verification. Which answer applies Recursive static route most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "Route prefix is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A route prefix identifies the destination network or subnet in a routing table entry."
        },
        {
          "id": "b",
          "text": "Recursive static route is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A recursive static route points to a next-hop IP address that must itself be resolved through another route."
        },
        {
          "id": "c",
          "text": "Network mask is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A network mask or prefix length identifies which bits represent the network portion of an address."
        },
        {
          "id": "d",
          "text": "Next hop is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A next hop is the next router IP address to which a packet is sent on the way to the destination."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "Route prefix refers to A route prefix identifies the destination network or subnet in a routing table entry. Recursive static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "b": "A recursive static route points to a next-hop IP address that must itself be resolved through another route. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer.",
        "c": "Network mask refers to A network mask or prefix length identifies which bits represent the network portion of an address. Recursive static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "d": "Next hop refers to A next hop is the next router IP address to which a packet is sent on the way to the destination. Recursive static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step."
      }
    },
    {
      "id": 397,
      "domain": "3.0",
      "objective": "3.3",
      "keyword": "Fully specified static route",
      "difficulty": "easy",
      "text": "Select the description that best identifies Fully specified static route.",
      "answers": [
        {
          "id": "a",
          "text": "OSPF adjacency verification checks neighbor state, interface participation, area, timers, and reachability to confirm OSPF operation."
        },
        {
          "id": "b",
          "text": "OSPF route verification checks that OSPF-learned routes appear in the routing table and are used for forwarding."
        },
        {
          "id": "c",
          "text": "An OSPF area mismatch prevents neighbors from forming because both ends of the link must agree on the area."
        },
        {
          "id": "d",
          "text": "A fully specified static route includes both the exit interface and next-hop address."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "OSPF adjacency verification refers to OSPF adjacency verification checks neighbor state, interface participation, area, timers, and reachability to confirm OSPF operation. Fully specified static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "b": "OSPF route verification refers to OSPF route verification checks that OSPF-learned routes appear in the routing table and are used for forwarding. Fully specified static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "c": "OSPF mismatched area refers to An OSPF area mismatch prevents neighbors from forming because both ends of the link must agree on the area. Fully specified static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "d": "A fully specified static route includes both the exit interface and next-hop address. This is the correct answer."
      }
    },
    {
      "id": 398,
      "domain": "3.0",
      "objective": "3.3",
      "keyword": "Fully specified static route",
      "difficulty": "medium",
      "text": "A static route must be configured or verified using Fully specified static route. Which answer best describes the concept?",
      "answers": [
        {
          "id": "a",
          "text": "Fully specified static route is the best match when the IP connectivity scenario requires this behavior: A fully specified static route includes both the exit interface and next-hop address."
        },
        {
          "id": "b",
          "text": "Active router is the best match when the IP connectivity scenario requires this behavior: An active router in an FHRP group forwards traffic sent to the virtual gateway."
        },
        {
          "id": "c",
          "text": "Standby router is the best match when the IP connectivity scenario requires this behavior: A standby router waits to take over forwarding if the active first-hop device fails."
        },
        {
          "id": "d",
          "text": "FHRP failover is the best match when the IP connectivity scenario requires this behavior: FHRP failover moves the active gateway role to another device when the current active device becomes unavailable."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "A fully specified static route includes both the exit interface and next-hop address. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer.",
        "b": "Active router refers to An active router in an FHRP group forwards traffic sent to the virtual gateway. Fully specified static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "c": "Standby router refers to A standby router waits to take over forwarding if the active first-hop device fails. Fully specified static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "d": "FHRP failover refers to FHRP failover moves the active gateway role to another device when the current active device becomes unavailable. Fully specified static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step."
      }
    },
    {
      "id": 399,
      "domain": "3.0",
      "objective": "3.3",
      "keyword": "Fully specified static route",
      "difficulty": "medium-hard",
      "text": "A manually configured route must behave correctly during normal operation or failover. Which scenario best matches Fully specified static route?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to Static route verification when the routing or OSPF evidence depends on this distinction: Static route verification confirms configured static routes appear in the routing table and forward traffic as intended."
        },
        {
          "id": "b",
          "text": "A scenario points to Fully specified static route when the routing or OSPF evidence depends on this distinction: A fully specified static route includes both the exit interface and next-hop address."
        },
        {
          "id": "c",
          "text": "A scenario points to Static route prefix specificity when the routing or OSPF evidence depends on this distinction: Static route prefix specificity determines whether a default, network, or host route is the most specific match for a destination."
        },
        {
          "id": "d",
          "text": "A scenario points to Routing table when the routing or OSPF evidence depends on this distinction: A routing table lists known routes and the information a router uses to choose where to forward packets."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "Static route verification refers to Static route verification confirms configured static routes appear in the routing table and forward traffic as intended. Fully specified static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "b": "A fully specified static route includes both the exit interface and next-hop address. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer.",
        "c": "Static route prefix specificity refers to Static route prefix specificity determines whether a default, network, or host route is the most specific match for a destination. Fully specified static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "d": "Routing table refers to A routing table lists known routes and the information a router uses to choose where to forward packets. Fully specified static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step."
      }
    },
    {
      "id": 400,
      "domain": "3.0",
      "objective": "3.3",
      "keyword": "Fully specified static route",
      "difficulty": "hard",
      "text": "A static route design must support specific prefixes, backup paths, and correct verification. Which answer applies Fully specified static route most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "Forwarding decision is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A router forwarding decision uses the destination IP address, routing table, longest prefix match, and route attributes to choose an outgoing path."
        },
        {
          "id": "b",
          "text": "Route source preference is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: Route source preference uses administrative distance only when multiple sources advertise the same prefix length."
        },
        {
          "id": "c",
          "text": "Fully specified static route is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A fully specified static route includes both the exit interface and next-hop address."
        },
        {
          "id": "d",
          "text": "Metric comparison is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: Metric comparison chooses the best path among routes from the same routing protocol or route source."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "Forwarding decision refers to A router forwarding decision uses the destination IP address, routing table, longest prefix match, and route attributes to choose an outgoing path. Fully specified static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "b": "Route source preference refers to Route source preference uses administrative distance only when multiple sources advertise the same prefix length. Fully specified static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "c": "A fully specified static route includes both the exit interface and next-hop address. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer.",
        "d": "Metric comparison refers to Metric comparison chooses the best path among routes from the same routing protocol or route source. Fully specified static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step."
      }
    },
    {
      "id": 401,
      "domain": "3.0",
      "objective": "3.3",
      "keyword": "Exit-interface static route",
      "difficulty": "easy",
      "text": "Which description best matches Exit-interface static route in CCNA Objective 3.0 IP Connectivity?",
      "answers": [
        {
          "id": "a",
          "text": "An exit-interface static route specifies the local outgoing interface used to forward traffic."
        },
        {
          "id": "b",
          "text": "First hop redundancy protocols provide a redundant default gateway for hosts on a LAN."
        },
        {
          "id": "c",
          "text": "Default gateway redundancy lets hosts keep using a gateway address even if one physical router or Layer 3 switch fails."
        },
        {
          "id": "d",
          "text": "A virtual IP address is the shared gateway address used by hosts in a first hop redundancy design."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "An exit-interface static route specifies the local outgoing interface used to forward traffic. This is the correct answer.",
        "b": "First hop redundancy protocols refers to First hop redundancy protocols provide a redundant default gateway for hosts on a LAN. Exit-interface static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "c": "Default gateway redundancy refers to Default gateway redundancy lets hosts keep using a gateway address even if one physical router or Layer 3 switch fails. Exit-interface static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "d": "Virtual IP address refers to A virtual IP address is the shared gateway address used by hosts in a first hop redundancy design. Exit-interface static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step."
      }
    },
    {
      "id": 402,
      "domain": "3.0",
      "objective": "3.3",
      "keyword": "Exit-interface static route",
      "difficulty": "medium",
      "text": "A static route must be configured or verified using Exit-interface static route. Which answer best describes the concept?",
      "answers": [
        {
          "id": "a",
          "text": "Host route is the best match when the IP connectivity scenario requires this behavior: A host route points to one specific host address, typically using /32 for IPv4 or /128 for IPv6."
        },
        {
          "id": "b",
          "text": "Exit-interface static route is the best match when the IP connectivity scenario requires this behavior: An exit-interface static route specifies the local outgoing interface used to forward traffic."
        },
        {
          "id": "c",
          "text": "Floating static route is the best match when the IP connectivity scenario requires this behavior: A floating static route uses a higher administrative distance so it is installed only when a better route is unavailable."
        },
        {
          "id": "d",
          "text": "Recursive static route is the best match when the IP connectivity scenario requires this behavior: A recursive static route points to a next-hop IP address that must itself be resolved through another route."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "Host route refers to A host route points to one specific host address, typically using /32 for IPv4 or /128 for IPv6. Exit-interface static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "b": "An exit-interface static route specifies the local outgoing interface used to forward traffic. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer.",
        "c": "Floating static route refers to A floating static route uses a higher administrative distance so it is installed only when a better route is unavailable. Exit-interface static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "d": "Recursive static route refers to A recursive static route points to a next-hop IP address that must itself be resolved through another route. Exit-interface static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step."
      }
    },
    {
      "id": 403,
      "domain": "3.0",
      "objective": "3.3",
      "keyword": "Exit-interface static route",
      "difficulty": "medium-hard",
      "text": "A manually configured route must behave correctly during normal operation or failover. Which scenario best matches Exit-interface static route?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to Administrative distance when the routing or OSPF evidence depends on this distinction: Administrative distance ranks the trustworthiness of route sources when the same prefix is learned from multiple sources."
        },
        {
          "id": "b",
          "text": "A scenario points to Routing metric when the routing or OSPF evidence depends on this distinction: A routing metric ranks paths within the same routing protocol or route source."
        },
        {
          "id": "c",
          "text": "A scenario points to Exit-interface static route when the routing or OSPF evidence depends on this distinction: An exit-interface static route specifies the local outgoing interface used to forward traffic."
        },
        {
          "id": "d",
          "text": "A scenario points to Gateway of last resort when the routing or OSPF evidence depends on this distinction: The gateway of last resort is the default route used when no more specific route matches the destination."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "Administrative distance refers to Administrative distance ranks the trustworthiness of route sources when the same prefix is learned from multiple sources. Exit-interface static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "b": "Routing metric refers to A routing metric ranks paths within the same routing protocol or route source. Exit-interface static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "c": "An exit-interface static route specifies the local outgoing interface used to forward traffic. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer.",
        "d": "Gateway of last resort refers to The gateway of last resort is the default route used when no more specific route matches the destination. Exit-interface static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step."
      }
    },
    {
      "id": 404,
      "domain": "3.0",
      "objective": "3.3",
      "keyword": "Exit-interface static route",
      "difficulty": "hard",
      "text": "A static route design must support specific prefixes, backup paths, and correct verification. Which answer applies Exit-interface static route most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "OSPF network command is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: The OSPF network command enables OSPF on matching interfaces and assigns them to an area."
        },
        {
          "id": "b",
          "text": "OSPF passive interface is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: An OSPF passive interface advertises the connected network but does not send OSPF hello packets on that interface."
        },
        {
          "id": "c",
          "text": "OSPF router ID is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: An OSPF router ID uniquely identifies an OSPF router in the OSPF domain."
        },
        {
          "id": "d",
          "text": "Exit-interface static route is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: An exit-interface static route specifies the local outgoing interface used to forward traffic."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "OSPF network command refers to The OSPF network command enables OSPF on matching interfaces and assigns them to an area. Exit-interface static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "b": "OSPF passive interface refers to An OSPF passive interface advertises the connected network but does not send OSPF hello packets on that interface. Exit-interface static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "c": "OSPF router ID refers to An OSPF router ID uniquely identifies an OSPF router in the OSPF domain. Exit-interface static route is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "d": "An exit-interface static route specifies the local outgoing interface used to forward traffic. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer."
      }
    },
    {
      "id": 405,
      "domain": "3.0",
      "objective": "3.3",
      "keyword": "Static route verification",
      "difficulty": "easy",
      "text": "Which option correctly defines Static route verification?",
      "answers": [
        {
          "id": "a",
          "text": "An IPv4 static route is a manually configured route for an IPv4 destination prefix."
        },
        {
          "id": "b",
          "text": "Static route verification confirms configured static routes appear in the routing table and forward traffic as intended."
        },
        {
          "id": "c",
          "text": "An IPv6 static route is a manually configured route for an IPv6 destination prefix."
        },
        {
          "id": "d",
          "text": "A default route matches destinations that do not match any more specific route."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "IPv4 static route refers to An IPv4 static route is a manually configured route for an IPv4 destination prefix. Static route verification is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "b": "Static route verification confirms configured static routes appear in the routing table and forward traffic as intended. This is the correct answer.",
        "c": "IPv6 static route refers to An IPv6 static route is a manually configured route for an IPv6 destination prefix. Static route verification is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "d": "Default route refers to A default route matches destinations that do not match any more specific route. Static route verification is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step."
      }
    },
    {
      "id": 406,
      "domain": "3.0",
      "objective": "3.3",
      "keyword": "Static route verification",
      "difficulty": "medium",
      "text": "A static route must be configured or verified using Static route verification. Which answer best describes the concept?",
      "answers": [
        {
          "id": "a",
          "text": "Routing protocol code is the best match when the IP connectivity scenario requires this behavior: A routing protocol code identifies how a route was learned, such as connected, static, OSPF, or another source."
        },
        {
          "id": "b",
          "text": "Route prefix is the best match when the IP connectivity scenario requires this behavior: A route prefix identifies the destination network or subnet in a routing table entry."
        },
        {
          "id": "c",
          "text": "Static route verification is the best match when the IP connectivity scenario requires this behavior: Static route verification confirms configured static routes appear in the routing table and forward traffic as intended."
        },
        {
          "id": "d",
          "text": "Network mask is the best match when the IP connectivity scenario requires this behavior: A network mask or prefix length identifies which bits represent the network portion of an address."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "Routing protocol code refers to A routing protocol code identifies how a route was learned, such as connected, static, OSPF, or another source. Static route verification is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "b": "Route prefix refers to A route prefix identifies the destination network or subnet in a routing table entry. Static route verification is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "c": "Static route verification confirms configured static routes appear in the routing table and forward traffic as intended. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer.",
        "d": "Network mask refers to A network mask or prefix length identifies which bits represent the network portion of an address. Static route verification is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step."
      }
    },
    {
      "id": 407,
      "domain": "3.0",
      "objective": "3.3",
      "keyword": "Static route verification",
      "difficulty": "medium-hard",
      "text": "A manually configured route must behave correctly during normal operation or failover. Which scenario best matches Static route verification?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to Single-area OSPFv2 when the routing or OSPF evidence depends on this distinction: Single-area OSPFv2 runs OSPF for IPv4 within one area, commonly area 0, to exchange routes dynamically."
        },
        {
          "id": "b",
          "text": "A scenario points to OSPF neighbor adjacency when the routing or OSPF evidence depends on this distinction: An OSPF neighbor adjacency is the relationship formed between OSPF routers that successfully exchange hello packets and required parameters."
        },
        {
          "id": "c",
          "text": "A scenario points to OSPF hello packets when the routing or OSPF evidence depends on this distinction: OSPF hello packets discover neighbors and maintain OSPF neighbor relationships."
        },
        {
          "id": "d",
          "text": "A scenario points to Static route verification when the routing or OSPF evidence depends on this distinction: Static route verification confirms configured static routes appear in the routing table and forward traffic as intended."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "Single-area OSPFv2 refers to Single-area OSPFv2 runs OSPF for IPv4 within one area, commonly area 0, to exchange routes dynamically. Static route verification is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "b": "OSPF neighbor adjacency refers to An OSPF neighbor adjacency is the relationship formed between OSPF routers that successfully exchange hello packets and required parameters. Static route verification is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "c": "OSPF hello packets refers to OSPF hello packets discover neighbors and maintain OSPF neighbor relationships. Static route verification is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "d": "Static route verification confirms configured static routes appear in the routing table and forward traffic as intended. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer."
      }
    },
    {
      "id": 408,
      "domain": "3.0",
      "objective": "3.3",
      "keyword": "Static route verification",
      "difficulty": "hard",
      "text": "A static route design must support specific prefixes, backup paths, and correct verification. Which answer applies Static route verification most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "Static route verification is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: Static route verification confirms configured static routes appear in the routing table and forward traffic as intended."
        },
        {
          "id": "b",
          "text": "BDR election is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: Backup designated router election selects a standby router to take over if the DR fails."
        },
        {
          "id": "c",
          "text": "OSPF cost is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: OSPF cost is the metric used by OSPF to choose preferred paths."
        },
        {
          "id": "d",
          "text": "OSPF adjacency verification is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: OSPF adjacency verification checks neighbor state, interface participation, area, timers, and reachability to confirm OSPF operation."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "Static route verification confirms configured static routes appear in the routing table and forward traffic as intended. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer.",
        "b": "BDR election refers to Backup designated router election selects a standby router to take over if the DR fails. Static route verification is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "c": "OSPF cost refers to OSPF cost is the metric used by OSPF to choose preferred paths. Static route verification is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "d": "OSPF adjacency verification refers to OSPF adjacency verification checks neighbor state, interface participation, area, timers, and reachability to confirm OSPF operation. Static route verification is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step."
      }
    },
    {
      "id": 409,
      "domain": "3.0",
      "objective": "3.3",
      "keyword": "Static route prefix specificity",
      "difficulty": "easy",
      "text": "How is Static route prefix specificity best understood in an IP connectivity context?",
      "answers": [
        {
          "id": "a",
          "text": "A fully specified static route includes both the exit interface and next-hop address."
        },
        {
          "id": "b",
          "text": "An exit-interface static route specifies the local outgoing interface used to forward traffic."
        },
        {
          "id": "c",
          "text": "Static route prefix specificity determines whether a default, network, or host route is the most specific match for a destination."
        },
        {
          "id": "d",
          "text": "Static route verification confirms configured static routes appear in the routing table and forward traffic as intended."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "Fully specified static route refers to A fully specified static route includes both the exit interface and next-hop address. Static route prefix specificity is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "b": "Exit-interface static route refers to An exit-interface static route specifies the local outgoing interface used to forward traffic. Static route prefix specificity is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "c": "Static route prefix specificity determines whether a default, network, or host route is the most specific match for a destination. This is the correct answer.",
        "d": "Static route verification refers to Static route verification confirms configured static routes appear in the routing table and forward traffic as intended. Static route prefix specificity is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step."
      }
    },
    {
      "id": 410,
      "domain": "3.0",
      "objective": "3.3",
      "keyword": "Static route prefix specificity",
      "difficulty": "medium",
      "text": "A static route must be configured or verified using Static route prefix specificity. Which answer best describes the concept?",
      "answers": [
        {
          "id": "a",
          "text": "Longest prefix match is the best match when the IP connectivity scenario requires this behavior: Longest prefix match selects the route with the most specific matching prefix for the destination IP address."
        },
        {
          "id": "b",
          "text": "Forwarding decision is the best match when the IP connectivity scenario requires this behavior: A router forwarding decision uses the destination IP address, routing table, longest prefix match, and route attributes to choose an outgoing path."
        },
        {
          "id": "c",
          "text": "Route source preference is the best match when the IP connectivity scenario requires this behavior: Route source preference uses administrative distance only when multiple sources advertise the same prefix length."
        },
        {
          "id": "d",
          "text": "Static route prefix specificity is the best match when the IP connectivity scenario requires this behavior: Static route prefix specificity determines whether a default, network, or host route is the most specific match for a destination."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "Longest prefix match refers to Longest prefix match selects the route with the most specific matching prefix for the destination IP address. Static route prefix specificity is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "b": "Forwarding decision refers to A router forwarding decision uses the destination IP address, routing table, longest prefix match, and route attributes to choose an outgoing path. Static route prefix specificity is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "c": "Route source preference refers to Route source preference uses administrative distance only when multiple sources advertise the same prefix length. Static route prefix specificity is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "d": "Static route prefix specificity determines whether a default, network, or host route is the most specific match for a destination. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer."
      }
    },
    {
      "id": 411,
      "domain": "3.0",
      "objective": "3.3",
      "keyword": "Static route prefix specificity",
      "difficulty": "medium-hard",
      "text": "A manually configured route must behave correctly during normal operation or failover. Which scenario best matches Static route prefix specificity?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to Static route prefix specificity when the routing or OSPF evidence depends on this distinction: Static route prefix specificity determines whether a default, network, or host route is the most specific match for a destination."
        },
        {
          "id": "b",
          "text": "A scenario points to OSPF router ID selection when the routing or OSPF evidence depends on this distinction: OSPF router ID selection uses a manually configured router ID first, then the highest loopback IP, then the highest active physical interface IP."
        },
        {
          "id": "c",
          "text": "A scenario points to OSPF point-to-point network when the routing or OSPF evidence depends on this distinction: An OSPF point-to-point network type connects two routers without DR or BDR election."
        },
        {
          "id": "d",
          "text": "A scenario points to OSPF broadcast network when the routing or OSPF evidence depends on this distinction: An OSPF broadcast network type supports multiaccess Ethernet segments and uses DR/BDR election."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "Static route prefix specificity determines whether a default, network, or host route is the most specific match for a destination. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer.",
        "b": "OSPF router ID selection refers to OSPF router ID selection uses a manually configured router ID first, then the highest loopback IP, then the highest active physical interface IP. Static route prefix specificity is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "c": "OSPF point-to-point network refers to An OSPF point-to-point network type connects two routers without DR or BDR election. Static route prefix specificity is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "d": "OSPF broadcast network refers to An OSPF broadcast network type supports multiaccess Ethernet segments and uses DR/BDR election. Static route prefix specificity is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step."
      }
    },
    {
      "id": 412,
      "domain": "3.0",
      "objective": "3.3",
      "keyword": "Static route prefix specificity",
      "difficulty": "hard",
      "text": "A static route design must support specific prefixes, backup paths, and correct verification. Which answer applies Static route prefix specificity most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "OSPF wildcard mask is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: An OSPF wildcard mask in a network command identifies which interface addresses match for OSPF activation."
        },
        {
          "id": "b",
          "text": "Static route prefix specificity is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: Static route prefix specificity determines whether a default, network, or host route is the most specific match for a destination."
        },
        {
          "id": "c",
          "text": "OSPF process ID is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: The OSPF process ID locally identifies an OSPF routing process and does not need to match between routers."
        },
        {
          "id": "d",
          "text": "First hop redundancy protocols is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: First hop redundancy protocols provide a redundant default gateway for hosts on a LAN."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "OSPF wildcard mask refers to An OSPF wildcard mask in a network command identifies which interface addresses match for OSPF activation. Static route prefix specificity is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "b": "Static route prefix specificity determines whether a default, network, or host route is the most specific match for a destination. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer.",
        "c": "OSPF process ID refers to The OSPF process ID locally identifies an OSPF routing process and does not need to match between routers. Static route prefix specificity is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step.",
        "d": "First hop redundancy protocols refers to First hop redundancy protocols provide a redundant default gateway for hosts on a LAN. Static route prefix specificity is different because the scenario depends on the exact static-route type, next-hop behavior, prefix specificity, or verification step."
      }
    },
    {
      "id": 413,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "Single-area OSPFv2",
      "difficulty": "easy",
      "text": "Select the description that best identifies Single-area OSPFv2.",
      "answers": [
        {
          "id": "a",
          "text": "An OSPF area mismatch prevents neighbors from forming because both ends of the link must agree on the area."
        },
        {
          "id": "b",
          "text": "OSPF hello and dead timer mismatches can prevent neighbor adjacency from forming."
        },
        {
          "id": "c",
          "text": "OSPF network type mismatch can affect adjacency behavior, DR/BDR expectations, or route exchange."
        },
        {
          "id": "d",
          "text": "Single-area OSPFv2 runs OSPF for IPv4 within one area, commonly area 0, to exchange routes dynamically."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "OSPF mismatched area refers to An OSPF area mismatch prevents neighbors from forming because both ends of the link must agree on the area. Single-area OSPFv2 is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "b": "OSPF mismatched timers refers to OSPF hello and dead timer mismatches can prevent neighbor adjacency from forming. Single-area OSPFv2 is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "c": "OSPF network type mismatch refers to OSPF network type mismatch can affect adjacency behavior, DR/BDR expectations, or route exchange. Single-area OSPFv2 is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "d": "Single-area OSPFv2 runs OSPF for IPv4 within one area, commonly area 0, to exchange routes dynamically. This is the correct answer."
      }
    },
    {
      "id": 414,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "Single-area OSPFv2",
      "difficulty": "medium",
      "text": "An OSPFv2 single-area configuration references Single-area OSPFv2. Which answer best describes the OSPF behavior?",
      "answers": [
        {
          "id": "a",
          "text": "Single-area OSPFv2 is the best match when the IP connectivity scenario requires this behavior: Single-area OSPFv2 runs OSPF for IPv4 within one area, commonly area 0, to exchange routes dynamically."
        },
        {
          "id": "b",
          "text": "Routing metric is the best match when the IP connectivity scenario requires this behavior: A routing metric ranks paths within the same routing protocol or route source."
        },
        {
          "id": "c",
          "text": "Gateway of last resort is the best match when the IP connectivity scenario requires this behavior: The gateway of last resort is the default route used when no more specific route matches the destination."
        },
        {
          "id": "d",
          "text": "Longest prefix match is the best match when the IP connectivity scenario requires this behavior: Longest prefix match selects the route with the most specific matching prefix for the destination IP address."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "Single-area OSPFv2 runs OSPF for IPv4 within one area, commonly area 0, to exchange routes dynamically. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer.",
        "b": "Routing metric refers to A routing metric ranks paths within the same routing protocol or route source. Single-area OSPFv2 is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "c": "Gateway of last resort refers to The gateway of last resort is the default route used when no more specific route matches the destination. Single-area OSPFv2 is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "d": "Longest prefix match refers to Longest prefix match selects the route with the most specific matching prefix for the destination IP address. Single-area OSPFv2 is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection."
      }
    },
    {
      "id": 415,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "Single-area OSPFv2",
      "difficulty": "medium-hard",
      "text": "Two routers should form an OSPF adjacency or exchange routes, but the result is not as expected. Which scenario best matches Single-area OSPFv2?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to Floating static route when the routing or OSPF evidence depends on this distinction: A floating static route uses a higher administrative distance so it is installed only when a better route is unavailable."
        },
        {
          "id": "b",
          "text": "A scenario points to Single-area OSPFv2 when the routing or OSPF evidence depends on this distinction: Single-area OSPFv2 runs OSPF for IPv4 within one area, commonly area 0, to exchange routes dynamically."
        },
        {
          "id": "c",
          "text": "A scenario points to Recursive static route when the routing or OSPF evidence depends on this distinction: A recursive static route points to a next-hop IP address that must itself be resolved through another route."
        },
        {
          "id": "d",
          "text": "A scenario points to Fully specified static route when the routing or OSPF evidence depends on this distinction: A fully specified static route includes both the exit interface and next-hop address."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "Floating static route refers to A floating static route uses a higher administrative distance so it is installed only when a better route is unavailable. Single-area OSPFv2 is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "b": "Single-area OSPFv2 runs OSPF for IPv4 within one area, commonly area 0, to exchange routes dynamically. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer.",
        "c": "Recursive static route refers to A recursive static route points to a next-hop IP address that must itself be resolved through another route. Single-area OSPFv2 is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "d": "Fully specified static route refers to A fully specified static route includes both the exit interface and next-hop address. Single-area OSPFv2 is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection."
      }
    },
    {
      "id": 416,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "Single-area OSPFv2",
      "difficulty": "hard",
      "text": "An OSPF troubleshooting case requires separating router ID, timers, areas, network types, and route installation. Which answer applies Single-area OSPFv2 most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "Standby router is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A standby router waits to take over forwarding if the active first-hop device fails."
        },
        {
          "id": "b",
          "text": "FHRP failover is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: FHRP failover moves the active gateway role to another device when the current active device becomes unavailable."
        },
        {
          "id": "c",
          "text": "Single-area OSPFv2 is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: Single-area OSPFv2 runs OSPF for IPv4 within one area, commonly area 0, to exchange routes dynamically."
        },
        {
          "id": "d",
          "text": "OSPF neighbor adjacency is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: An OSPF neighbor adjacency is the relationship formed between OSPF routers that successfully exchange hello packets and required parameters."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "Standby router refers to A standby router waits to take over forwarding if the active first-hop device fails. Single-area OSPFv2 is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "b": "FHRP failover refers to FHRP failover moves the active gateway role to another device when the current active device becomes unavailable. Single-area OSPFv2 is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "c": "Single-area OSPFv2 runs OSPF for IPv4 within one area, commonly area 0, to exchange routes dynamically. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer.",
        "d": "OSPF neighbor adjacency refers to An OSPF neighbor adjacency is the relationship formed between OSPF routers that successfully exchange hello packets and required parameters. Single-area OSPFv2 is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection."
      }
    },
    {
      "id": 417,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF neighbor adjacency",
      "difficulty": "easy",
      "text": "Which description best matches OSPF neighbor adjacency in CCNA Objective 3.0 IP Connectivity?",
      "answers": [
        {
          "id": "a",
          "text": "An OSPF neighbor adjacency is the relationship formed between OSPF routers that successfully exchange hello packets and required parameters."
        },
        {
          "id": "b",
          "text": "A route prefix identifies the destination network or subnet in a routing table entry."
        },
        {
          "id": "c",
          "text": "A network mask or prefix length identifies which bits represent the network portion of an address."
        },
        {
          "id": "d",
          "text": "A next hop is the next router IP address to which a packet is sent on the way to the destination."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "An OSPF neighbor adjacency is the relationship formed between OSPF routers that successfully exchange hello packets and required parameters. This is the correct answer.",
        "b": "Route prefix refers to A route prefix identifies the destination network or subnet in a routing table entry. OSPF neighbor adjacency is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "c": "Network mask refers to A network mask or prefix length identifies which bits represent the network portion of an address. OSPF neighbor adjacency is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "d": "Next hop refers to A next hop is the next router IP address to which a packet is sent on the way to the destination. OSPF neighbor adjacency is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection."
      }
    },
    {
      "id": 418,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF neighbor adjacency",
      "difficulty": "medium",
      "text": "An OSPFv2 single-area configuration references OSPF neighbor adjacency. Which answer best describes the OSPF behavior?",
      "answers": [
        {
          "id": "a",
          "text": "IPv6 static route is the best match when the IP connectivity scenario requires this behavior: An IPv6 static route is a manually configured route for an IPv6 destination prefix."
        },
        {
          "id": "b",
          "text": "OSPF neighbor adjacency is the best match when the IP connectivity scenario requires this behavior: An OSPF neighbor adjacency is the relationship formed between OSPF routers that successfully exchange hello packets and required parameters."
        },
        {
          "id": "c",
          "text": "Default route is the best match when the IP connectivity scenario requires this behavior: A default route matches destinations that do not match any more specific route."
        },
        {
          "id": "d",
          "text": "Network route is the best match when the IP connectivity scenario requires this behavior: A network route points to an entire subnet or network rather than one host."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "IPv6 static route refers to An IPv6 static route is a manually configured route for an IPv6 destination prefix. OSPF neighbor adjacency is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "b": "An OSPF neighbor adjacency is the relationship formed between OSPF routers that successfully exchange hello packets and required parameters. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer.",
        "c": "Default route refers to A default route matches destinations that do not match any more specific route. OSPF neighbor adjacency is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "d": "Network route refers to A network route points to an entire subnet or network rather than one host. OSPF neighbor adjacency is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection."
      }
    },
    {
      "id": 419,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF neighbor adjacency",
      "difficulty": "medium-hard",
      "text": "Two routers should form an OSPF adjacency or exchange routes, but the result is not as expected. Which scenario best matches OSPF neighbor adjacency?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to Default gateway redundancy when the routing or OSPF evidence depends on this distinction: Default gateway redundancy lets hosts keep using a gateway address even if one physical router or Layer 3 switch fails."
        },
        {
          "id": "b",
          "text": "A scenario points to Virtual IP address when the routing or OSPF evidence depends on this distinction: A virtual IP address is the shared gateway address used by hosts in a first hop redundancy design."
        },
        {
          "id": "c",
          "text": "A scenario points to OSPF neighbor adjacency when the routing or OSPF evidence depends on this distinction: An OSPF neighbor adjacency is the relationship formed between OSPF routers that successfully exchange hello packets and required parameters."
        },
        {
          "id": "d",
          "text": "A scenario points to Virtual MAC address when the routing or OSPF evidence depends on this distinction: A virtual MAC address is used by the active first-hop device so hosts can forward frames to a redundant gateway."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "Default gateway redundancy refers to Default gateway redundancy lets hosts keep using a gateway address even if one physical router or Layer 3 switch fails. OSPF neighbor adjacency is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "b": "Virtual IP address refers to A virtual IP address is the shared gateway address used by hosts in a first hop redundancy design. OSPF neighbor adjacency is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "c": "An OSPF neighbor adjacency is the relationship formed between OSPF routers that successfully exchange hello packets and required parameters. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer.",
        "d": "Virtual MAC address refers to A virtual MAC address is used by the active first-hop device so hosts can forward frames to a redundant gateway. OSPF neighbor adjacency is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection."
      }
    },
    {
      "id": 420,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF neighbor adjacency",
      "difficulty": "hard",
      "text": "An OSPF troubleshooting case requires separating router ID, timers, areas, network types, and route installation. Which answer applies OSPF neighbor adjacency most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "OSPF point-to-point network is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: An OSPF point-to-point network type connects two routers without DR or BDR election."
        },
        {
          "id": "b",
          "text": "OSPF broadcast network is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: An OSPF broadcast network type supports multiaccess Ethernet segments and uses DR/BDR election."
        },
        {
          "id": "c",
          "text": "DR election is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: Designated router election selects a router on an OSPF broadcast network to reduce adjacency overhead."
        },
        {
          "id": "d",
          "text": "OSPF neighbor adjacency is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: An OSPF neighbor adjacency is the relationship formed between OSPF routers that successfully exchange hello packets and required parameters."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "OSPF point-to-point network refers to An OSPF point-to-point network type connects two routers without DR or BDR election. OSPF neighbor adjacency is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "b": "OSPF broadcast network refers to An OSPF broadcast network type supports multiaccess Ethernet segments and uses DR/BDR election. OSPF neighbor adjacency is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "c": "DR election refers to Designated router election selects a router on an OSPF broadcast network to reduce adjacency overhead. OSPF neighbor adjacency is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "d": "An OSPF neighbor adjacency is the relationship formed between OSPF routers that successfully exchange hello packets and required parameters. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer."
      }
    },
    {
      "id": 421,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF hello packets",
      "difficulty": "easy",
      "text": "Which option correctly defines OSPF hello packets?",
      "answers": [
        {
          "id": "a",
          "text": "A router forwarding decision uses the destination IP address, routing table, longest prefix match, and route attributes to choose an outgoing path."
        },
        {
          "id": "b",
          "text": "OSPF hello packets discover neighbors and maintain OSPF neighbor relationships."
        },
        {
          "id": "c",
          "text": "Route source preference uses administrative distance only when multiple sources advertise the same prefix length."
        },
        {
          "id": "d",
          "text": "Metric comparison chooses the best path among routes from the same routing protocol or route source."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "Forwarding decision refers to A router forwarding decision uses the destination IP address, routing table, longest prefix match, and route attributes to choose an outgoing path. OSPF hello packets is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "b": "OSPF hello packets discover neighbors and maintain OSPF neighbor relationships. This is the correct answer.",
        "c": "Route source preference refers to Route source preference uses administrative distance only when multiple sources advertise the same prefix length. OSPF hello packets is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "d": "Metric comparison refers to Metric comparison chooses the best path among routes from the same routing protocol or route source. OSPF hello packets is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection."
      }
    },
    {
      "id": 422,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF hello packets",
      "difficulty": "medium",
      "text": "An OSPFv2 single-area configuration references OSPF hello packets. Which answer best describes the OSPF behavior?",
      "answers": [
        {
          "id": "a",
          "text": "Exit-interface static route is the best match when the IP connectivity scenario requires this behavior: An exit-interface static route specifies the local outgoing interface used to forward traffic."
        },
        {
          "id": "b",
          "text": "Static route verification is the best match when the IP connectivity scenario requires this behavior: Static route verification confirms configured static routes appear in the routing table and forward traffic as intended."
        },
        {
          "id": "c",
          "text": "OSPF hello packets is the best match when the IP connectivity scenario requires this behavior: OSPF hello packets discover neighbors and maintain OSPF neighbor relationships."
        },
        {
          "id": "d",
          "text": "Static route prefix specificity is the best match when the IP connectivity scenario requires this behavior: Static route prefix specificity determines whether a default, network, or host route is the most specific match for a destination."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "Exit-interface static route refers to An exit-interface static route specifies the local outgoing interface used to forward traffic. OSPF hello packets is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "b": "Static route verification refers to Static route verification confirms configured static routes appear in the routing table and forward traffic as intended. OSPF hello packets is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "c": "OSPF hello packets discover neighbors and maintain OSPF neighbor relationships. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer.",
        "d": "Static route prefix specificity refers to Static route prefix specificity determines whether a default, network, or host route is the most specific match for a destination. OSPF hello packets is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection."
      }
    },
    {
      "id": 423,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF hello packets",
      "difficulty": "medium-hard",
      "text": "Two routers should form an OSPF adjacency or exchange routes, but the result is not as expected. Which scenario best matches OSPF hello packets?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to OSPF neighbor adjacency when the routing or OSPF evidence depends on this distinction: An OSPF neighbor adjacency is the relationship formed between OSPF routers that successfully exchange hello packets and required parameters."
        },
        {
          "id": "b",
          "text": "A scenario points to OSPF area when the routing or OSPF evidence depends on this distinction: An OSPF area groups routers and link-state information; single-area CCNA scenarios commonly use area 0."
        },
        {
          "id": "c",
          "text": "A scenario points to OSPF router ID when the routing or OSPF evidence depends on this distinction: An OSPF router ID uniquely identifies an OSPF router in the OSPF domain."
        },
        {
          "id": "d",
          "text": "A scenario points to OSPF hello packets when the routing or OSPF evidence depends on this distinction: OSPF hello packets discover neighbors and maintain OSPF neighbor relationships."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "OSPF neighbor adjacency refers to An OSPF neighbor adjacency is the relationship formed between OSPF routers that successfully exchange hello packets and required parameters. OSPF hello packets is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "b": "OSPF area refers to An OSPF area groups routers and link-state information; single-area CCNA scenarios commonly use area 0. OSPF hello packets is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "c": "OSPF router ID refers to An OSPF router ID uniquely identifies an OSPF router in the OSPF domain. OSPF hello packets is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "d": "OSPF hello packets discover neighbors and maintain OSPF neighbor relationships. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer."
      }
    },
    {
      "id": 424,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF hello packets",
      "difficulty": "hard",
      "text": "An OSPF troubleshooting case requires separating router ID, timers, areas, network types, and route installation. Which answer applies OSPF hello packets most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "OSPF hello packets is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: OSPF hello packets discover neighbors and maintain OSPF neighbor relationships."
        },
        {
          "id": "b",
          "text": "OSPF adjacency verification is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: OSPF adjacency verification checks neighbor state, interface participation, area, timers, and reachability to confirm OSPF operation."
        },
        {
          "id": "c",
          "text": "OSPF route verification is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: OSPF route verification checks that OSPF-learned routes appear in the routing table and are used for forwarding."
        },
        {
          "id": "d",
          "text": "OSPF mismatched area is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: An OSPF area mismatch prevents neighbors from forming because both ends of the link must agree on the area."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "OSPF hello packets discover neighbors and maintain OSPF neighbor relationships. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer.",
        "b": "OSPF adjacency verification refers to OSPF adjacency verification checks neighbor state, interface participation, area, timers, and reachability to confirm OSPF operation. OSPF hello packets is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "c": "OSPF route verification refers to OSPF route verification checks that OSPF-learned routes appear in the routing table and are used for forwarding. OSPF hello packets is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "d": "OSPF mismatched area refers to An OSPF area mismatch prevents neighbors from forming because both ends of the link must agree on the area. OSPF hello packets is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection."
      }
    },
    {
      "id": 425,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF area",
      "difficulty": "easy",
      "text": "How is OSPF area best understood in an IP connectivity context?",
      "answers": [
        {
          "id": "a",
          "text": "A host route points to one specific host address, typically using /32 for IPv4 or /128 for IPv6."
        },
        {
          "id": "b",
          "text": "A floating static route uses a higher administrative distance so it is installed only when a better route is unavailable."
        },
        {
          "id": "c",
          "text": "An OSPF area groups routers and link-state information; single-area CCNA scenarios commonly use area 0."
        },
        {
          "id": "d",
          "text": "A recursive static route points to a next-hop IP address that must itself be resolved through another route."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "Host route refers to A host route points to one specific host address, typically using /32 for IPv4 or /128 for IPv6. OSPF area is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "b": "Floating static route refers to A floating static route uses a higher administrative distance so it is installed only when a better route is unavailable. OSPF area is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "c": "An OSPF area groups routers and link-state information; single-area CCNA scenarios commonly use area 0. This is the correct answer.",
        "d": "Recursive static route refers to A recursive static route points to a next-hop IP address that must itself be resolved through another route. OSPF area is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection."
      }
    },
    {
      "id": 426,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF area",
      "difficulty": "medium",
      "text": "An OSPFv2 single-area configuration references OSPF area. Which answer best describes the OSPF behavior?",
      "answers": [
        {
          "id": "a",
          "text": "Active router is the best match when the IP connectivity scenario requires this behavior: An active router in an FHRP group forwards traffic sent to the virtual gateway."
        },
        {
          "id": "b",
          "text": "Standby router is the best match when the IP connectivity scenario requires this behavior: A standby router waits to take over forwarding if the active first-hop device fails."
        },
        {
          "id": "c",
          "text": "FHRP failover is the best match when the IP connectivity scenario requires this behavior: FHRP failover moves the active gateway role to another device when the current active device becomes unavailable."
        },
        {
          "id": "d",
          "text": "OSPF area is the best match when the IP connectivity scenario requires this behavior: An OSPF area groups routers and link-state information; single-area CCNA scenarios commonly use area 0."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "Active router refers to An active router in an FHRP group forwards traffic sent to the virtual gateway. OSPF area is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "b": "Standby router refers to A standby router waits to take over forwarding if the active first-hop device fails. OSPF area is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "c": "FHRP failover refers to FHRP failover moves the active gateway role to another device when the current active device becomes unavailable. OSPF area is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "d": "An OSPF area groups routers and link-state information; single-area CCNA scenarios commonly use area 0. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer."
      }
    },
    {
      "id": 427,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF area",
      "difficulty": "medium-hard",
      "text": "Two routers should form an OSPF adjacency or exchange routes, but the result is not as expected. Which scenario best matches OSPF area?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to OSPF area when the routing or OSPF evidence depends on this distinction: An OSPF area groups routers and link-state information; single-area CCNA scenarios commonly use area 0."
        },
        {
          "id": "b",
          "text": "A scenario points to BDR election when the routing or OSPF evidence depends on this distinction: Backup designated router election selects a standby router to take over if the DR fails."
        },
        {
          "id": "c",
          "text": "A scenario points to OSPF cost when the routing or OSPF evidence depends on this distinction: OSPF cost is the metric used by OSPF to choose preferred paths."
        },
        {
          "id": "d",
          "text": "A scenario points to OSPF network command when the routing or OSPF evidence depends on this distinction: The OSPF network command enables OSPF on matching interfaces and assigns them to an area."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "An OSPF area groups routers and link-state information; single-area CCNA scenarios commonly use area 0. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer.",
        "b": "BDR election refers to Backup designated router election selects a standby router to take over if the DR fails. OSPF area is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "c": "OSPF cost refers to OSPF cost is the metric used by OSPF to choose preferred paths. OSPF area is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "d": "OSPF network command refers to The OSPF network command enables OSPF on matching interfaces and assigns them to an area. OSPF area is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection."
      }
    },
    {
      "id": 428,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF area",
      "difficulty": "hard",
      "text": "An OSPF troubleshooting case requires separating router ID, timers, areas, network types, and route installation. Which answer applies OSPF area most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "Routing table is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A routing table lists known routes and the information a router uses to choose where to forward packets."
        },
        {
          "id": "b",
          "text": "OSPF area is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: An OSPF area groups routers and link-state information; single-area CCNA scenarios commonly use area 0."
        },
        {
          "id": "c",
          "text": "Routing protocol code is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A routing protocol code identifies how a route was learned, such as connected, static, OSPF, or another source."
        },
        {
          "id": "d",
          "text": "Route prefix is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A route prefix identifies the destination network or subnet in a routing table entry."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "Routing table refers to A routing table lists known routes and the information a router uses to choose where to forward packets. OSPF area is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "b": "An OSPF area groups routers and link-state information; single-area CCNA scenarios commonly use area 0. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer.",
        "c": "Routing protocol code refers to A routing protocol code identifies how a route was learned, such as connected, static, OSPF, or another source. OSPF area is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "d": "Route prefix refers to A route prefix identifies the destination network or subnet in a routing table entry. OSPF area is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection."
      }
    },
    {
      "id": 429,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF network command",
      "difficulty": "easy",
      "text": "Select the description that best identifies OSPF network command.",
      "answers": [
        {
          "id": "a",
          "text": "First hop redundancy protocols provide a redundant default gateway for hosts on a LAN."
        },
        {
          "id": "b",
          "text": "Default gateway redundancy lets hosts keep using a gateway address even if one physical router or Layer 3 switch fails."
        },
        {
          "id": "c",
          "text": "A virtual IP address is the shared gateway address used by hosts in a first hop redundancy design."
        },
        {
          "id": "d",
          "text": "The OSPF network command enables OSPF on matching interfaces and assigns them to an area."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "First hop redundancy protocols refers to First hop redundancy protocols provide a redundant default gateway for hosts on a LAN. OSPF network command is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask.",
        "b": "Default gateway redundancy refers to Default gateway redundancy lets hosts keep using a gateway address even if one physical router or Layer 3 switch fails. OSPF network command is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask.",
        "c": "Virtual IP address refers to A virtual IP address is the shared gateway address used by hosts in a first hop redundancy design. OSPF network command is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask.",
        "d": "The OSPF network command enables OSPF on matching interfaces and assigns them to an area. This is the correct answer."
      }
    },
    {
      "id": 430,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF network command",
      "difficulty": "medium",
      "text": "An OSPFv2 single-area configuration references OSPF network command. Which answer best describes the OSPF behavior?",
      "answers": [
        {
          "id": "a",
          "text": "OSPF network command is the best match when the IP connectivity scenario requires this behavior: The OSPF network command enables OSPF on matching interfaces and assigns them to an area."
        },
        {
          "id": "b",
          "text": "OSPF neighbor adjacency is the best match when the IP connectivity scenario requires this behavior: An OSPF neighbor adjacency is the relationship formed between OSPF routers that successfully exchange hello packets and required parameters."
        },
        {
          "id": "c",
          "text": "OSPF hello packets is the best match when the IP connectivity scenario requires this behavior: OSPF hello packets discover neighbors and maintain OSPF neighbor relationships."
        },
        {
          "id": "d",
          "text": "OSPF area is the best match when the IP connectivity scenario requires this behavior: An OSPF area groups routers and link-state information; single-area CCNA scenarios commonly use area 0."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "The OSPF network command enables OSPF on matching interfaces and assigns them to an area. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer.",
        "b": "OSPF neighbor adjacency refers to An OSPF neighbor adjacency is the relationship formed between OSPF routers that successfully exchange hello packets and required parameters. OSPF network command is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask.",
        "c": "OSPF hello packets refers to OSPF hello packets discover neighbors and maintain OSPF neighbor relationships. OSPF network command is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask.",
        "d": "OSPF area refers to An OSPF area groups routers and link-state information; single-area CCNA scenarios commonly use area 0. OSPF network command is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask."
      }
    },
    {
      "id": 431,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF network command",
      "difficulty": "medium-hard",
      "text": "Two routers should form an OSPF adjacency or exchange routes, but the result is not as expected. Which scenario best matches OSPF network command?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to OSPF route verification when the routing or OSPF evidence depends on this distinction: OSPF route verification checks that OSPF-learned routes appear in the routing table and are used for forwarding."
        },
        {
          "id": "b",
          "text": "A scenario points to OSPF network command when the routing or OSPF evidence depends on this distinction: The OSPF network command enables OSPF on matching interfaces and assigns them to an area."
        },
        {
          "id": "c",
          "text": "A scenario points to OSPF mismatched area when the routing or OSPF evidence depends on this distinction: An OSPF area mismatch prevents neighbors from forming because both ends of the link must agree on the area."
        },
        {
          "id": "d",
          "text": "A scenario points to OSPF mismatched timers when the routing or OSPF evidence depends on this distinction: OSPF hello and dead timer mismatches can prevent neighbor adjacency from forming."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "OSPF route verification refers to OSPF route verification checks that OSPF-learned routes appear in the routing table and are used for forwarding. OSPF network command is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask.",
        "b": "The OSPF network command enables OSPF on matching interfaces and assigns them to an area. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer.",
        "c": "OSPF mismatched area refers to An OSPF area mismatch prevents neighbors from forming because both ends of the link must agree on the area. OSPF network command is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask.",
        "d": "OSPF mismatched timers refers to OSPF hello and dead timer mismatches can prevent neighbor adjacency from forming. OSPF network command is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask."
      }
    },
    {
      "id": 432,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF network command",
      "difficulty": "hard",
      "text": "An OSPF troubleshooting case requires separating router ID, timers, areas, network types, and route installation. Which answer applies OSPF network command most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "Gateway of last resort is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: The gateway of last resort is the default route used when no more specific route matches the destination."
        },
        {
          "id": "b",
          "text": "Longest prefix match is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: Longest prefix match selects the route with the most specific matching prefix for the destination IP address."
        },
        {
          "id": "c",
          "text": "OSPF network command is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: The OSPF network command enables OSPF on matching interfaces and assigns them to an area."
        },
        {
          "id": "d",
          "text": "Forwarding decision is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A router forwarding decision uses the destination IP address, routing table, longest prefix match, and route attributes to choose an outgoing path."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "Gateway of last resort refers to The gateway of last resort is the default route used when no more specific route matches the destination. OSPF network command is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask.",
        "b": "Longest prefix match refers to Longest prefix match selects the route with the most specific matching prefix for the destination IP address. OSPF network command is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask.",
        "c": "The OSPF network command enables OSPF on matching interfaces and assigns them to an area. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer.",
        "d": "Forwarding decision refers to A router forwarding decision uses the destination IP address, routing table, longest prefix match, and route attributes to choose an outgoing path. OSPF network command is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask."
      }
    },
    {
      "id": 433,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF passive interface",
      "difficulty": "easy",
      "text": "Which description best matches OSPF passive interface in CCNA Objective 3.0 IP Connectivity?",
      "answers": [
        {
          "id": "a",
          "text": "An OSPF passive interface advertises the connected network but does not send OSPF hello packets on that interface."
        },
        {
          "id": "b",
          "text": "The OSPF network command enables OSPF on matching interfaces and assigns them to an area."
        },
        {
          "id": "c",
          "text": "An OSPF wildcard mask in a network command identifies which interface addresses match for OSPF activation."
        },
        {
          "id": "d",
          "text": "The OSPF process ID locally identifies an OSPF routing process and does not need to match between routers."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "An OSPF passive interface advertises the connected network but does not send OSPF hello packets on that interface. This is the correct answer.",
        "b": "OSPF network command refers to The OSPF network command enables OSPF on matching interfaces and assigns them to an area. OSPF passive interface is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask.",
        "c": "OSPF wildcard mask refers to An OSPF wildcard mask in a network command identifies which interface addresses match for OSPF activation. OSPF passive interface is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask.",
        "d": "OSPF process ID refers to The OSPF process ID locally identifies an OSPF routing process and does not need to match between routers. OSPF passive interface is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask."
      }
    },
    {
      "id": 434,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF passive interface",
      "difficulty": "medium",
      "text": "An OSPFv2 single-area configuration references OSPF passive interface. Which answer best describes the OSPF behavior?",
      "answers": [
        {
          "id": "a",
          "text": "DR election is the best match when the IP connectivity scenario requires this behavior: Designated router election selects a router on an OSPF broadcast network to reduce adjacency overhead."
        },
        {
          "id": "b",
          "text": "OSPF passive interface is the best match when the IP connectivity scenario requires this behavior: An OSPF passive interface advertises the connected network but does not send OSPF hello packets on that interface."
        },
        {
          "id": "c",
          "text": "BDR election is the best match when the IP connectivity scenario requires this behavior: Backup designated router election selects a standby router to take over if the DR fails."
        },
        {
          "id": "d",
          "text": "OSPF cost is the best match when the IP connectivity scenario requires this behavior: OSPF cost is the metric used by OSPF to choose preferred paths."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "DR election refers to Designated router election selects a router on an OSPF broadcast network to reduce adjacency overhead. OSPF passive interface is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask.",
        "b": "An OSPF passive interface advertises the connected network but does not send OSPF hello packets on that interface. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer.",
        "c": "BDR election refers to Backup designated router election selects a standby router to take over if the DR fails. OSPF passive interface is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask.",
        "d": "OSPF cost refers to OSPF cost is the metric used by OSPF to choose preferred paths. OSPF passive interface is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask."
      }
    },
    {
      "id": 435,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF passive interface",
      "difficulty": "medium-hard",
      "text": "Two routers should form an OSPF adjacency or exchange routes, but the result is not as expected. Which scenario best matches OSPF passive interface?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to Network mask when the routing or OSPF evidence depends on this distinction: A network mask or prefix length identifies which bits represent the network portion of an address."
        },
        {
          "id": "b",
          "text": "A scenario points to Next hop when the routing or OSPF evidence depends on this distinction: A next hop is the next router IP address to which a packet is sent on the way to the destination."
        },
        {
          "id": "c",
          "text": "A scenario points to OSPF passive interface when the routing or OSPF evidence depends on this distinction: An OSPF passive interface advertises the connected network but does not send OSPF hello packets on that interface."
        },
        {
          "id": "d",
          "text": "A scenario points to Administrative distance when the routing or OSPF evidence depends on this distinction: Administrative distance ranks the trustworthiness of route sources when the same prefix is learned from multiple sources."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "Network mask refers to A network mask or prefix length identifies which bits represent the network portion of an address. OSPF passive interface is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask.",
        "b": "Next hop refers to A next hop is the next router IP address to which a packet is sent on the way to the destination. OSPF passive interface is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask.",
        "c": "An OSPF passive interface advertises the connected network but does not send OSPF hello packets on that interface. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer.",
        "d": "Administrative distance refers to Administrative distance ranks the trustworthiness of route sources when the same prefix is learned from multiple sources. OSPF passive interface is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask."
      }
    },
    {
      "id": 436,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF passive interface",
      "difficulty": "hard",
      "text": "An OSPF troubleshooting case requires separating router ID, timers, areas, network types, and route installation. Which answer applies OSPF passive interface most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "Default route is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A default route matches destinations that do not match any more specific route."
        },
        {
          "id": "b",
          "text": "Network route is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A network route points to an entire subnet or network rather than one host."
        },
        {
          "id": "c",
          "text": "Host route is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A host route points to one specific host address, typically using /32 for IPv4 or /128 for IPv6."
        },
        {
          "id": "d",
          "text": "OSPF passive interface is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: An OSPF passive interface advertises the connected network but does not send OSPF hello packets on that interface."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "Default route refers to A default route matches destinations that do not match any more specific route. OSPF passive interface is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask.",
        "b": "Network route refers to A network route points to an entire subnet or network rather than one host. OSPF passive interface is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask.",
        "c": "Host route refers to A host route points to one specific host address, typically using /32 for IPv4 or /128 for IPv6. OSPF passive interface is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask.",
        "d": "An OSPF passive interface advertises the connected network but does not send OSPF hello packets on that interface. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer."
      }
    },
    {
      "id": 437,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF router ID",
      "difficulty": "easy",
      "text": "Which option correctly defines OSPF router ID?",
      "answers": [
        {
          "id": "a",
          "text": "Designated router election selects a router on an OSPF broadcast network to reduce adjacency overhead."
        },
        {
          "id": "b",
          "text": "An OSPF router ID uniquely identifies an OSPF router in the OSPF domain."
        },
        {
          "id": "c",
          "text": "Backup designated router election selects a standby router to take over if the DR fails."
        },
        {
          "id": "d",
          "text": "OSPF cost is the metric used by OSPF to choose preferred paths."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "DR election refers to Designated router election selects a router on an OSPF broadcast network to reduce adjacency overhead. OSPF router ID is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "b": "An OSPF router ID uniquely identifies an OSPF router in the OSPF domain. This is the correct answer.",
        "c": "BDR election refers to Backup designated router election selects a standby router to take over if the DR fails. OSPF router ID is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "d": "OSPF cost refers to OSPF cost is the metric used by OSPF to choose preferred paths. OSPF router ID is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection."
      }
    },
    {
      "id": 438,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF router ID",
      "difficulty": "medium",
      "text": "An OSPFv2 single-area configuration references OSPF router ID. Which answer best describes the OSPF behavior?",
      "answers": [
        {
          "id": "a",
          "text": "OSPF process ID is the best match when the IP connectivity scenario requires this behavior: The OSPF process ID locally identifies an OSPF routing process and does not need to match between routers."
        },
        {
          "id": "b",
          "text": "Routing table is the best match when the IP connectivity scenario requires this behavior: A routing table lists known routes and the information a router uses to choose where to forward packets."
        },
        {
          "id": "c",
          "text": "OSPF router ID is the best match when the IP connectivity scenario requires this behavior: An OSPF router ID uniquely identifies an OSPF router in the OSPF domain."
        },
        {
          "id": "d",
          "text": "Routing protocol code is the best match when the IP connectivity scenario requires this behavior: A routing protocol code identifies how a route was learned, such as connected, static, OSPF, or another source."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "OSPF process ID refers to The OSPF process ID locally identifies an OSPF routing process and does not need to match between routers. OSPF router ID is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "b": "Routing table refers to A routing table lists known routes and the information a router uses to choose where to forward packets. OSPF router ID is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "c": "An OSPF router ID uniquely identifies an OSPF router in the OSPF domain. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer.",
        "d": "Routing protocol code refers to A routing protocol code identifies how a route was learned, such as connected, static, OSPF, or another source. OSPF router ID is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection."
      }
    },
    {
      "id": 439,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF router ID",
      "difficulty": "medium-hard",
      "text": "Two routers should form an OSPF adjacency or exchange routes, but the result is not as expected. Which scenario best matches OSPF router ID?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to Route source preference when the routing or OSPF evidence depends on this distinction: Route source preference uses administrative distance only when multiple sources advertise the same prefix length."
        },
        {
          "id": "b",
          "text": "A scenario points to Metric comparison when the routing or OSPF evidence depends on this distinction: Metric comparison chooses the best path among routes from the same routing protocol or route source."
        },
        {
          "id": "c",
          "text": "A scenario points to IPv4 static route when the routing or OSPF evidence depends on this distinction: An IPv4 static route is a manually configured route for an IPv4 destination prefix."
        },
        {
          "id": "d",
          "text": "A scenario points to OSPF router ID when the routing or OSPF evidence depends on this distinction: An OSPF router ID uniquely identifies an OSPF router in the OSPF domain."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "Route source preference refers to Route source preference uses administrative distance only when multiple sources advertise the same prefix length. OSPF router ID is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "b": "Metric comparison refers to Metric comparison chooses the best path among routes from the same routing protocol or route source. OSPF router ID is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "c": "IPv4 static route refers to An IPv4 static route is a manually configured route for an IPv4 destination prefix. OSPF router ID is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "d": "An OSPF router ID uniquely identifies an OSPF router in the OSPF domain. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer."
      }
    },
    {
      "id": 440,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF router ID",
      "difficulty": "hard",
      "text": "An OSPF troubleshooting case requires separating router ID, timers, areas, network types, and route installation. Which answer applies OSPF router ID most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "OSPF router ID is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: An OSPF router ID uniquely identifies an OSPF router in the OSPF domain."
        },
        {
          "id": "b",
          "text": "Static route verification is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: Static route verification confirms configured static routes appear in the routing table and forward traffic as intended."
        },
        {
          "id": "c",
          "text": "Static route prefix specificity is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: Static route prefix specificity determines whether a default, network, or host route is the most specific match for a destination."
        },
        {
          "id": "d",
          "text": "First hop redundancy protocols is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: First hop redundancy protocols provide a redundant default gateway for hosts on a LAN."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "An OSPF router ID uniquely identifies an OSPF router in the OSPF domain. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer.",
        "b": "Static route verification refers to Static route verification confirms configured static routes appear in the routing table and forward traffic as intended. OSPF router ID is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "c": "Static route prefix specificity refers to Static route prefix specificity determines whether a default, network, or host route is the most specific match for a destination. OSPF router ID is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "d": "First hop redundancy protocols refers to First hop redundancy protocols provide a redundant default gateway for hosts on a LAN. OSPF router ID is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection."
      }
    },
    {
      "id": 441,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF router ID selection",
      "difficulty": "easy",
      "text": "How is OSPF router ID selection best understood in an IP connectivity context?",
      "answers": [
        {
          "id": "a",
          "text": "An OSPF area mismatch prevents neighbors from forming because both ends of the link must agree on the area."
        },
        {
          "id": "b",
          "text": "OSPF hello and dead timer mismatches can prevent neighbor adjacency from forming."
        },
        {
          "id": "c",
          "text": "OSPF router ID selection uses a manually configured router ID first, then the highest loopback IP, then the highest active physical interface IP."
        },
        {
          "id": "d",
          "text": "OSPF network type mismatch can affect adjacency behavior, DR/BDR expectations, or route exchange."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "OSPF mismatched area refers to An OSPF area mismatch prevents neighbors from forming because both ends of the link must agree on the area. OSPF router ID selection is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "b": "OSPF mismatched timers refers to OSPF hello and dead timer mismatches can prevent neighbor adjacency from forming. OSPF router ID selection is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "c": "OSPF router ID selection uses a manually configured router ID first, then the highest loopback IP, then the highest active physical interface IP. This is the correct answer.",
        "d": "OSPF network type mismatch refers to OSPF network type mismatch can affect adjacency behavior, DR/BDR expectations, or route exchange. OSPF router ID selection is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection."
      }
    },
    {
      "id": 442,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF router ID selection",
      "difficulty": "medium",
      "text": "An OSPFv2 single-area configuration references OSPF router ID selection. Which answer best describes the OSPF behavior?",
      "answers": [
        {
          "id": "a",
          "text": "Routing metric is the best match when the IP connectivity scenario requires this behavior: A routing metric ranks paths within the same routing protocol or route source."
        },
        {
          "id": "b",
          "text": "Gateway of last resort is the best match when the IP connectivity scenario requires this behavior: The gateway of last resort is the default route used when no more specific route matches the destination."
        },
        {
          "id": "c",
          "text": "Longest prefix match is the best match when the IP connectivity scenario requires this behavior: Longest prefix match selects the route with the most specific matching prefix for the destination IP address."
        },
        {
          "id": "d",
          "text": "OSPF router ID selection is the best match when the IP connectivity scenario requires this behavior: OSPF router ID selection uses a manually configured router ID first, then the highest loopback IP, then the highest active physical interface IP."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "Routing metric refers to A routing metric ranks paths within the same routing protocol or route source. OSPF router ID selection is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "b": "Gateway of last resort refers to The gateway of last resort is the default route used when no more specific route matches the destination. OSPF router ID selection is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "c": "Longest prefix match refers to Longest prefix match selects the route with the most specific matching prefix for the destination IP address. OSPF router ID selection is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "d": "OSPF router ID selection uses a manually configured router ID first, then the highest loopback IP, then the highest active physical interface IP. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer."
      }
    },
    {
      "id": 443,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF router ID selection",
      "difficulty": "medium-hard",
      "text": "Two routers should form an OSPF adjacency or exchange routes, but the result is not as expected. Which scenario best matches OSPF router ID selection?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to OSPF router ID selection when the routing or OSPF evidence depends on this distinction: OSPF router ID selection uses a manually configured router ID first, then the highest loopback IP, then the highest active physical interface IP."
        },
        {
          "id": "b",
          "text": "A scenario points to Floating static route when the routing or OSPF evidence depends on this distinction: A floating static route uses a higher administrative distance so it is installed only when a better route is unavailable."
        },
        {
          "id": "c",
          "text": "A scenario points to Recursive static route when the routing or OSPF evidence depends on this distinction: A recursive static route points to a next-hop IP address that must itself be resolved through another route."
        },
        {
          "id": "d",
          "text": "A scenario points to Fully specified static route when the routing or OSPF evidence depends on this distinction: A fully specified static route includes both the exit interface and next-hop address."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "OSPF router ID selection uses a manually configured router ID first, then the highest loopback IP, then the highest active physical interface IP. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer.",
        "b": "Floating static route refers to A floating static route uses a higher administrative distance so it is installed only when a better route is unavailable. OSPF router ID selection is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "c": "Recursive static route refers to A recursive static route points to a next-hop IP address that must itself be resolved through another route. OSPF router ID selection is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "d": "Fully specified static route refers to A fully specified static route includes both the exit interface and next-hop address. OSPF router ID selection is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection."
      }
    },
    {
      "id": 444,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF router ID selection",
      "difficulty": "hard",
      "text": "An OSPF troubleshooting case requires separating router ID, timers, areas, network types, and route installation. Which answer applies OSPF router ID selection most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "Standby router is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A standby router waits to take over forwarding if the active first-hop device fails."
        },
        {
          "id": "b",
          "text": "OSPF router ID selection is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: OSPF router ID selection uses a manually configured router ID first, then the highest loopback IP, then the highest active physical interface IP."
        },
        {
          "id": "c",
          "text": "FHRP failover is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: FHRP failover moves the active gateway role to another device when the current active device becomes unavailable."
        },
        {
          "id": "d",
          "text": "Single-area OSPFv2 is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: Single-area OSPFv2 runs OSPF for IPv4 within one area, commonly area 0, to exchange routes dynamically."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "Standby router refers to A standby router waits to take over forwarding if the active first-hop device fails. OSPF router ID selection is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "b": "OSPF router ID selection uses a manually configured router ID first, then the highest loopback IP, then the highest active physical interface IP. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer.",
        "c": "FHRP failover refers to FHRP failover moves the active gateway role to another device when the current active device becomes unavailable. OSPF router ID selection is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "d": "Single-area OSPFv2 refers to Single-area OSPFv2 runs OSPF for IPv4 within one area, commonly area 0, to exchange routes dynamically. OSPF router ID selection is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection."
      }
    },
    {
      "id": 445,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF point-to-point network",
      "difficulty": "easy",
      "text": "Select the description that best identifies OSPF point-to-point network.",
      "answers": [
        {
          "id": "a",
          "text": "A route prefix identifies the destination network or subnet in a routing table entry."
        },
        {
          "id": "b",
          "text": "A network mask or prefix length identifies which bits represent the network portion of an address."
        },
        {
          "id": "c",
          "text": "A next hop is the next router IP address to which a packet is sent on the way to the destination."
        },
        {
          "id": "d",
          "text": "An OSPF point-to-point network type connects two routers without DR or BDR election."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "Route prefix refers to A route prefix identifies the destination network or subnet in a routing table entry. OSPF point-to-point network is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "b": "Network mask refers to A network mask or prefix length identifies which bits represent the network portion of an address. OSPF point-to-point network is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "c": "Next hop refers to A next hop is the next router IP address to which a packet is sent on the way to the destination. OSPF point-to-point network is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "d": "An OSPF point-to-point network type connects two routers without DR or BDR election. This is the correct answer."
      }
    },
    {
      "id": 446,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF point-to-point network",
      "difficulty": "medium",
      "text": "An OSPFv2 single-area configuration references OSPF point-to-point network. Which answer best describes the OSPF behavior?",
      "answers": [
        {
          "id": "a",
          "text": "OSPF point-to-point network is the best match when the IP connectivity scenario requires this behavior: An OSPF point-to-point network type connects two routers without DR or BDR election."
        },
        {
          "id": "b",
          "text": "IPv6 static route is the best match when the IP connectivity scenario requires this behavior: An IPv6 static route is a manually configured route for an IPv6 destination prefix."
        },
        {
          "id": "c",
          "text": "Default route is the best match when the IP connectivity scenario requires this behavior: A default route matches destinations that do not match any more specific route."
        },
        {
          "id": "d",
          "text": "Network route is the best match when the IP connectivity scenario requires this behavior: A network route points to an entire subnet or network rather than one host."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "An OSPF point-to-point network type connects two routers without DR or BDR election. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer.",
        "b": "IPv6 static route refers to An IPv6 static route is a manually configured route for an IPv6 destination prefix. OSPF point-to-point network is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "c": "Default route refers to A default route matches destinations that do not match any more specific route. OSPF point-to-point network is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "d": "Network route refers to A network route points to an entire subnet or network rather than one host. OSPF point-to-point network is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection."
      }
    },
    {
      "id": 447,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF point-to-point network",
      "difficulty": "medium-hard",
      "text": "Two routers should form an OSPF adjacency or exchange routes, but the result is not as expected. Which scenario best matches OSPF point-to-point network?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to Default gateway redundancy when the routing or OSPF evidence depends on this distinction: Default gateway redundancy lets hosts keep using a gateway address even if one physical router or Layer 3 switch fails."
        },
        {
          "id": "b",
          "text": "A scenario points to OSPF point-to-point network when the routing or OSPF evidence depends on this distinction: An OSPF point-to-point network type connects two routers without DR or BDR election."
        },
        {
          "id": "c",
          "text": "A scenario points to Virtual IP address when the routing or OSPF evidence depends on this distinction: A virtual IP address is the shared gateway address used by hosts in a first hop redundancy design."
        },
        {
          "id": "d",
          "text": "A scenario points to Virtual MAC address when the routing or OSPF evidence depends on this distinction: A virtual MAC address is used by the active first-hop device so hosts can forward frames to a redundant gateway."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "Default gateway redundancy refers to Default gateway redundancy lets hosts keep using a gateway address even if one physical router or Layer 3 switch fails. OSPF point-to-point network is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "b": "An OSPF point-to-point network type connects two routers without DR or BDR election. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer.",
        "c": "Virtual IP address refers to A virtual IP address is the shared gateway address used by hosts in a first hop redundancy design. OSPF point-to-point network is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "d": "Virtual MAC address refers to A virtual MAC address is used by the active first-hop device so hosts can forward frames to a redundant gateway. OSPF point-to-point network is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection."
      }
    },
    {
      "id": 448,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF point-to-point network",
      "difficulty": "hard",
      "text": "An OSPF troubleshooting case requires separating router ID, timers, areas, network types, and route installation. Which answer applies OSPF point-to-point network most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "OSPF router ID selection is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: OSPF router ID selection uses a manually configured router ID first, then the highest loopback IP, then the highest active physical interface IP."
        },
        {
          "id": "b",
          "text": "OSPF broadcast network is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: An OSPF broadcast network type supports multiaccess Ethernet segments and uses DR/BDR election."
        },
        {
          "id": "c",
          "text": "OSPF point-to-point network is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: An OSPF point-to-point network type connects two routers without DR or BDR election."
        },
        {
          "id": "d",
          "text": "DR election is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: Designated router election selects a router on an OSPF broadcast network to reduce adjacency overhead."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "OSPF router ID selection refers to OSPF router ID selection uses a manually configured router ID first, then the highest loopback IP, then the highest active physical interface IP. OSPF point-to-point network is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "b": "OSPF broadcast network refers to An OSPF broadcast network type supports multiaccess Ethernet segments and uses DR/BDR election. OSPF point-to-point network is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "c": "An OSPF point-to-point network type connects two routers without DR or BDR election. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer.",
        "d": "DR election refers to Designated router election selects a router on an OSPF broadcast network to reduce adjacency overhead. OSPF point-to-point network is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection."
      }
    },
    {
      "id": 449,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF broadcast network",
      "difficulty": "easy",
      "text": "Which description best matches OSPF broadcast network in CCNA Objective 3.0 IP Connectivity?",
      "answers": [
        {
          "id": "a",
          "text": "An OSPF broadcast network type supports multiaccess Ethernet segments and uses DR/BDR election."
        },
        {
          "id": "b",
          "text": "A router forwarding decision uses the destination IP address, routing table, longest prefix match, and route attributes to choose an outgoing path."
        },
        {
          "id": "c",
          "text": "Route source preference uses administrative distance only when multiple sources advertise the same prefix length."
        },
        {
          "id": "d",
          "text": "Metric comparison chooses the best path among routes from the same routing protocol or route source."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "An OSPF broadcast network type supports multiaccess Ethernet segments and uses DR/BDR election. This is the correct answer.",
        "b": "Forwarding decision refers to A router forwarding decision uses the destination IP address, routing table, longest prefix match, and route attributes to choose an outgoing path. OSPF broadcast network is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "c": "Route source preference refers to Route source preference uses administrative distance only when multiple sources advertise the same prefix length. OSPF broadcast network is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "d": "Metric comparison refers to Metric comparison chooses the best path among routes from the same routing protocol or route source. OSPF broadcast network is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection."
      }
    },
    {
      "id": 450,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF broadcast network",
      "difficulty": "medium",
      "text": "An OSPFv2 single-area configuration references OSPF broadcast network. Which answer best describes the OSPF behavior?",
      "answers": [
        {
          "id": "a",
          "text": "Exit-interface static route is the best match when the IP connectivity scenario requires this behavior: An exit-interface static route specifies the local outgoing interface used to forward traffic."
        },
        {
          "id": "b",
          "text": "OSPF broadcast network is the best match when the IP connectivity scenario requires this behavior: An OSPF broadcast network type supports multiaccess Ethernet segments and uses DR/BDR election."
        },
        {
          "id": "c",
          "text": "Static route verification is the best match when the IP connectivity scenario requires this behavior: Static route verification confirms configured static routes appear in the routing table and forward traffic as intended."
        },
        {
          "id": "d",
          "text": "Static route prefix specificity is the best match when the IP connectivity scenario requires this behavior: Static route prefix specificity determines whether a default, network, or host route is the most specific match for a destination."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "Exit-interface static route refers to An exit-interface static route specifies the local outgoing interface used to forward traffic. OSPF broadcast network is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "b": "An OSPF broadcast network type supports multiaccess Ethernet segments and uses DR/BDR election. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer.",
        "c": "Static route verification refers to Static route verification confirms configured static routes appear in the routing table and forward traffic as intended. OSPF broadcast network is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "d": "Static route prefix specificity refers to Static route prefix specificity determines whether a default, network, or host route is the most specific match for a destination. OSPF broadcast network is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection."
      }
    },
    {
      "id": 451,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF broadcast network",
      "difficulty": "medium-hard",
      "text": "Two routers should form an OSPF adjacency or exchange routes, but the result is not as expected. Which scenario best matches OSPF broadcast network?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to OSPF neighbor adjacency when the routing or OSPF evidence depends on this distinction: An OSPF neighbor adjacency is the relationship formed between OSPF routers that successfully exchange hello packets and required parameters."
        },
        {
          "id": "b",
          "text": "A scenario points to OSPF hello packets when the routing or OSPF evidence depends on this distinction: OSPF hello packets discover neighbors and maintain OSPF neighbor relationships."
        },
        {
          "id": "c",
          "text": "A scenario points to OSPF broadcast network when the routing or OSPF evidence depends on this distinction: An OSPF broadcast network type supports multiaccess Ethernet segments and uses DR/BDR election."
        },
        {
          "id": "d",
          "text": "A scenario points to OSPF area when the routing or OSPF evidence depends on this distinction: An OSPF area groups routers and link-state information; single-area CCNA scenarios commonly use area 0."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "OSPF neighbor adjacency refers to An OSPF neighbor adjacency is the relationship formed between OSPF routers that successfully exchange hello packets and required parameters. OSPF broadcast network is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "b": "OSPF hello packets refers to OSPF hello packets discover neighbors and maintain OSPF neighbor relationships. OSPF broadcast network is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "c": "An OSPF broadcast network type supports multiaccess Ethernet segments and uses DR/BDR election. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer.",
        "d": "OSPF area refers to An OSPF area groups routers and link-state information; single-area CCNA scenarios commonly use area 0. OSPF broadcast network is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection."
      }
    },
    {
      "id": 452,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF broadcast network",
      "difficulty": "hard",
      "text": "An OSPF troubleshooting case requires separating router ID, timers, areas, network types, and route installation. Which answer applies OSPF broadcast network most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "OSPF adjacency verification is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: OSPF adjacency verification checks neighbor state, interface participation, area, timers, and reachability to confirm OSPF operation."
        },
        {
          "id": "b",
          "text": "OSPF route verification is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: OSPF route verification checks that OSPF-learned routes appear in the routing table and are used for forwarding."
        },
        {
          "id": "c",
          "text": "OSPF mismatched area is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: An OSPF area mismatch prevents neighbors from forming because both ends of the link must agree on the area."
        },
        {
          "id": "d",
          "text": "OSPF broadcast network is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: An OSPF broadcast network type supports multiaccess Ethernet segments and uses DR/BDR election."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "OSPF adjacency verification refers to OSPF adjacency verification checks neighbor state, interface participation, area, timers, and reachability to confirm OSPF operation. OSPF broadcast network is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "b": "OSPF route verification refers to OSPF route verification checks that OSPF-learned routes appear in the routing table and are used for forwarding. OSPF broadcast network is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "c": "OSPF mismatched area refers to An OSPF area mismatch prevents neighbors from forming because both ends of the link must agree on the area. OSPF broadcast network is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "d": "An OSPF broadcast network type supports multiaccess Ethernet segments and uses DR/BDR election. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer."
      }
    },
    {
      "id": 453,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "DR election",
      "difficulty": "easy",
      "text": "Which option correctly defines DR election?",
      "answers": [
        {
          "id": "a",
          "text": "A host route points to one specific host address, typically using /32 for IPv4 or /128 for IPv6."
        },
        {
          "id": "b",
          "text": "Designated router election selects a router on an OSPF broadcast network to reduce adjacency overhead."
        },
        {
          "id": "c",
          "text": "A floating static route uses a higher administrative distance so it is installed only when a better route is unavailable."
        },
        {
          "id": "d",
          "text": "A recursive static route points to a next-hop IP address that must itself be resolved through another route."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "Host route refers to A host route points to one specific host address, typically using /32 for IPv4 or /128 for IPv6. DR election is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "b": "Designated router election selects a router on an OSPF broadcast network to reduce adjacency overhead. This is the correct answer.",
        "c": "Floating static route refers to A floating static route uses a higher administrative distance so it is installed only when a better route is unavailable. DR election is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "d": "Recursive static route refers to A recursive static route points to a next-hop IP address that must itself be resolved through another route. DR election is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection."
      }
    },
    {
      "id": 454,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "DR election",
      "difficulty": "medium",
      "text": "An OSPFv2 single-area configuration references DR election. Which answer best describes the OSPF behavior?",
      "answers": [
        {
          "id": "a",
          "text": "Active router is the best match when the IP connectivity scenario requires this behavior: An active router in an FHRP group forwards traffic sent to the virtual gateway."
        },
        {
          "id": "b",
          "text": "Standby router is the best match when the IP connectivity scenario requires this behavior: A standby router waits to take over forwarding if the active first-hop device fails."
        },
        {
          "id": "c",
          "text": "DR election is the best match when the IP connectivity scenario requires this behavior: Designated router election selects a router on an OSPF broadcast network to reduce adjacency overhead."
        },
        {
          "id": "d",
          "text": "FHRP failover is the best match when the IP connectivity scenario requires this behavior: FHRP failover moves the active gateway role to another device when the current active device becomes unavailable."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "Active router refers to An active router in an FHRP group forwards traffic sent to the virtual gateway. DR election is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "b": "Standby router refers to A standby router waits to take over forwarding if the active first-hop device fails. DR election is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "c": "Designated router election selects a router on an OSPF broadcast network to reduce adjacency overhead. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer.",
        "d": "FHRP failover refers to FHRP failover moves the active gateway role to another device when the current active device becomes unavailable. DR election is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection."
      }
    },
    {
      "id": 455,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "DR election",
      "difficulty": "medium-hard",
      "text": "Two routers should form an OSPF adjacency or exchange routes, but the result is not as expected. Which scenario best matches DR election?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to BDR election when the routing or OSPF evidence depends on this distinction: Backup designated router election selects a standby router to take over if the DR fails."
        },
        {
          "id": "b",
          "text": "A scenario points to OSPF cost when the routing or OSPF evidence depends on this distinction: OSPF cost is the metric used by OSPF to choose preferred paths."
        },
        {
          "id": "c",
          "text": "A scenario points to OSPF network command when the routing or OSPF evidence depends on this distinction: The OSPF network command enables OSPF on matching interfaces and assigns them to an area."
        },
        {
          "id": "d",
          "text": "A scenario points to DR election when the routing or OSPF evidence depends on this distinction: Designated router election selects a router on an OSPF broadcast network to reduce adjacency overhead."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "BDR election refers to Backup designated router election selects a standby router to take over if the DR fails. DR election is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "b": "OSPF cost refers to OSPF cost is the metric used by OSPF to choose preferred paths. DR election is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "c": "OSPF network command refers to The OSPF network command enables OSPF on matching interfaces and assigns them to an area. DR election is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "d": "Designated router election selects a router on an OSPF broadcast network to reduce adjacency overhead. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer."
      }
    },
    {
      "id": 456,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "DR election",
      "difficulty": "hard",
      "text": "An OSPF troubleshooting case requires separating router ID, timers, areas, network types, and route installation. Which answer applies DR election most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "DR election is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: Designated router election selects a router on an OSPF broadcast network to reduce adjacency overhead."
        },
        {
          "id": "b",
          "text": "Routing table is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A routing table lists known routes and the information a router uses to choose where to forward packets."
        },
        {
          "id": "c",
          "text": "Routing protocol code is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A routing protocol code identifies how a route was learned, such as connected, static, OSPF, or another source."
        },
        {
          "id": "d",
          "text": "Route prefix is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A route prefix identifies the destination network or subnet in a routing table entry."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "Designated router election selects a router on an OSPF broadcast network to reduce adjacency overhead. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer.",
        "b": "Routing table refers to A routing table lists known routes and the information a router uses to choose where to forward packets. DR election is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "c": "Routing protocol code refers to A routing protocol code identifies how a route was learned, such as connected, static, OSPF, or another source. DR election is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "d": "Route prefix refers to A route prefix identifies the destination network or subnet in a routing table entry. DR election is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection."
      }
    },
    {
      "id": 457,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "BDR election",
      "difficulty": "easy",
      "text": "How is BDR election best understood in an IP connectivity context?",
      "answers": [
        {
          "id": "a",
          "text": "First hop redundancy protocols provide a redundant default gateway for hosts on a LAN."
        },
        {
          "id": "b",
          "text": "Default gateway redundancy lets hosts keep using a gateway address even if one physical router or Layer 3 switch fails."
        },
        {
          "id": "c",
          "text": "Backup designated router election selects a standby router to take over if the DR fails."
        },
        {
          "id": "d",
          "text": "A virtual IP address is the shared gateway address used by hosts in a first hop redundancy design."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "First hop redundancy protocols refers to First hop redundancy protocols provide a redundant default gateway for hosts on a LAN. BDR election is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "b": "Default gateway redundancy refers to Default gateway redundancy lets hosts keep using a gateway address even if one physical router or Layer 3 switch fails. BDR election is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "c": "Backup designated router election selects a standby router to take over if the DR fails. This is the correct answer.",
        "d": "Virtual IP address refers to A virtual IP address is the shared gateway address used by hosts in a first hop redundancy design. BDR election is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection."
      }
    },
    {
      "id": 458,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "BDR election",
      "difficulty": "medium",
      "text": "An OSPFv2 single-area configuration references BDR election. Which answer best describes the OSPF behavior?",
      "answers": [
        {
          "id": "a",
          "text": "OSPF router ID is the best match when the IP connectivity scenario requires this behavior: An OSPF router ID uniquely identifies an OSPF router in the OSPF domain."
        },
        {
          "id": "b",
          "text": "OSPF router ID selection is the best match when the IP connectivity scenario requires this behavior: OSPF router ID selection uses a manually configured router ID first, then the highest loopback IP, then the highest active physical interface IP."
        },
        {
          "id": "c",
          "text": "OSPF point-to-point network is the best match when the IP connectivity scenario requires this behavior: An OSPF point-to-point network type connects two routers without DR or BDR election."
        },
        {
          "id": "d",
          "text": "BDR election is the best match when the IP connectivity scenario requires this behavior: Backup designated router election selects a standby router to take over if the DR fails."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "OSPF router ID refers to An OSPF router ID uniquely identifies an OSPF router in the OSPF domain. BDR election is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "b": "OSPF router ID selection refers to OSPF router ID selection uses a manually configured router ID first, then the highest loopback IP, then the highest active physical interface IP. BDR election is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "c": "OSPF point-to-point network refers to An OSPF point-to-point network type connects two routers without DR or BDR election. BDR election is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "d": "Backup designated router election selects a standby router to take over if the DR fails. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer."
      }
    },
    {
      "id": 459,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "BDR election",
      "difficulty": "medium-hard",
      "text": "Two routers should form an OSPF adjacency or exchange routes, but the result is not as expected. Which scenario best matches BDR election?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to BDR election when the routing or OSPF evidence depends on this distinction: Backup designated router election selects a standby router to take over if the DR fails."
        },
        {
          "id": "b",
          "text": "A scenario points to OSPF mismatched timers when the routing or OSPF evidence depends on this distinction: OSPF hello and dead timer mismatches can prevent neighbor adjacency from forming."
        },
        {
          "id": "c",
          "text": "A scenario points to OSPF network type mismatch when the routing or OSPF evidence depends on this distinction: OSPF network type mismatch can affect adjacency behavior, DR/BDR expectations, or route exchange."
        },
        {
          "id": "d",
          "text": "A scenario points to OSPF wildcard mask when the routing or OSPF evidence depends on this distinction: An OSPF wildcard mask in a network command identifies which interface addresses match for OSPF activation."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "Backup designated router election selects a standby router to take over if the DR fails. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer.",
        "b": "OSPF mismatched timers refers to OSPF hello and dead timer mismatches can prevent neighbor adjacency from forming. BDR election is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "c": "OSPF network type mismatch refers to OSPF network type mismatch can affect adjacency behavior, DR/BDR expectations, or route exchange. BDR election is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "d": "OSPF wildcard mask refers to An OSPF wildcard mask in a network command identifies which interface addresses match for OSPF activation. BDR election is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection."
      }
    },
    {
      "id": 460,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "BDR election",
      "difficulty": "hard",
      "text": "An OSPF troubleshooting case requires separating router ID, timers, areas, network types, and route installation. Which answer applies BDR election most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "Gateway of last resort is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: The gateway of last resort is the default route used when no more specific route matches the destination."
        },
        {
          "id": "b",
          "text": "BDR election is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: Backup designated router election selects a standby router to take over if the DR fails."
        },
        {
          "id": "c",
          "text": "Longest prefix match is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: Longest prefix match selects the route with the most specific matching prefix for the destination IP address."
        },
        {
          "id": "d",
          "text": "Forwarding decision is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A router forwarding decision uses the destination IP address, routing table, longest prefix match, and route attributes to choose an outgoing path."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "Gateway of last resort refers to The gateway of last resort is the default route used when no more specific route matches the destination. BDR election is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "b": "Backup designated router election selects a standby router to take over if the DR fails. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer.",
        "c": "Longest prefix match refers to Longest prefix match selects the route with the most specific matching prefix for the destination IP address. BDR election is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "d": "Forwarding decision refers to A router forwarding decision uses the destination IP address, routing table, longest prefix match, and route attributes to choose an outgoing path. BDR election is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection."
      }
    },
    {
      "id": 461,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF cost",
      "difficulty": "easy",
      "text": "Select the description that best identifies OSPF cost.",
      "answers": [
        {
          "id": "a",
          "text": "Single-area OSPFv2 runs OSPF for IPv4 within one area, commonly area 0, to exchange routes dynamically."
        },
        {
          "id": "b",
          "text": "An OSPF neighbor adjacency is the relationship formed between OSPF routers that successfully exchange hello packets and required parameters."
        },
        {
          "id": "c",
          "text": "OSPF hello packets discover neighbors and maintain OSPF neighbor relationships."
        },
        {
          "id": "d",
          "text": "OSPF cost is the metric used by OSPF to choose preferred paths."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "Single-area OSPFv2 refers to Single-area OSPFv2 runs OSPF for IPv4 within one area, commonly area 0, to exchange routes dynamically. OSPF cost is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "b": "OSPF neighbor adjacency refers to An OSPF neighbor adjacency is the relationship formed between OSPF routers that successfully exchange hello packets and required parameters. OSPF cost is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "c": "OSPF hello packets refers to OSPF hello packets discover neighbors and maintain OSPF neighbor relationships. OSPF cost is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "d": "OSPF cost is the metric used by OSPF to choose preferred paths. This is the correct answer."
      }
    },
    {
      "id": 462,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF cost",
      "difficulty": "medium",
      "text": "An OSPFv2 single-area configuration references OSPF cost. Which answer best describes the OSPF behavior?",
      "answers": [
        {
          "id": "a",
          "text": "OSPF cost is the best match when the IP connectivity scenario requires this behavior: OSPF cost is the metric used by OSPF to choose preferred paths."
        },
        {
          "id": "b",
          "text": "OSPF passive interface is the best match when the IP connectivity scenario requires this behavior: An OSPF passive interface advertises the connected network but does not send OSPF hello packets on that interface."
        },
        {
          "id": "c",
          "text": "OSPF adjacency verification is the best match when the IP connectivity scenario requires this behavior: OSPF adjacency verification checks neighbor state, interface participation, area, timers, and reachability to confirm OSPF operation."
        },
        {
          "id": "d",
          "text": "OSPF route verification is the best match when the IP connectivity scenario requires this behavior: OSPF route verification checks that OSPF-learned routes appear in the routing table and are used for forwarding."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "OSPF cost is the metric used by OSPF to choose preferred paths. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer.",
        "b": "OSPF passive interface refers to An OSPF passive interface advertises the connected network but does not send OSPF hello packets on that interface. OSPF cost is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "c": "OSPF adjacency verification refers to OSPF adjacency verification checks neighbor state, interface participation, area, timers, and reachability to confirm OSPF operation. OSPF cost is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "d": "OSPF route verification refers to OSPF route verification checks that OSPF-learned routes appear in the routing table and are used for forwarding. OSPF cost is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection."
      }
    },
    {
      "id": 463,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF cost",
      "difficulty": "medium-hard",
      "text": "Two routers should form an OSPF adjacency or exchange routes, but the result is not as expected. Which scenario best matches OSPF cost?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to Network mask when the routing or OSPF evidence depends on this distinction: A network mask or prefix length identifies which bits represent the network portion of an address."
        },
        {
          "id": "b",
          "text": "A scenario points to OSPF cost when the routing or OSPF evidence depends on this distinction: OSPF cost is the metric used by OSPF to choose preferred paths."
        },
        {
          "id": "c",
          "text": "A scenario points to Next hop when the routing or OSPF evidence depends on this distinction: A next hop is the next router IP address to which a packet is sent on the way to the destination."
        },
        {
          "id": "d",
          "text": "A scenario points to Administrative distance when the routing or OSPF evidence depends on this distinction: Administrative distance ranks the trustworthiness of route sources when the same prefix is learned from multiple sources."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "Network mask refers to A network mask or prefix length identifies which bits represent the network portion of an address. OSPF cost is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "b": "OSPF cost is the metric used by OSPF to choose preferred paths. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer.",
        "c": "Next hop refers to A next hop is the next router IP address to which a packet is sent on the way to the destination. OSPF cost is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "d": "Administrative distance refers to Administrative distance ranks the trustworthiness of route sources when the same prefix is learned from multiple sources. OSPF cost is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection."
      }
    },
    {
      "id": 464,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF cost",
      "difficulty": "hard",
      "text": "An OSPF troubleshooting case requires separating router ID, timers, areas, network types, and route installation. Which answer applies OSPF cost most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "Default route is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A default route matches destinations that do not match any more specific route."
        },
        {
          "id": "b",
          "text": "Network route is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A network route points to an entire subnet or network rather than one host."
        },
        {
          "id": "c",
          "text": "OSPF cost is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: OSPF cost is the metric used by OSPF to choose preferred paths."
        },
        {
          "id": "d",
          "text": "Host route is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A host route points to one specific host address, typically using /32 for IPv4 or /128 for IPv6."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "Default route refers to A default route matches destinations that do not match any more specific route. OSPF cost is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "b": "Network route refers to A network route points to an entire subnet or network rather than one host. OSPF cost is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection.",
        "c": "OSPF cost is the metric used by OSPF to choose preferred paths. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer.",
        "d": "Host route refers to A host route points to one specific host address, typically using /32 for IPv4 or /128 for IPv6. OSPF cost is different because the scenario depends on OSPF neighbor behavior, area operation, router ID, network type, or OSPF path selection."
      }
    },
    {
      "id": 465,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF adjacency verification",
      "difficulty": "easy",
      "text": "Which description best matches OSPF adjacency verification in CCNA Objective 3.0 IP Connectivity?",
      "answers": [
        {
          "id": "a",
          "text": "OSPF adjacency verification checks neighbor state, interface participation, area, timers, and reachability to confirm OSPF operation."
        },
        {
          "id": "b",
          "text": "An OSPF router ID uniquely identifies an OSPF router in the OSPF domain."
        },
        {
          "id": "c",
          "text": "OSPF router ID selection uses a manually configured router ID first, then the highest loopback IP, then the highest active physical interface IP."
        },
        {
          "id": "d",
          "text": "An OSPF point-to-point network type connects two routers without DR or BDR election."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "OSPF adjacency verification checks neighbor state, interface participation, area, timers, and reachability to confirm OSPF operation. This is the correct answer.",
        "b": "OSPF router ID refers to An OSPF router ID uniquely identifies an OSPF router in the OSPF domain. OSPF adjacency verification is different because the scenario depends on verifying whether OSPF neighbors or OSPF routes are actually operating as expected.",
        "c": "OSPF router ID selection refers to OSPF router ID selection uses a manually configured router ID first, then the highest loopback IP, then the highest active physical interface IP. OSPF adjacency verification is different because the scenario depends on verifying whether OSPF neighbors or OSPF routes are actually operating as expected.",
        "d": "OSPF point-to-point network refers to An OSPF point-to-point network type connects two routers without DR or BDR election. OSPF adjacency verification is different because the scenario depends on verifying whether OSPF neighbors or OSPF routes are actually operating as expected."
      }
    },
    {
      "id": 466,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF adjacency verification",
      "difficulty": "medium",
      "text": "An OSPFv2 single-area configuration references OSPF adjacency verification. Which answer best describes the OSPF behavior?",
      "answers": [
        {
          "id": "a",
          "text": "OSPF process ID is the best match when the IP connectivity scenario requires this behavior: The OSPF process ID locally identifies an OSPF routing process and does not need to match between routers."
        },
        {
          "id": "b",
          "text": "OSPF adjacency verification is the best match when the IP connectivity scenario requires this behavior: OSPF adjacency verification checks neighbor state, interface participation, area, timers, and reachability to confirm OSPF operation."
        },
        {
          "id": "c",
          "text": "Routing table is the best match when the IP connectivity scenario requires this behavior: A routing table lists known routes and the information a router uses to choose where to forward packets."
        },
        {
          "id": "d",
          "text": "Routing protocol code is the best match when the IP connectivity scenario requires this behavior: A routing protocol code identifies how a route was learned, such as connected, static, OSPF, or another source."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "OSPF process ID refers to The OSPF process ID locally identifies an OSPF routing process and does not need to match between routers. OSPF adjacency verification is different because the scenario depends on verifying whether OSPF neighbors or OSPF routes are actually operating as expected.",
        "b": "OSPF adjacency verification checks neighbor state, interface participation, area, timers, and reachability to confirm OSPF operation. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer.",
        "c": "Routing table refers to A routing table lists known routes and the information a router uses to choose where to forward packets. OSPF adjacency verification is different because the scenario depends on verifying whether OSPF neighbors or OSPF routes are actually operating as expected.",
        "d": "Routing protocol code refers to A routing protocol code identifies how a route was learned, such as connected, static, OSPF, or another source. OSPF adjacency verification is different because the scenario depends on verifying whether OSPF neighbors or OSPF routes are actually operating as expected."
      }
    },
    {
      "id": 467,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF adjacency verification",
      "difficulty": "medium-hard",
      "text": "Two routers should form an OSPF adjacency or exchange routes, but the result is not as expected. Which scenario best matches OSPF adjacency verification?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to Route source preference when the routing or OSPF evidence depends on this distinction: Route source preference uses administrative distance only when multiple sources advertise the same prefix length."
        },
        {
          "id": "b",
          "text": "A scenario points to Metric comparison when the routing or OSPF evidence depends on this distinction: Metric comparison chooses the best path among routes from the same routing protocol or route source."
        },
        {
          "id": "c",
          "text": "A scenario points to OSPF adjacency verification when the routing or OSPF evidence depends on this distinction: OSPF adjacency verification checks neighbor state, interface participation, area, timers, and reachability to confirm OSPF operation."
        },
        {
          "id": "d",
          "text": "A scenario points to IPv4 static route when the routing or OSPF evidence depends on this distinction: An IPv4 static route is a manually configured route for an IPv4 destination prefix."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "Route source preference refers to Route source preference uses administrative distance only when multiple sources advertise the same prefix length. OSPF adjacency verification is different because the scenario depends on verifying whether OSPF neighbors or OSPF routes are actually operating as expected.",
        "b": "Metric comparison refers to Metric comparison chooses the best path among routes from the same routing protocol or route source. OSPF adjacency verification is different because the scenario depends on verifying whether OSPF neighbors or OSPF routes are actually operating as expected.",
        "c": "OSPF adjacency verification checks neighbor state, interface participation, area, timers, and reachability to confirm OSPF operation. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer.",
        "d": "IPv4 static route refers to An IPv4 static route is a manually configured route for an IPv4 destination prefix. OSPF adjacency verification is different because the scenario depends on verifying whether OSPF neighbors or OSPF routes are actually operating as expected."
      }
    },
    {
      "id": 468,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF adjacency verification",
      "difficulty": "hard",
      "text": "An OSPF troubleshooting case requires separating router ID, timers, areas, network types, and route installation. Which answer applies OSPF adjacency verification most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "Static route verification is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: Static route verification confirms configured static routes appear in the routing table and forward traffic as intended."
        },
        {
          "id": "b",
          "text": "Static route prefix specificity is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: Static route prefix specificity determines whether a default, network, or host route is the most specific match for a destination."
        },
        {
          "id": "c",
          "text": "First hop redundancy protocols is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: First hop redundancy protocols provide a redundant default gateway for hosts on a LAN."
        },
        {
          "id": "d",
          "text": "OSPF adjacency verification is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: OSPF adjacency verification checks neighbor state, interface participation, area, timers, and reachability to confirm OSPF operation."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "Static route verification refers to Static route verification confirms configured static routes appear in the routing table and forward traffic as intended. OSPF adjacency verification is different because the scenario depends on verifying whether OSPF neighbors or OSPF routes are actually operating as expected.",
        "b": "Static route prefix specificity refers to Static route prefix specificity determines whether a default, network, or host route is the most specific match for a destination. OSPF adjacency verification is different because the scenario depends on verifying whether OSPF neighbors or OSPF routes are actually operating as expected.",
        "c": "First hop redundancy protocols refers to First hop redundancy protocols provide a redundant default gateway for hosts on a LAN. OSPF adjacency verification is different because the scenario depends on verifying whether OSPF neighbors or OSPF routes are actually operating as expected.",
        "d": "OSPF adjacency verification checks neighbor state, interface participation, area, timers, and reachability to confirm OSPF operation. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer."
      }
    },
    {
      "id": 469,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF route verification",
      "difficulty": "easy",
      "text": "Which option correctly defines OSPF route verification?",
      "answers": [
        {
          "id": "a",
          "text": "An OSPF area mismatch prevents neighbors from forming because both ends of the link must agree on the area."
        },
        {
          "id": "b",
          "text": "OSPF route verification checks that OSPF-learned routes appear in the routing table and are used for forwarding."
        },
        {
          "id": "c",
          "text": "OSPF hello and dead timer mismatches can prevent neighbor adjacency from forming."
        },
        {
          "id": "d",
          "text": "OSPF network type mismatch can affect adjacency behavior, DR/BDR expectations, or route exchange."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "OSPF mismatched area refers to An OSPF area mismatch prevents neighbors from forming because both ends of the link must agree on the area. OSPF route verification is different because the scenario depends on verifying whether OSPF neighbors or OSPF routes are actually operating as expected.",
        "b": "OSPF route verification checks that OSPF-learned routes appear in the routing table and are used for forwarding. This is the correct answer.",
        "c": "OSPF mismatched timers refers to OSPF hello and dead timer mismatches can prevent neighbor adjacency from forming. OSPF route verification is different because the scenario depends on verifying whether OSPF neighbors or OSPF routes are actually operating as expected.",
        "d": "OSPF network type mismatch refers to OSPF network type mismatch can affect adjacency behavior, DR/BDR expectations, or route exchange. OSPF route verification is different because the scenario depends on verifying whether OSPF neighbors or OSPF routes are actually operating as expected."
      }
    },
    {
      "id": 470,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF route verification",
      "difficulty": "medium",
      "text": "An OSPFv2 single-area configuration references OSPF route verification. Which answer best describes the OSPF behavior?",
      "answers": [
        {
          "id": "a",
          "text": "Routing metric is the best match when the IP connectivity scenario requires this behavior: A routing metric ranks paths within the same routing protocol or route source."
        },
        {
          "id": "b",
          "text": "Gateway of last resort is the best match when the IP connectivity scenario requires this behavior: The gateway of last resort is the default route used when no more specific route matches the destination."
        },
        {
          "id": "c",
          "text": "OSPF route verification is the best match when the IP connectivity scenario requires this behavior: OSPF route verification checks that OSPF-learned routes appear in the routing table and are used for forwarding."
        },
        {
          "id": "d",
          "text": "Longest prefix match is the best match when the IP connectivity scenario requires this behavior: Longest prefix match selects the route with the most specific matching prefix for the destination IP address."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "Routing metric refers to A routing metric ranks paths within the same routing protocol or route source. OSPF route verification is different because the scenario depends on verifying whether OSPF neighbors or OSPF routes are actually operating as expected.",
        "b": "Gateway of last resort refers to The gateway of last resort is the default route used when no more specific route matches the destination. OSPF route verification is different because the scenario depends on verifying whether OSPF neighbors or OSPF routes are actually operating as expected.",
        "c": "OSPF route verification checks that OSPF-learned routes appear in the routing table and are used for forwarding. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer.",
        "d": "Longest prefix match refers to Longest prefix match selects the route with the most specific matching prefix for the destination IP address. OSPF route verification is different because the scenario depends on verifying whether OSPF neighbors or OSPF routes are actually operating as expected."
      }
    },
    {
      "id": 471,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF route verification",
      "difficulty": "medium-hard",
      "text": "Two routers should form an OSPF adjacency or exchange routes, but the result is not as expected. Which scenario best matches OSPF route verification?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to Floating static route when the routing or OSPF evidence depends on this distinction: A floating static route uses a higher administrative distance so it is installed only when a better route is unavailable."
        },
        {
          "id": "b",
          "text": "A scenario points to Recursive static route when the routing or OSPF evidence depends on this distinction: A recursive static route points to a next-hop IP address that must itself be resolved through another route."
        },
        {
          "id": "c",
          "text": "A scenario points to Fully specified static route when the routing or OSPF evidence depends on this distinction: A fully specified static route includes both the exit interface and next-hop address."
        },
        {
          "id": "d",
          "text": "A scenario points to OSPF route verification when the routing or OSPF evidence depends on this distinction: OSPF route verification checks that OSPF-learned routes appear in the routing table and are used for forwarding."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "Floating static route refers to A floating static route uses a higher administrative distance so it is installed only when a better route is unavailable. OSPF route verification is different because the scenario depends on verifying whether OSPF neighbors or OSPF routes are actually operating as expected.",
        "b": "Recursive static route refers to A recursive static route points to a next-hop IP address that must itself be resolved through another route. OSPF route verification is different because the scenario depends on verifying whether OSPF neighbors or OSPF routes are actually operating as expected.",
        "c": "Fully specified static route refers to A fully specified static route includes both the exit interface and next-hop address. OSPF route verification is different because the scenario depends on verifying whether OSPF neighbors or OSPF routes are actually operating as expected.",
        "d": "OSPF route verification checks that OSPF-learned routes appear in the routing table and are used for forwarding. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer."
      }
    },
    {
      "id": 472,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF route verification",
      "difficulty": "hard",
      "text": "An OSPF troubleshooting case requires separating router ID, timers, areas, network types, and route installation. Which answer applies OSPF route verification most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "OSPF route verification is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: OSPF route verification checks that OSPF-learned routes appear in the routing table and are used for forwarding."
        },
        {
          "id": "b",
          "text": "Standby router is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A standby router waits to take over forwarding if the active first-hop device fails."
        },
        {
          "id": "c",
          "text": "FHRP failover is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: FHRP failover moves the active gateway role to another device when the current active device becomes unavailable."
        },
        {
          "id": "d",
          "text": "OSPF adjacency verification is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: OSPF adjacency verification checks neighbor state, interface participation, area, timers, and reachability to confirm OSPF operation."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "OSPF route verification checks that OSPF-learned routes appear in the routing table and are used for forwarding. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer.",
        "b": "Standby router refers to A standby router waits to take over forwarding if the active first-hop device fails. OSPF route verification is different because the scenario depends on verifying whether OSPF neighbors or OSPF routes are actually operating as expected.",
        "c": "FHRP failover refers to FHRP failover moves the active gateway role to another device when the current active device becomes unavailable. OSPF route verification is different because the scenario depends on verifying whether OSPF neighbors or OSPF routes are actually operating as expected.",
        "d": "OSPF adjacency verification refers to OSPF adjacency verification checks neighbor state, interface participation, area, timers, and reachability to confirm OSPF operation. OSPF route verification is different because the scenario depends on verifying whether OSPF neighbors or OSPF routes are actually operating as expected."
      }
    },
    {
      "id": 473,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF mismatched area",
      "difficulty": "easy",
      "text": "How is OSPF mismatched area best understood in an IP connectivity context?",
      "answers": [
        {
          "id": "a",
          "text": "A route prefix identifies the destination network or subnet in a routing table entry."
        },
        {
          "id": "b",
          "text": "A network mask or prefix length identifies which bits represent the network portion of an address."
        },
        {
          "id": "c",
          "text": "An OSPF area mismatch prevents neighbors from forming because both ends of the link must agree on the area."
        },
        {
          "id": "d",
          "text": "A next hop is the next router IP address to which a packet is sent on the way to the destination."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "Route prefix refers to A route prefix identifies the destination network or subnet in a routing table entry. OSPF mismatched area is different because the scenario depends on a specific OSPF mismatch that would change adjacency formation or route exchange.",
        "b": "Network mask refers to A network mask or prefix length identifies which bits represent the network portion of an address. OSPF mismatched area is different because the scenario depends on a specific OSPF mismatch that would change adjacency formation or route exchange.",
        "c": "An OSPF area mismatch prevents neighbors from forming because both ends of the link must agree on the area. This is the correct answer.",
        "d": "Next hop refers to A next hop is the next router IP address to which a packet is sent on the way to the destination. OSPF mismatched area is different because the scenario depends on a specific OSPF mismatch that would change adjacency formation or route exchange."
      }
    },
    {
      "id": 474,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF mismatched area",
      "difficulty": "medium",
      "text": "An OSPFv2 single-area configuration references OSPF mismatched area. Which answer best describes the OSPF behavior?",
      "answers": [
        {
          "id": "a",
          "text": "IPv6 static route is the best match when the IP connectivity scenario requires this behavior: An IPv6 static route is a manually configured route for an IPv6 destination prefix."
        },
        {
          "id": "b",
          "text": "Default route is the best match when the IP connectivity scenario requires this behavior: A default route matches destinations that do not match any more specific route."
        },
        {
          "id": "c",
          "text": "Network route is the best match when the IP connectivity scenario requires this behavior: A network route points to an entire subnet or network rather than one host."
        },
        {
          "id": "d",
          "text": "OSPF mismatched area is the best match when the IP connectivity scenario requires this behavior: An OSPF area mismatch prevents neighbors from forming because both ends of the link must agree on the area."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "IPv6 static route refers to An IPv6 static route is a manually configured route for an IPv6 destination prefix. OSPF mismatched area is different because the scenario depends on a specific OSPF mismatch that would change adjacency formation or route exchange.",
        "b": "Default route refers to A default route matches destinations that do not match any more specific route. OSPF mismatched area is different because the scenario depends on a specific OSPF mismatch that would change adjacency formation or route exchange.",
        "c": "Network route refers to A network route points to an entire subnet or network rather than one host. OSPF mismatched area is different because the scenario depends on a specific OSPF mismatch that would change adjacency formation or route exchange.",
        "d": "An OSPF area mismatch prevents neighbors from forming because both ends of the link must agree on the area. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer."
      }
    },
    {
      "id": 475,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF mismatched area",
      "difficulty": "medium-hard",
      "text": "Two routers should form an OSPF adjacency or exchange routes, but the result is not as expected. Which scenario best matches OSPF mismatched area?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to OSPF mismatched area when the routing or OSPF evidence depends on this distinction: An OSPF area mismatch prevents neighbors from forming because both ends of the link must agree on the area."
        },
        {
          "id": "b",
          "text": "A scenario points to Default gateway redundancy when the routing or OSPF evidence depends on this distinction: Default gateway redundancy lets hosts keep using a gateway address even if one physical router or Layer 3 switch fails."
        },
        {
          "id": "c",
          "text": "A scenario points to Virtual IP address when the routing or OSPF evidence depends on this distinction: A virtual IP address is the shared gateway address used by hosts in a first hop redundancy design."
        },
        {
          "id": "d",
          "text": "A scenario points to Virtual MAC address when the routing or OSPF evidence depends on this distinction: A virtual MAC address is used by the active first-hop device so hosts can forward frames to a redundant gateway."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "An OSPF area mismatch prevents neighbors from forming because both ends of the link must agree on the area. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer.",
        "b": "Default gateway redundancy refers to Default gateway redundancy lets hosts keep using a gateway address even if one physical router or Layer 3 switch fails. OSPF mismatched area is different because the scenario depends on a specific OSPF mismatch that would change adjacency formation or route exchange.",
        "c": "Virtual IP address refers to A virtual IP address is the shared gateway address used by hosts in a first hop redundancy design. OSPF mismatched area is different because the scenario depends on a specific OSPF mismatch that would change adjacency formation or route exchange.",
        "d": "Virtual MAC address refers to A virtual MAC address is used by the active first-hop device so hosts can forward frames to a redundant gateway. OSPF mismatched area is different because the scenario depends on a specific OSPF mismatch that would change adjacency formation or route exchange."
      }
    },
    {
      "id": 476,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF mismatched area",
      "difficulty": "hard",
      "text": "An OSPF troubleshooting case requires separating router ID, timers, areas, network types, and route installation. Which answer applies OSPF mismatched area most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "OSPF area is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: An OSPF area groups routers and link-state information; single-area CCNA scenarios commonly use area 0."
        },
        {
          "id": "b",
          "text": "OSPF mismatched area is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: An OSPF area mismatch prevents neighbors from forming because both ends of the link must agree on the area."
        },
        {
          "id": "c",
          "text": "OSPF network command is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: The OSPF network command enables OSPF on matching interfaces and assigns them to an area."
        },
        {
          "id": "d",
          "text": "OSPF passive interface is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: An OSPF passive interface advertises the connected network but does not send OSPF hello packets on that interface."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "OSPF area refers to An OSPF area groups routers and link-state information; single-area CCNA scenarios commonly use area 0. OSPF mismatched area is different because the scenario depends on a specific OSPF mismatch that would change adjacency formation or route exchange.",
        "b": "An OSPF area mismatch prevents neighbors from forming because both ends of the link must agree on the area. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer.",
        "c": "OSPF network command refers to The OSPF network command enables OSPF on matching interfaces and assigns them to an area. OSPF mismatched area is different because the scenario depends on a specific OSPF mismatch that would change adjacency formation or route exchange.",
        "d": "OSPF passive interface refers to An OSPF passive interface advertises the connected network but does not send OSPF hello packets on that interface. OSPF mismatched area is different because the scenario depends on a specific OSPF mismatch that would change adjacency formation or route exchange."
      }
    },
    {
      "id": 477,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF mismatched timers",
      "difficulty": "easy",
      "text": "Select the description that best identifies OSPF mismatched timers.",
      "answers": [
        {
          "id": "a",
          "text": "A router forwarding decision uses the destination IP address, routing table, longest prefix match, and route attributes to choose an outgoing path."
        },
        {
          "id": "b",
          "text": "Route source preference uses administrative distance only when multiple sources advertise the same prefix length."
        },
        {
          "id": "c",
          "text": "Metric comparison chooses the best path among routes from the same routing protocol or route source."
        },
        {
          "id": "d",
          "text": "OSPF hello and dead timer mismatches can prevent neighbor adjacency from forming."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "Forwarding decision refers to A router forwarding decision uses the destination IP address, routing table, longest prefix match, and route attributes to choose an outgoing path. OSPF mismatched timers is different because the scenario depends on a specific OSPF mismatch that would change adjacency formation or route exchange.",
        "b": "Route source preference refers to Route source preference uses administrative distance only when multiple sources advertise the same prefix length. OSPF mismatched timers is different because the scenario depends on a specific OSPF mismatch that would change adjacency formation or route exchange.",
        "c": "Metric comparison refers to Metric comparison chooses the best path among routes from the same routing protocol or route source. OSPF mismatched timers is different because the scenario depends on a specific OSPF mismatch that would change adjacency formation or route exchange.",
        "d": "OSPF hello and dead timer mismatches can prevent neighbor adjacency from forming. This is the correct answer."
      }
    },
    {
      "id": 478,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF mismatched timers",
      "difficulty": "medium",
      "text": "An OSPFv2 single-area configuration references OSPF mismatched timers. Which answer best describes the OSPF behavior?",
      "answers": [
        {
          "id": "a",
          "text": "OSPF mismatched timers is the best match when the IP connectivity scenario requires this behavior: OSPF hello and dead timer mismatches can prevent neighbor adjacency from forming."
        },
        {
          "id": "b",
          "text": "Exit-interface static route is the best match when the IP connectivity scenario requires this behavior: An exit-interface static route specifies the local outgoing interface used to forward traffic."
        },
        {
          "id": "c",
          "text": "Static route verification is the best match when the IP connectivity scenario requires this behavior: Static route verification confirms configured static routes appear in the routing table and forward traffic as intended."
        },
        {
          "id": "d",
          "text": "Static route prefix specificity is the best match when the IP connectivity scenario requires this behavior: Static route prefix specificity determines whether a default, network, or host route is the most specific match for a destination."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "OSPF hello and dead timer mismatches can prevent neighbor adjacency from forming. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer.",
        "b": "Exit-interface static route refers to An exit-interface static route specifies the local outgoing interface used to forward traffic. OSPF mismatched timers is different because the scenario depends on a specific OSPF mismatch that would change adjacency formation or route exchange.",
        "c": "Static route verification refers to Static route verification confirms configured static routes appear in the routing table and forward traffic as intended. OSPF mismatched timers is different because the scenario depends on a specific OSPF mismatch that would change adjacency formation or route exchange.",
        "d": "Static route prefix specificity refers to Static route prefix specificity determines whether a default, network, or host route is the most specific match for a destination. OSPF mismatched timers is different because the scenario depends on a specific OSPF mismatch that would change adjacency formation or route exchange."
      }
    },
    {
      "id": 479,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF mismatched timers",
      "difficulty": "medium-hard",
      "text": "Two routers should form an OSPF adjacency or exchange routes, but the result is not as expected. Which scenario best matches OSPF mismatched timers?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to OSPF network type mismatch when the routing or OSPF evidence depends on this distinction: OSPF network type mismatch can affect adjacency behavior, DR/BDR expectations, or route exchange."
        },
        {
          "id": "b",
          "text": "A scenario points to OSPF mismatched timers when the routing or OSPF evidence depends on this distinction: OSPF hello and dead timer mismatches can prevent neighbor adjacency from forming."
        },
        {
          "id": "c",
          "text": "A scenario points to Single-area OSPFv2 when the routing or OSPF evidence depends on this distinction: Single-area OSPFv2 runs OSPF for IPv4 within one area, commonly area 0, to exchange routes dynamically."
        },
        {
          "id": "d",
          "text": "A scenario points to OSPF neighbor adjacency when the routing or OSPF evidence depends on this distinction: An OSPF neighbor adjacency is the relationship formed between OSPF routers that successfully exchange hello packets and required parameters."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "OSPF network type mismatch refers to OSPF network type mismatch can affect adjacency behavior, DR/BDR expectations, or route exchange. OSPF mismatched timers is different because the scenario depends on a specific OSPF mismatch that would change adjacency formation or route exchange.",
        "b": "OSPF hello and dead timer mismatches can prevent neighbor adjacency from forming. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer.",
        "c": "Single-area OSPFv2 refers to Single-area OSPFv2 runs OSPF for IPv4 within one area, commonly area 0, to exchange routes dynamically. OSPF mismatched timers is different because the scenario depends on a specific OSPF mismatch that would change adjacency formation or route exchange.",
        "d": "OSPF neighbor adjacency refers to An OSPF neighbor adjacency is the relationship formed between OSPF routers that successfully exchange hello packets and required parameters. OSPF mismatched timers is different because the scenario depends on a specific OSPF mismatch that would change adjacency formation or route exchange."
      }
    },
    {
      "id": 480,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF mismatched timers",
      "difficulty": "hard",
      "text": "An OSPF troubleshooting case requires separating router ID, timers, areas, network types, and route installation. Which answer applies OSPF mismatched timers most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "DR election is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: Designated router election selects a router on an OSPF broadcast network to reduce adjacency overhead."
        },
        {
          "id": "b",
          "text": "BDR election is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: Backup designated router election selects a standby router to take over if the DR fails."
        },
        {
          "id": "c",
          "text": "OSPF mismatched timers is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: OSPF hello and dead timer mismatches can prevent neighbor adjacency from forming."
        },
        {
          "id": "d",
          "text": "OSPF cost is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: OSPF cost is the metric used by OSPF to choose preferred paths."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "DR election refers to Designated router election selects a router on an OSPF broadcast network to reduce adjacency overhead. OSPF mismatched timers is different because the scenario depends on a specific OSPF mismatch that would change adjacency formation or route exchange.",
        "b": "BDR election refers to Backup designated router election selects a standby router to take over if the DR fails. OSPF mismatched timers is different because the scenario depends on a specific OSPF mismatch that would change adjacency formation or route exchange.",
        "c": "OSPF hello and dead timer mismatches can prevent neighbor adjacency from forming. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer.",
        "d": "OSPF cost refers to OSPF cost is the metric used by OSPF to choose preferred paths. OSPF mismatched timers is different because the scenario depends on a specific OSPF mismatch that would change adjacency formation or route exchange."
      }
    },
    {
      "id": 481,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF network type mismatch",
      "difficulty": "easy",
      "text": "Which description best matches OSPF network type mismatch in CCNA Objective 3.0 IP Connectivity?",
      "answers": [
        {
          "id": "a",
          "text": "OSPF network type mismatch can affect adjacency behavior, DR/BDR expectations, or route exchange."
        },
        {
          "id": "b",
          "text": "A host route points to one specific host address, typically using /32 for IPv4 or /128 for IPv6."
        },
        {
          "id": "c",
          "text": "A floating static route uses a higher administrative distance so it is installed only when a better route is unavailable."
        },
        {
          "id": "d",
          "text": "A recursive static route points to a next-hop IP address that must itself be resolved through another route."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "OSPF network type mismatch can affect adjacency behavior, DR/BDR expectations, or route exchange. This is the correct answer.",
        "b": "Host route refers to A host route points to one specific host address, typically using /32 for IPv4 or /128 for IPv6. OSPF network type mismatch is different because the scenario depends on a specific OSPF mismatch that would change adjacency formation or route exchange.",
        "c": "Floating static route refers to A floating static route uses a higher administrative distance so it is installed only when a better route is unavailable. OSPF network type mismatch is different because the scenario depends on a specific OSPF mismatch that would change adjacency formation or route exchange.",
        "d": "Recursive static route refers to A recursive static route points to a next-hop IP address that must itself be resolved through another route. OSPF network type mismatch is different because the scenario depends on a specific OSPF mismatch that would change adjacency formation or route exchange."
      }
    },
    {
      "id": 482,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF network type mismatch",
      "difficulty": "medium",
      "text": "An OSPFv2 single-area configuration references OSPF network type mismatch. Which answer best describes the OSPF behavior?",
      "answers": [
        {
          "id": "a",
          "text": "Active router is the best match when the IP connectivity scenario requires this behavior: An active router in an FHRP group forwards traffic sent to the virtual gateway."
        },
        {
          "id": "b",
          "text": "OSPF network type mismatch is the best match when the IP connectivity scenario requires this behavior: OSPF network type mismatch can affect adjacency behavior, DR/BDR expectations, or route exchange."
        },
        {
          "id": "c",
          "text": "Standby router is the best match when the IP connectivity scenario requires this behavior: A standby router waits to take over forwarding if the active first-hop device fails."
        },
        {
          "id": "d",
          "text": "FHRP failover is the best match when the IP connectivity scenario requires this behavior: FHRP failover moves the active gateway role to another device when the current active device becomes unavailable."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "Active router refers to An active router in an FHRP group forwards traffic sent to the virtual gateway. OSPF network type mismatch is different because the scenario depends on a specific OSPF mismatch that would change adjacency formation or route exchange.",
        "b": "OSPF network type mismatch can affect adjacency behavior, DR/BDR expectations, or route exchange. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer.",
        "c": "Standby router refers to A standby router waits to take over forwarding if the active first-hop device fails. OSPF network type mismatch is different because the scenario depends on a specific OSPF mismatch that would change adjacency formation or route exchange.",
        "d": "FHRP failover refers to FHRP failover moves the active gateway role to another device when the current active device becomes unavailable. OSPF network type mismatch is different because the scenario depends on a specific OSPF mismatch that would change adjacency formation or route exchange."
      }
    },
    {
      "id": 483,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF network type mismatch",
      "difficulty": "medium-hard",
      "text": "Two routers should form an OSPF adjacency or exchange routes, but the result is not as expected. Which scenario best matches OSPF network type mismatch?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to OSPF router ID when the routing or OSPF evidence depends on this distinction: An OSPF router ID uniquely identifies an OSPF router in the OSPF domain."
        },
        {
          "id": "b",
          "text": "A scenario points to OSPF router ID selection when the routing or OSPF evidence depends on this distinction: OSPF router ID selection uses a manually configured router ID first, then the highest loopback IP, then the highest active physical interface IP."
        },
        {
          "id": "c",
          "text": "A scenario points to OSPF network type mismatch when the routing or OSPF evidence depends on this distinction: OSPF network type mismatch can affect adjacency behavior, DR/BDR expectations, or route exchange."
        },
        {
          "id": "d",
          "text": "A scenario points to OSPF point-to-point network when the routing or OSPF evidence depends on this distinction: An OSPF point-to-point network type connects two routers without DR or BDR election."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "OSPF router ID refers to An OSPF router ID uniquely identifies an OSPF router in the OSPF domain. OSPF network type mismatch is different because the scenario depends on a specific OSPF mismatch that would change adjacency formation or route exchange.",
        "b": "OSPF router ID selection refers to OSPF router ID selection uses a manually configured router ID first, then the highest loopback IP, then the highest active physical interface IP. OSPF network type mismatch is different because the scenario depends on a specific OSPF mismatch that would change adjacency formation or route exchange.",
        "c": "OSPF network type mismatch can affect adjacency behavior, DR/BDR expectations, or route exchange. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer.",
        "d": "OSPF point-to-point network refers to An OSPF point-to-point network type connects two routers without DR or BDR election. OSPF network type mismatch is different because the scenario depends on a specific OSPF mismatch that would change adjacency formation or route exchange."
      }
    },
    {
      "id": 484,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF network type mismatch",
      "difficulty": "hard",
      "text": "An OSPF troubleshooting case requires separating router ID, timers, areas, network types, and route installation. Which answer applies OSPF network type mismatch most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "Routing table is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A routing table lists known routes and the information a router uses to choose where to forward packets."
        },
        {
          "id": "b",
          "text": "Routing protocol code is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A routing protocol code identifies how a route was learned, such as connected, static, OSPF, or another source."
        },
        {
          "id": "c",
          "text": "Route prefix is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A route prefix identifies the destination network or subnet in a routing table entry."
        },
        {
          "id": "d",
          "text": "OSPF network type mismatch is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: OSPF network type mismatch can affect adjacency behavior, DR/BDR expectations, or route exchange."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "Routing table refers to A routing table lists known routes and the information a router uses to choose where to forward packets. OSPF network type mismatch is different because the scenario depends on a specific OSPF mismatch that would change adjacency formation or route exchange.",
        "b": "Routing protocol code refers to A routing protocol code identifies how a route was learned, such as connected, static, OSPF, or another source. OSPF network type mismatch is different because the scenario depends on a specific OSPF mismatch that would change adjacency formation or route exchange.",
        "c": "Route prefix refers to A route prefix identifies the destination network or subnet in a routing table entry. OSPF network type mismatch is different because the scenario depends on a specific OSPF mismatch that would change adjacency formation or route exchange.",
        "d": "OSPF network type mismatch can affect adjacency behavior, DR/BDR expectations, or route exchange. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer."
      }
    },
    {
      "id": 485,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF wildcard mask",
      "difficulty": "easy",
      "text": "Which option correctly defines OSPF wildcard mask?",
      "answers": [
        {
          "id": "a",
          "text": "First hop redundancy protocols provide a redundant default gateway for hosts on a LAN."
        },
        {
          "id": "b",
          "text": "An OSPF wildcard mask in a network command identifies which interface addresses match for OSPF activation."
        },
        {
          "id": "c",
          "text": "Default gateway redundancy lets hosts keep using a gateway address even if one physical router or Layer 3 switch fails."
        },
        {
          "id": "d",
          "text": "A virtual IP address is the shared gateway address used by hosts in a first hop redundancy design."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "First hop redundancy protocols refers to First hop redundancy protocols provide a redundant default gateway for hosts on a LAN. OSPF wildcard mask is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask.",
        "b": "An OSPF wildcard mask in a network command identifies which interface addresses match for OSPF activation. This is the correct answer.",
        "c": "Default gateway redundancy refers to Default gateway redundancy lets hosts keep using a gateway address even if one physical router or Layer 3 switch fails. OSPF wildcard mask is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask.",
        "d": "Virtual IP address refers to A virtual IP address is the shared gateway address used by hosts in a first hop redundancy design. OSPF wildcard mask is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask."
      }
    },
    {
      "id": 486,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF wildcard mask",
      "difficulty": "medium",
      "text": "An OSPFv2 single-area configuration references OSPF wildcard mask. Which answer best describes the OSPF behavior?",
      "answers": [
        {
          "id": "a",
          "text": "OSPF neighbor adjacency is the best match when the IP connectivity scenario requires this behavior: An OSPF neighbor adjacency is the relationship formed between OSPF routers that successfully exchange hello packets and required parameters."
        },
        {
          "id": "b",
          "text": "OSPF hello packets is the best match when the IP connectivity scenario requires this behavior: OSPF hello packets discover neighbors and maintain OSPF neighbor relationships."
        },
        {
          "id": "c",
          "text": "OSPF wildcard mask is the best match when the IP connectivity scenario requires this behavior: An OSPF wildcard mask in a network command identifies which interface addresses match for OSPF activation."
        },
        {
          "id": "d",
          "text": "OSPF area is the best match when the IP connectivity scenario requires this behavior: An OSPF area groups routers and link-state information; single-area CCNA scenarios commonly use area 0."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "OSPF neighbor adjacency refers to An OSPF neighbor adjacency is the relationship formed between OSPF routers that successfully exchange hello packets and required parameters. OSPF wildcard mask is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask.",
        "b": "OSPF hello packets refers to OSPF hello packets discover neighbors and maintain OSPF neighbor relationships. OSPF wildcard mask is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask.",
        "c": "An OSPF wildcard mask in a network command identifies which interface addresses match for OSPF activation. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer.",
        "d": "OSPF area refers to An OSPF area groups routers and link-state information; single-area CCNA scenarios commonly use area 0. OSPF wildcard mask is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask."
      }
    },
    {
      "id": 487,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF wildcard mask",
      "difficulty": "medium-hard",
      "text": "Two routers should form an OSPF adjacency or exchange routes, but the result is not as expected. Which scenario best matches OSPF wildcard mask?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to OSPF route verification when the routing or OSPF evidence depends on this distinction: OSPF route verification checks that OSPF-learned routes appear in the routing table and are used for forwarding."
        },
        {
          "id": "b",
          "text": "A scenario points to OSPF mismatched area when the routing or OSPF evidence depends on this distinction: An OSPF area mismatch prevents neighbors from forming because both ends of the link must agree on the area."
        },
        {
          "id": "c",
          "text": "A scenario points to OSPF mismatched timers when the routing or OSPF evidence depends on this distinction: OSPF hello and dead timer mismatches can prevent neighbor adjacency from forming."
        },
        {
          "id": "d",
          "text": "A scenario points to OSPF wildcard mask when the routing or OSPF evidence depends on this distinction: An OSPF wildcard mask in a network command identifies which interface addresses match for OSPF activation."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "OSPF route verification refers to OSPF route verification checks that OSPF-learned routes appear in the routing table and are used for forwarding. OSPF wildcard mask is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask.",
        "b": "OSPF mismatched area refers to An OSPF area mismatch prevents neighbors from forming because both ends of the link must agree on the area. OSPF wildcard mask is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask.",
        "c": "OSPF mismatched timers refers to OSPF hello and dead timer mismatches can prevent neighbor adjacency from forming. OSPF wildcard mask is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask.",
        "d": "An OSPF wildcard mask in a network command identifies which interface addresses match for OSPF activation. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer."
      }
    },
    {
      "id": 488,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF wildcard mask",
      "difficulty": "hard",
      "text": "An OSPF troubleshooting case requires separating router ID, timers, areas, network types, and route installation. Which answer applies OSPF wildcard mask most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "OSPF wildcard mask is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: An OSPF wildcard mask in a network command identifies which interface addresses match for OSPF activation."
        },
        {
          "id": "b",
          "text": "Gateway of last resort is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: The gateway of last resort is the default route used when no more specific route matches the destination."
        },
        {
          "id": "c",
          "text": "Longest prefix match is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: Longest prefix match selects the route with the most specific matching prefix for the destination IP address."
        },
        {
          "id": "d",
          "text": "Forwarding decision is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A router forwarding decision uses the destination IP address, routing table, longest prefix match, and route attributes to choose an outgoing path."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "An OSPF wildcard mask in a network command identifies which interface addresses match for OSPF activation. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer.",
        "b": "Gateway of last resort refers to The gateway of last resort is the default route used when no more specific route matches the destination. OSPF wildcard mask is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask.",
        "c": "Longest prefix match refers to Longest prefix match selects the route with the most specific matching prefix for the destination IP address. OSPF wildcard mask is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask.",
        "d": "Forwarding decision refers to A router forwarding decision uses the destination IP address, routing table, longest prefix match, and route attributes to choose an outgoing path. OSPF wildcard mask is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask."
      }
    },
    {
      "id": 489,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF process ID",
      "difficulty": "easy",
      "text": "How is OSPF process ID best understood in an IP connectivity context?",
      "answers": [
        {
          "id": "a",
          "text": "The OSPF network command enables OSPF on matching interfaces and assigns them to an area."
        },
        {
          "id": "b",
          "text": "An OSPF passive interface advertises the connected network but does not send OSPF hello packets on that interface."
        },
        {
          "id": "c",
          "text": "The OSPF process ID locally identifies an OSPF routing process and does not need to match between routers."
        },
        {
          "id": "d",
          "text": "An OSPF wildcard mask in a network command identifies which interface addresses match for OSPF activation."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "OSPF network command refers to The OSPF network command enables OSPF on matching interfaces and assigns them to an area. OSPF process ID is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask.",
        "b": "OSPF passive interface refers to An OSPF passive interface advertises the connected network but does not send OSPF hello packets on that interface. OSPF process ID is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask.",
        "c": "The OSPF process ID locally identifies an OSPF routing process and does not need to match between routers. This is the correct answer.",
        "d": "OSPF wildcard mask refers to An OSPF wildcard mask in a network command identifies which interface addresses match for OSPF activation. OSPF process ID is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask."
      }
    },
    {
      "id": 490,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF process ID",
      "difficulty": "medium",
      "text": "An OSPFv2 single-area configuration references OSPF process ID. Which answer best describes the OSPF behavior?",
      "answers": [
        {
          "id": "a",
          "text": "DR election is the best match when the IP connectivity scenario requires this behavior: Designated router election selects a router on an OSPF broadcast network to reduce adjacency overhead."
        },
        {
          "id": "b",
          "text": "BDR election is the best match when the IP connectivity scenario requires this behavior: Backup designated router election selects a standby router to take over if the DR fails."
        },
        {
          "id": "c",
          "text": "OSPF cost is the best match when the IP connectivity scenario requires this behavior: OSPF cost is the metric used by OSPF to choose preferred paths."
        },
        {
          "id": "d",
          "text": "OSPF process ID is the best match when the IP connectivity scenario requires this behavior: The OSPF process ID locally identifies an OSPF routing process and does not need to match between routers."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "DR election refers to Designated router election selects a router on an OSPF broadcast network to reduce adjacency overhead. OSPF process ID is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask.",
        "b": "BDR election refers to Backup designated router election selects a standby router to take over if the DR fails. OSPF process ID is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask.",
        "c": "OSPF cost refers to OSPF cost is the metric used by OSPF to choose preferred paths. OSPF process ID is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask.",
        "d": "The OSPF process ID locally identifies an OSPF routing process and does not need to match between routers. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer."
      }
    },
    {
      "id": 491,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF process ID",
      "difficulty": "medium-hard",
      "text": "Two routers should form an OSPF adjacency or exchange routes, but the result is not as expected. Which scenario best matches OSPF process ID?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to OSPF process ID when the routing or OSPF evidence depends on this distinction: The OSPF process ID locally identifies an OSPF routing process and does not need to match between routers."
        },
        {
          "id": "b",
          "text": "A scenario points to Network mask when the routing or OSPF evidence depends on this distinction: A network mask or prefix length identifies which bits represent the network portion of an address."
        },
        {
          "id": "c",
          "text": "A scenario points to Next hop when the routing or OSPF evidence depends on this distinction: A next hop is the next router IP address to which a packet is sent on the way to the destination."
        },
        {
          "id": "d",
          "text": "A scenario points to Administrative distance when the routing or OSPF evidence depends on this distinction: Administrative distance ranks the trustworthiness of route sources when the same prefix is learned from multiple sources."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "The OSPF process ID locally identifies an OSPF routing process and does not need to match between routers. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer.",
        "b": "Network mask refers to A network mask or prefix length identifies which bits represent the network portion of an address. OSPF process ID is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask.",
        "c": "Next hop refers to A next hop is the next router IP address to which a packet is sent on the way to the destination. OSPF process ID is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask.",
        "d": "Administrative distance refers to Administrative distance ranks the trustworthiness of route sources when the same prefix is learned from multiple sources. OSPF process ID is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask."
      }
    },
    {
      "id": 492,
      "domain": "3.0",
      "objective": "3.4",
      "keyword": "OSPF process ID",
      "difficulty": "hard",
      "text": "An OSPF troubleshooting case requires separating router ID, timers, areas, network types, and route installation. Which answer applies OSPF process ID most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "Default route is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A default route matches destinations that do not match any more specific route."
        },
        {
          "id": "b",
          "text": "OSPF process ID is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: The OSPF process ID locally identifies an OSPF routing process and does not need to match between routers."
        },
        {
          "id": "c",
          "text": "Network route is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A network route points to an entire subnet or network rather than one host."
        },
        {
          "id": "d",
          "text": "Host route is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A host route points to one specific host address, typically using /32 for IPv4 or /128 for IPv6."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "Default route refers to A default route matches destinations that do not match any more specific route. OSPF process ID is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask.",
        "b": "The OSPF process ID locally identifies an OSPF routing process and does not need to match between routers. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer.",
        "c": "Network route refers to A network route points to an entire subnet or network rather than one host. OSPF process ID is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask.",
        "d": "Host route refers to A host route points to one specific host address, typically using /32 for IPv4 or /128 for IPv6. OSPF process ID is different because the scenario depends on an OSPF configuration element such as the network command, process ID, passive interface, or wildcard mask."
      }
    },
    {
      "id": 493,
      "domain": "3.0",
      "objective": "3.5",
      "keyword": "First hop redundancy protocols",
      "difficulty": "easy",
      "text": "Select the description that best identifies First hop redundancy protocols.",
      "answers": [
        {
          "id": "a",
          "text": "A routing protocol code identifies how a route was learned, such as connected, static, OSPF, or another source."
        },
        {
          "id": "b",
          "text": "A route prefix identifies the destination network or subnet in a routing table entry."
        },
        {
          "id": "c",
          "text": "A network mask or prefix length identifies which bits represent the network portion of an address."
        },
        {
          "id": "d",
          "text": "First hop redundancy protocols provide a redundant default gateway for hosts on a LAN."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "Routing protocol code refers to A routing protocol code identifies how a route was learned, such as connected, static, OSPF, or another source. First hop redundancy protocols is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "b": "Route prefix refers to A route prefix identifies the destination network or subnet in a routing table entry. First hop redundancy protocols is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "c": "Network mask refers to A network mask or prefix length identifies which bits represent the network portion of an address. First hop redundancy protocols is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "d": "First hop redundancy protocols provide a redundant default gateway for hosts on a LAN. This is the correct answer."
      }
    },
    {
      "id": 494,
      "domain": "3.0",
      "objective": "3.5",
      "keyword": "First hop redundancy protocols",
      "difficulty": "medium",
      "text": "A LAN needs redundant default-gateway behavior involving First hop redundancy protocols. Which answer best describes the concept?",
      "answers": [
        {
          "id": "a",
          "text": "First hop redundancy protocols is the best match when the IP connectivity scenario requires this behavior: First hop redundancy protocols provide a redundant default gateway for hosts on a LAN."
        },
        {
          "id": "b",
          "text": "IPv4 static route is the best match when the IP connectivity scenario requires this behavior: An IPv4 static route is a manually configured route for an IPv4 destination prefix."
        },
        {
          "id": "c",
          "text": "IPv6 static route is the best match when the IP connectivity scenario requires this behavior: An IPv6 static route is a manually configured route for an IPv6 destination prefix."
        },
        {
          "id": "d",
          "text": "Default route is the best match when the IP connectivity scenario requires this behavior: A default route matches destinations that do not match any more specific route."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "First hop redundancy protocols provide a redundant default gateway for hosts on a LAN. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer.",
        "b": "IPv4 static route refers to An IPv4 static route is a manually configured route for an IPv4 destination prefix. First hop redundancy protocols is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "c": "IPv6 static route refers to An IPv6 static route is a manually configured route for an IPv6 destination prefix. First hop redundancy protocols is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "d": "Default route refers to A default route matches destinations that do not match any more specific route. First hop redundancy protocols is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection."
      }
    },
    {
      "id": 495,
      "domain": "3.0",
      "objective": "3.5",
      "keyword": "First hop redundancy protocols",
      "difficulty": "medium-hard",
      "text": "Hosts must keep their default gateway after a router failure. Which scenario best matches First hop redundancy protocols?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to Single-area OSPFv2 when the routing or OSPF evidence depends on this distinction: Single-area OSPFv2 runs OSPF for IPv4 within one area, commonly area 0, to exchange routes dynamically."
        },
        {
          "id": "b",
          "text": "A scenario points to First hop redundancy protocols when the routing or OSPF evidence depends on this distinction: First hop redundancy protocols provide a redundant default gateway for hosts on a LAN."
        },
        {
          "id": "c",
          "text": "A scenario points to OSPF neighbor adjacency when the routing or OSPF evidence depends on this distinction: An OSPF neighbor adjacency is the relationship formed between OSPF routers that successfully exchange hello packets and required parameters."
        },
        {
          "id": "d",
          "text": "A scenario points to OSPF hello packets when the routing or OSPF evidence depends on this distinction: OSPF hello packets discover neighbors and maintain OSPF neighbor relationships."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "Single-area OSPFv2 refers to Single-area OSPFv2 runs OSPF for IPv4 within one area, commonly area 0, to exchange routes dynamically. First hop redundancy protocols is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "b": "First hop redundancy protocols provide a redundant default gateway for hosts on a LAN. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer.",
        "c": "OSPF neighbor adjacency refers to An OSPF neighbor adjacency is the relationship formed between OSPF routers that successfully exchange hello packets and required parameters. First hop redundancy protocols is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "d": "OSPF hello packets refers to OSPF hello packets discover neighbors and maintain OSPF neighbor relationships. First hop redundancy protocols is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection."
      }
    },
    {
      "id": 496,
      "domain": "3.0",
      "objective": "3.5",
      "keyword": "First hop redundancy protocols",
      "difficulty": "hard",
      "text": "A default-gateway redundancy design must continue forwarding during device failure. Which answer applies First hop redundancy protocols most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "BDR election is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: Backup designated router election selects a standby router to take over if the DR fails."
        },
        {
          "id": "b",
          "text": "OSPF cost is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: OSPF cost is the metric used by OSPF to choose preferred paths."
        },
        {
          "id": "c",
          "text": "First hop redundancy protocols is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: First hop redundancy protocols provide a redundant default gateway for hosts on a LAN."
        },
        {
          "id": "d",
          "text": "OSPF adjacency verification is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: OSPF adjacency verification checks neighbor state, interface participation, area, timers, and reachability to confirm OSPF operation."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "BDR election refers to Backup designated router election selects a standby router to take over if the DR fails. First hop redundancy protocols is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "b": "OSPF cost refers to OSPF cost is the metric used by OSPF to choose preferred paths. First hop redundancy protocols is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "c": "First hop redundancy protocols provide a redundant default gateway for hosts on a LAN. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer.",
        "d": "OSPF adjacency verification refers to OSPF adjacency verification checks neighbor state, interface participation, area, timers, and reachability to confirm OSPF operation. First hop redundancy protocols is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection."
      }
    },
    {
      "id": 497,
      "domain": "3.0",
      "objective": "3.5",
      "keyword": "Default gateway redundancy",
      "difficulty": "easy",
      "text": "Which description best matches Default gateway redundancy in CCNA Objective 3.0 IP Connectivity?",
      "answers": [
        {
          "id": "a",
          "text": "Default gateway redundancy lets hosts keep using a gateway address even if one physical router or Layer 3 switch fails."
        },
        {
          "id": "b",
          "text": "Longest prefix match selects the route with the most specific matching prefix for the destination IP address."
        },
        {
          "id": "c",
          "text": "A router forwarding decision uses the destination IP address, routing table, longest prefix match, and route attributes to choose an outgoing path."
        },
        {
          "id": "d",
          "text": "Route source preference uses administrative distance only when multiple sources advertise the same prefix length."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "Default gateway redundancy lets hosts keep using a gateway address even if one physical router or Layer 3 switch fails. This is the correct answer.",
        "b": "Longest prefix match refers to Longest prefix match selects the route with the most specific matching prefix for the destination IP address. Default gateway redundancy is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "c": "Forwarding decision refers to A router forwarding decision uses the destination IP address, routing table, longest prefix match, and route attributes to choose an outgoing path. Default gateway redundancy is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "d": "Route source preference refers to Route source preference uses administrative distance only when multiple sources advertise the same prefix length. Default gateway redundancy is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection."
      }
    },
    {
      "id": 498,
      "domain": "3.0",
      "objective": "3.5",
      "keyword": "Default gateway redundancy",
      "difficulty": "medium",
      "text": "A LAN needs redundant default-gateway behavior involving Default gateway redundancy. Which answer best describes the concept?",
      "answers": [
        {
          "id": "a",
          "text": "Fully specified static route is the best match when the IP connectivity scenario requires this behavior: A fully specified static route includes both the exit interface and next-hop address."
        },
        {
          "id": "b",
          "text": "Default gateway redundancy is the best match when the IP connectivity scenario requires this behavior: Default gateway redundancy lets hosts keep using a gateway address even if one physical router or Layer 3 switch fails."
        },
        {
          "id": "c",
          "text": "Exit-interface static route is the best match when the IP connectivity scenario requires this behavior: An exit-interface static route specifies the local outgoing interface used to forward traffic."
        },
        {
          "id": "d",
          "text": "Static route verification is the best match when the IP connectivity scenario requires this behavior: Static route verification confirms configured static routes appear in the routing table and forward traffic as intended."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "Fully specified static route refers to A fully specified static route includes both the exit interface and next-hop address. Default gateway redundancy is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "b": "Default gateway redundancy lets hosts keep using a gateway address even if one physical router or Layer 3 switch fails. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer.",
        "c": "Exit-interface static route refers to An exit-interface static route specifies the local outgoing interface used to forward traffic. Default gateway redundancy is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "d": "Static route verification refers to Static route verification confirms configured static routes appear in the routing table and forward traffic as intended. Default gateway redundancy is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection."
      }
    },
    {
      "id": 499,
      "domain": "3.0",
      "objective": "3.5",
      "keyword": "Default gateway redundancy",
      "difficulty": "medium-hard",
      "text": "Hosts must keep their default gateway after a router failure. Which scenario best matches Default gateway redundancy?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to OSPF router ID selection when the routing or OSPF evidence depends on this distinction: OSPF router ID selection uses a manually configured router ID first, then the highest loopback IP, then the highest active physical interface IP."
        },
        {
          "id": "b",
          "text": "A scenario points to OSPF point-to-point network when the routing or OSPF evidence depends on this distinction: An OSPF point-to-point network type connects two routers without DR or BDR election."
        },
        {
          "id": "c",
          "text": "A scenario points to Default gateway redundancy when the routing or OSPF evidence depends on this distinction: Default gateway redundancy lets hosts keep using a gateway address even if one physical router or Layer 3 switch fails."
        },
        {
          "id": "d",
          "text": "A scenario points to OSPF broadcast network when the routing or OSPF evidence depends on this distinction: An OSPF broadcast network type supports multiaccess Ethernet segments and uses DR/BDR election."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "OSPF router ID selection refers to OSPF router ID selection uses a manually configured router ID first, then the highest loopback IP, then the highest active physical interface IP. Default gateway redundancy is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "b": "OSPF point-to-point network refers to An OSPF point-to-point network type connects two routers without DR or BDR election. Default gateway redundancy is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "c": "Default gateway redundancy lets hosts keep using a gateway address even if one physical router or Layer 3 switch fails. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer.",
        "d": "OSPF broadcast network refers to An OSPF broadcast network type supports multiaccess Ethernet segments and uses DR/BDR election. Default gateway redundancy is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection."
      }
    },
    {
      "id": 500,
      "domain": "3.0",
      "objective": "3.5",
      "keyword": "Default gateway redundancy",
      "difficulty": "hard",
      "text": "A default-gateway redundancy design must continue forwarding during device failure. Which answer applies Default gateway redundancy most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "OSPF wildcard mask is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: An OSPF wildcard mask in a network command identifies which interface addresses match for OSPF activation."
        },
        {
          "id": "b",
          "text": "OSPF process ID is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: The OSPF process ID locally identifies an OSPF routing process and does not need to match between routers."
        },
        {
          "id": "c",
          "text": "First hop redundancy protocols is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: First hop redundancy protocols provide a redundant default gateway for hosts on a LAN."
        },
        {
          "id": "d",
          "text": "Default gateway redundancy is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: Default gateway redundancy lets hosts keep using a gateway address even if one physical router or Layer 3 switch fails."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "OSPF wildcard mask refers to An OSPF wildcard mask in a network command identifies which interface addresses match for OSPF activation. Default gateway redundancy is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "b": "OSPF process ID refers to The OSPF process ID locally identifies an OSPF routing process and does not need to match between routers. Default gateway redundancy is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "c": "First hop redundancy protocols refers to First hop redundancy protocols provide a redundant default gateway for hosts on a LAN. Default gateway redundancy is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "d": "Default gateway redundancy lets hosts keep using a gateway address even if one physical router or Layer 3 switch fails. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer."
      }
    },
    {
      "id": 501,
      "domain": "3.0",
      "objective": "3.5",
      "keyword": "Virtual IP address",
      "difficulty": "easy",
      "text": "Which option correctly defines Virtual IP address?",
      "answers": [
        {
          "id": "a",
          "text": "A network route points to an entire subnet or network rather than one host."
        },
        {
          "id": "b",
          "text": "A virtual IP address is the shared gateway address used by hosts in a first hop redundancy design."
        },
        {
          "id": "c",
          "text": "A host route points to one specific host address, typically using /32 for IPv4 or /128 for IPv6."
        },
        {
          "id": "d",
          "text": "A floating static route uses a higher administrative distance so it is installed only when a better route is unavailable."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "Network route refers to A network route points to an entire subnet or network rather than one host. Virtual IP address is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "b": "A virtual IP address is the shared gateway address used by hosts in a first hop redundancy design. This is the correct answer.",
        "c": "Host route refers to A host route points to one specific host address, typically using /32 for IPv4 or /128 for IPv6. Virtual IP address is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "d": "Floating static route refers to A floating static route uses a higher administrative distance so it is installed only when a better route is unavailable. Virtual IP address is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection."
      }
    },
    {
      "id": 502,
      "domain": "3.0",
      "objective": "3.5",
      "keyword": "Virtual IP address",
      "difficulty": "medium",
      "text": "A LAN needs redundant default-gateway behavior involving Virtual IP address. Which answer best describes the concept?",
      "answers": [
        {
          "id": "a",
          "text": "OSPF area is the best match when the IP connectivity scenario requires this behavior: An OSPF area groups routers and link-state information; single-area CCNA scenarios commonly use area 0."
        },
        {
          "id": "b",
          "text": "OSPF network command is the best match when the IP connectivity scenario requires this behavior: The OSPF network command enables OSPF on matching interfaces and assigns them to an area."
        },
        {
          "id": "c",
          "text": "Virtual IP address is the best match when the IP connectivity scenario requires this behavior: A virtual IP address is the shared gateway address used by hosts in a first hop redundancy design."
        },
        {
          "id": "d",
          "text": "OSPF passive interface is the best match when the IP connectivity scenario requires this behavior: An OSPF passive interface advertises the connected network but does not send OSPF hello packets on that interface."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "OSPF area refers to An OSPF area groups routers and link-state information; single-area CCNA scenarios commonly use area 0. Virtual IP address is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "b": "OSPF network command refers to The OSPF network command enables OSPF on matching interfaces and assigns them to an area. Virtual IP address is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "c": "A virtual IP address is the shared gateway address used by hosts in a first hop redundancy design. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer.",
        "d": "OSPF passive interface refers to An OSPF passive interface advertises the connected network but does not send OSPF hello packets on that interface. Virtual IP address is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection."
      }
    },
    {
      "id": 503,
      "domain": "3.0",
      "objective": "3.5",
      "keyword": "Virtual IP address",
      "difficulty": "medium-hard",
      "text": "Hosts must keep their default gateway after a router failure. Which scenario best matches Virtual IP address?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to OSPF route verification when the routing or OSPF evidence depends on this distinction: OSPF route verification checks that OSPF-learned routes appear in the routing table and are used for forwarding."
        },
        {
          "id": "b",
          "text": "A scenario points to OSPF mismatched area when the routing or OSPF evidence depends on this distinction: An OSPF area mismatch prevents neighbors from forming because both ends of the link must agree on the area."
        },
        {
          "id": "c",
          "text": "A scenario points to OSPF mismatched timers when the routing or OSPF evidence depends on this distinction: OSPF hello and dead timer mismatches can prevent neighbor adjacency from forming."
        },
        {
          "id": "d",
          "text": "A scenario points to Virtual IP address when the routing or OSPF evidence depends on this distinction: A virtual IP address is the shared gateway address used by hosts in a first hop redundancy design."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "OSPF route verification refers to OSPF route verification checks that OSPF-learned routes appear in the routing table and are used for forwarding. Virtual IP address is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "b": "OSPF mismatched area refers to An OSPF area mismatch prevents neighbors from forming because both ends of the link must agree on the area. Virtual IP address is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "c": "OSPF mismatched timers refers to OSPF hello and dead timer mismatches can prevent neighbor adjacency from forming. Virtual IP address is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "d": "A virtual IP address is the shared gateway address used by hosts in a first hop redundancy design. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer."
      }
    },
    {
      "id": 504,
      "domain": "3.0",
      "objective": "3.5",
      "keyword": "Virtual IP address",
      "difficulty": "hard",
      "text": "A default-gateway redundancy design must continue forwarding during device failure. Which answer applies Virtual IP address most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "Virtual IP address is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A virtual IP address is the shared gateway address used by hosts in a first hop redundancy design."
        },
        {
          "id": "b",
          "text": "FHRP failover is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: FHRP failover moves the active gateway role to another device when the current active device becomes unavailable."
        },
        {
          "id": "c",
          "text": "Routing table is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A routing table lists known routes and the information a router uses to choose where to forward packets."
        },
        {
          "id": "d",
          "text": "Routing protocol code is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A routing protocol code identifies how a route was learned, such as connected, static, OSPF, or another source."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "A virtual IP address is the shared gateway address used by hosts in a first hop redundancy design. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer.",
        "b": "FHRP failover refers to FHRP failover moves the active gateway role to another device when the current active device becomes unavailable. Virtual IP address is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "c": "Routing table refers to A routing table lists known routes and the information a router uses to choose where to forward packets. Virtual IP address is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "d": "Routing protocol code refers to A routing protocol code identifies how a route was learned, such as connected, static, OSPF, or another source. Virtual IP address is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection."
      }
    },
    {
      "id": 505,
      "domain": "3.0",
      "objective": "3.5",
      "keyword": "Virtual MAC address",
      "difficulty": "easy",
      "text": "How is Virtual MAC address best understood in an IP connectivity context?",
      "answers": [
        {
          "id": "a",
          "text": "Static route prefix specificity determines whether a default, network, or host route is the most specific match for a destination."
        },
        {
          "id": "b",
          "text": "Single-area OSPFv2 runs OSPF for IPv4 within one area, commonly area 0, to exchange routes dynamically."
        },
        {
          "id": "c",
          "text": "A virtual MAC address is used by the active first-hop device so hosts can forward frames to a redundant gateway."
        },
        {
          "id": "d",
          "text": "An OSPF neighbor adjacency is the relationship formed between OSPF routers that successfully exchange hello packets and required parameters."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "Static route prefix specificity refers to Static route prefix specificity determines whether a default, network, or host route is the most specific match for a destination. Virtual MAC address is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "b": "Single-area OSPFv2 refers to Single-area OSPFv2 runs OSPF for IPv4 within one area, commonly area 0, to exchange routes dynamically. Virtual MAC address is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "c": "A virtual MAC address is used by the active first-hop device so hosts can forward frames to a redundant gateway. This is the correct answer.",
        "d": "OSPF neighbor adjacency refers to An OSPF neighbor adjacency is the relationship formed between OSPF routers that successfully exchange hello packets and required parameters. Virtual MAC address is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection."
      }
    },
    {
      "id": 506,
      "domain": "3.0",
      "objective": "3.5",
      "keyword": "Virtual MAC address",
      "difficulty": "medium",
      "text": "A LAN needs redundant default-gateway behavior involving Virtual MAC address. Which answer best describes the concept?",
      "answers": [
        {
          "id": "a",
          "text": "DR election is the best match when the IP connectivity scenario requires this behavior: Designated router election selects a router on an OSPF broadcast network to reduce adjacency overhead."
        },
        {
          "id": "b",
          "text": "BDR election is the best match when the IP connectivity scenario requires this behavior: Backup designated router election selects a standby router to take over if the DR fails."
        },
        {
          "id": "c",
          "text": "OSPF cost is the best match when the IP connectivity scenario requires this behavior: OSPF cost is the metric used by OSPF to choose preferred paths."
        },
        {
          "id": "d",
          "text": "Virtual MAC address is the best match when the IP connectivity scenario requires this behavior: A virtual MAC address is used by the active first-hop device so hosts can forward frames to a redundant gateway."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "DR election refers to Designated router election selects a router on an OSPF broadcast network to reduce adjacency overhead. Virtual MAC address is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "b": "BDR election refers to Backup designated router election selects a standby router to take over if the DR fails. Virtual MAC address is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "c": "OSPF cost refers to OSPF cost is the metric used by OSPF to choose preferred paths. Virtual MAC address is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "d": "A virtual MAC address is used by the active first-hop device so hosts can forward frames to a redundant gateway. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer."
      }
    },
    {
      "id": 507,
      "domain": "3.0",
      "objective": "3.5",
      "keyword": "Virtual MAC address",
      "difficulty": "medium-hard",
      "text": "Hosts must keep their default gateway after a router failure. Which scenario best matches Virtual MAC address?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to Virtual MAC address when the routing or OSPF evidence depends on this distinction: A virtual MAC address is used by the active first-hop device so hosts can forward frames to a redundant gateway."
        },
        {
          "id": "b",
          "text": "A scenario points to Default gateway redundancy when the routing or OSPF evidence depends on this distinction: Default gateway redundancy lets hosts keep using a gateway address even if one physical router or Layer 3 switch fails."
        },
        {
          "id": "c",
          "text": "A scenario points to Virtual IP address when the routing or OSPF evidence depends on this distinction: A virtual IP address is the shared gateway address used by hosts in a first hop redundancy design."
        },
        {
          "id": "d",
          "text": "A scenario points to Active router when the routing or OSPF evidence depends on this distinction: An active router in an FHRP group forwards traffic sent to the virtual gateway."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "A virtual MAC address is used by the active first-hop device so hosts can forward frames to a redundant gateway. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer.",
        "b": "Default gateway redundancy refers to Default gateway redundancy lets hosts keep using a gateway address even if one physical router or Layer 3 switch fails. Virtual MAC address is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "c": "Virtual IP address refers to A virtual IP address is the shared gateway address used by hosts in a first hop redundancy design. Virtual MAC address is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "d": "Active router refers to An active router in an FHRP group forwards traffic sent to the virtual gateway. Virtual MAC address is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection."
      }
    },
    {
      "id": 508,
      "domain": "3.0",
      "objective": "3.5",
      "keyword": "Virtual MAC address",
      "difficulty": "hard",
      "text": "A default-gateway redundancy design must continue forwarding during device failure. Which answer applies Virtual MAC address most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "Routing metric is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A routing metric ranks paths within the same routing protocol or route source."
        },
        {
          "id": "b",
          "text": "Virtual MAC address is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A virtual MAC address is used by the active first-hop device so hosts can forward frames to a redundant gateway."
        },
        {
          "id": "c",
          "text": "Gateway of last resort is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: The gateway of last resort is the default route used when no more specific route matches the destination."
        },
        {
          "id": "d",
          "text": "Longest prefix match is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: Longest prefix match selects the route with the most specific matching prefix for the destination IP address."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "Routing metric refers to A routing metric ranks paths within the same routing protocol or route source. Virtual MAC address is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "b": "A virtual MAC address is used by the active first-hop device so hosts can forward frames to a redundant gateway. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer.",
        "c": "Gateway of last resort refers to The gateway of last resort is the default route used when no more specific route matches the destination. Virtual MAC address is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "d": "Longest prefix match refers to Longest prefix match selects the route with the most specific matching prefix for the destination IP address. Virtual MAC address is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection."
      }
    },
    {
      "id": 509,
      "domain": "3.0",
      "objective": "3.5",
      "keyword": "Active router",
      "difficulty": "easy",
      "text": "Select the description that best identifies Active router.",
      "answers": [
        {
          "id": "a",
          "text": "An OSPF router ID uniquely identifies an OSPF router in the OSPF domain."
        },
        {
          "id": "b",
          "text": "OSPF router ID selection uses a manually configured router ID first, then the highest loopback IP, then the highest active physical interface IP."
        },
        {
          "id": "c",
          "text": "An OSPF point-to-point network type connects two routers without DR or BDR election."
        },
        {
          "id": "d",
          "text": "An active router in an FHRP group forwards traffic sent to the virtual gateway."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "OSPF router ID refers to An OSPF router ID uniquely identifies an OSPF router in the OSPF domain. Active router is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "b": "OSPF router ID selection refers to OSPF router ID selection uses a manually configured router ID first, then the highest loopback IP, then the highest active physical interface IP. Active router is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "c": "OSPF point-to-point network refers to An OSPF point-to-point network type connects two routers without DR or BDR election. Active router is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "d": "An active router in an FHRP group forwards traffic sent to the virtual gateway. This is the correct answer."
      }
    },
    {
      "id": 510,
      "domain": "3.0",
      "objective": "3.5",
      "keyword": "Active router",
      "difficulty": "medium",
      "text": "A LAN needs redundant default-gateway behavior involving Active router. Which answer best describes the concept?",
      "answers": [
        {
          "id": "a",
          "text": "Active router is the best match when the IP connectivity scenario requires this behavior: An active router in an FHRP group forwards traffic sent to the virtual gateway."
        },
        {
          "id": "b",
          "text": "OSPF network type mismatch is the best match when the IP connectivity scenario requires this behavior: OSPF network type mismatch can affect adjacency behavior, DR/BDR expectations, or route exchange."
        },
        {
          "id": "c",
          "text": "OSPF wildcard mask is the best match when the IP connectivity scenario requires this behavior: An OSPF wildcard mask in a network command identifies which interface addresses match for OSPF activation."
        },
        {
          "id": "d",
          "text": "OSPF process ID is the best match when the IP connectivity scenario requires this behavior: The OSPF process ID locally identifies an OSPF routing process and does not need to match between routers."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "An active router in an FHRP group forwards traffic sent to the virtual gateway. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer.",
        "b": "OSPF network type mismatch refers to OSPF network type mismatch can affect adjacency behavior, DR/BDR expectations, or route exchange. Active router is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "c": "OSPF wildcard mask refers to An OSPF wildcard mask in a network command identifies which interface addresses match for OSPF activation. Active router is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "d": "OSPF process ID refers to The OSPF process ID locally identifies an OSPF routing process and does not need to match between routers. Active router is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection."
      }
    },
    {
      "id": 511,
      "domain": "3.0",
      "objective": "3.5",
      "keyword": "Active router",
      "difficulty": "medium-hard",
      "text": "Hosts must keep their default gateway after a router failure. Which scenario best matches Active router?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to Route prefix when the routing or OSPF evidence depends on this distinction: A route prefix identifies the destination network or subnet in a routing table entry."
        },
        {
          "id": "b",
          "text": "A scenario points to Active router when the routing or OSPF evidence depends on this distinction: An active router in an FHRP group forwards traffic sent to the virtual gateway."
        },
        {
          "id": "c",
          "text": "A scenario points to Network mask when the routing or OSPF evidence depends on this distinction: A network mask or prefix length identifies which bits represent the network portion of an address."
        },
        {
          "id": "d",
          "text": "A scenario points to Next hop when the routing or OSPF evidence depends on this distinction: A next hop is the next router IP address to which a packet is sent on the way to the destination."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "Route prefix refers to A route prefix identifies the destination network or subnet in a routing table entry. Active router is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "b": "An active router in an FHRP group forwards traffic sent to the virtual gateway. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer.",
        "c": "Network mask refers to A network mask or prefix length identifies which bits represent the network portion of an address. Active router is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "d": "Next hop refers to A next hop is the next router IP address to which a packet is sent on the way to the destination. Active router is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection."
      }
    },
    {
      "id": 512,
      "domain": "3.0",
      "objective": "3.5",
      "keyword": "Active router",
      "difficulty": "hard",
      "text": "A default-gateway redundancy design must continue forwarding during device failure. Which answer applies Active router most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "IPv6 static route is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: An IPv6 static route is a manually configured route for an IPv6 destination prefix."
        },
        {
          "id": "b",
          "text": "Default route is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A default route matches destinations that do not match any more specific route."
        },
        {
          "id": "c",
          "text": "Active router is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: An active router in an FHRP group forwards traffic sent to the virtual gateway."
        },
        {
          "id": "d",
          "text": "Network route is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A network route points to an entire subnet or network rather than one host."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "IPv6 static route refers to An IPv6 static route is a manually configured route for an IPv6 destination prefix. Active router is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "b": "Default route refers to A default route matches destinations that do not match any more specific route. Active router is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "c": "An active router in an FHRP group forwards traffic sent to the virtual gateway. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer.",
        "d": "Network route refers to A network route points to an entire subnet or network rather than one host. Active router is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection."
      }
    },
    {
      "id": 513,
      "domain": "3.0",
      "objective": "3.5",
      "keyword": "Standby router",
      "difficulty": "easy",
      "text": "Which description best matches Standby router in CCNA Objective 3.0 IP Connectivity?",
      "answers": [
        {
          "id": "a",
          "text": "A standby router waits to take over forwarding if the active first-hop device fails."
        },
        {
          "id": "b",
          "text": "OSPF adjacency verification checks neighbor state, interface participation, area, timers, and reachability to confirm OSPF operation."
        },
        {
          "id": "c",
          "text": "OSPF route verification checks that OSPF-learned routes appear in the routing table and are used for forwarding."
        },
        {
          "id": "d",
          "text": "An OSPF area mismatch prevents neighbors from forming because both ends of the link must agree on the area."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "A standby router waits to take over forwarding if the active first-hop device fails. This is the correct answer.",
        "b": "OSPF adjacency verification refers to OSPF adjacency verification checks neighbor state, interface participation, area, timers, and reachability to confirm OSPF operation. Standby router is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "c": "OSPF route verification refers to OSPF route verification checks that OSPF-learned routes appear in the routing table and are used for forwarding. Standby router is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "d": "OSPF mismatched area refers to An OSPF area mismatch prevents neighbors from forming because both ends of the link must agree on the area. Standby router is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection."
      }
    },
    {
      "id": 514,
      "domain": "3.0",
      "objective": "3.5",
      "keyword": "Standby router",
      "difficulty": "medium",
      "text": "A LAN needs redundant default-gateway behavior involving Standby router. Which answer best describes the concept?",
      "answers": [
        {
          "id": "a",
          "text": "Active router is the best match when the IP connectivity scenario requires this behavior: An active router in an FHRP group forwards traffic sent to the virtual gateway."
        },
        {
          "id": "b",
          "text": "Standby router is the best match when the IP connectivity scenario requires this behavior: A standby router waits to take over forwarding if the active first-hop device fails."
        },
        {
          "id": "c",
          "text": "FHRP failover is the best match when the IP connectivity scenario requires this behavior: FHRP failover moves the active gateway role to another device when the current active device becomes unavailable."
        },
        {
          "id": "d",
          "text": "Routing table is the best match when the IP connectivity scenario requires this behavior: A routing table lists known routes and the information a router uses to choose where to forward packets."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "Active router refers to An active router in an FHRP group forwards traffic sent to the virtual gateway. Standby router is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "b": "A standby router waits to take over forwarding if the active first-hop device fails. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer.",
        "c": "FHRP failover refers to FHRP failover moves the active gateway role to another device when the current active device becomes unavailable. Standby router is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "d": "Routing table refers to A routing table lists known routes and the information a router uses to choose where to forward packets. Standby router is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection."
      }
    },
    {
      "id": 515,
      "domain": "3.0",
      "objective": "3.5",
      "keyword": "Standby router",
      "difficulty": "medium-hard",
      "text": "Hosts must keep their default gateway after a router failure. Which scenario best matches Standby router?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to Forwarding decision when the routing or OSPF evidence depends on this distinction: A router forwarding decision uses the destination IP address, routing table, longest prefix match, and route attributes to choose an outgoing path."
        },
        {
          "id": "b",
          "text": "A scenario points to Route source preference when the routing or OSPF evidence depends on this distinction: Route source preference uses administrative distance only when multiple sources advertise the same prefix length."
        },
        {
          "id": "c",
          "text": "A scenario points to Standby router when the routing or OSPF evidence depends on this distinction: A standby router waits to take over forwarding if the active first-hop device fails."
        },
        {
          "id": "d",
          "text": "A scenario points to Metric comparison when the routing or OSPF evidence depends on this distinction: Metric comparison chooses the best path among routes from the same routing protocol or route source."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "Forwarding decision refers to A router forwarding decision uses the destination IP address, routing table, longest prefix match, and route attributes to choose an outgoing path. Standby router is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "b": "Route source preference refers to Route source preference uses administrative distance only when multiple sources advertise the same prefix length. Standby router is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "c": "A standby router waits to take over forwarding if the active first-hop device fails. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer.",
        "d": "Metric comparison refers to Metric comparison chooses the best path among routes from the same routing protocol or route source. Standby router is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection."
      }
    },
    {
      "id": 516,
      "domain": "3.0",
      "objective": "3.5",
      "keyword": "Standby router",
      "difficulty": "hard",
      "text": "A default-gateway redundancy design must continue forwarding during device failure. Which answer applies Standby router most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "Exit-interface static route is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: An exit-interface static route specifies the local outgoing interface used to forward traffic."
        },
        {
          "id": "b",
          "text": "Static route verification is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: Static route verification confirms configured static routes appear in the routing table and forward traffic as intended."
        },
        {
          "id": "c",
          "text": "Static route prefix specificity is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: Static route prefix specificity determines whether a default, network, or host route is the most specific match for a destination."
        },
        {
          "id": "d",
          "text": "Standby router is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: A standby router waits to take over forwarding if the active first-hop device fails."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "Exit-interface static route refers to An exit-interface static route specifies the local outgoing interface used to forward traffic. Standby router is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "b": "Static route verification refers to Static route verification confirms configured static routes appear in the routing table and forward traffic as intended. Standby router is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "c": "Static route prefix specificity refers to Static route prefix specificity determines whether a default, network, or host route is the most specific match for a destination. Standby router is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "d": "A standby router waits to take over forwarding if the active first-hop device fails. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer."
      }
    },
    {
      "id": 517,
      "domain": "3.0",
      "objective": "3.5",
      "keyword": "FHRP failover",
      "difficulty": "easy",
      "text": "Which option correctly defines FHRP failover?",
      "answers": [
        {
          "id": "a",
          "text": "First hop redundancy protocols provide a redundant default gateway for hosts on a LAN."
        },
        {
          "id": "b",
          "text": "FHRP failover moves the active gateway role to another device when the current active device becomes unavailable."
        },
        {
          "id": "c",
          "text": "Default gateway redundancy lets hosts keep using a gateway address even if one physical router or Layer 3 switch fails."
        },
        {
          "id": "d",
          "text": "A virtual IP address is the shared gateway address used by hosts in a first hop redundancy design."
        }
      ],
      "correct": "b",
      "explanations": {
        "a": "First hop redundancy protocols refers to First hop redundancy protocols provide a redundant default gateway for hosts on a LAN. FHRP failover is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "b": "FHRP failover moves the active gateway role to another device when the current active device becomes unavailable. This is the correct answer.",
        "c": "Default gateway redundancy refers to Default gateway redundancy lets hosts keep using a gateway address even if one physical router or Layer 3 switch fails. FHRP failover is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "d": "Virtual IP address refers to A virtual IP address is the shared gateway address used by hosts in a first hop redundancy design. FHRP failover is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection."
      }
    },
    {
      "id": 518,
      "domain": "3.0",
      "objective": "3.5",
      "keyword": "FHRP failover",
      "difficulty": "medium",
      "text": "A LAN needs redundant default-gateway behavior involving FHRP failover. Which answer best describes the concept?",
      "answers": [
        {
          "id": "a",
          "text": "Administrative distance is the best match when the IP connectivity scenario requires this behavior: Administrative distance ranks the trustworthiness of route sources when the same prefix is learned from multiple sources."
        },
        {
          "id": "b",
          "text": "Routing metric is the best match when the IP connectivity scenario requires this behavior: A routing metric ranks paths within the same routing protocol or route source."
        },
        {
          "id": "c",
          "text": "FHRP failover is the best match when the IP connectivity scenario requires this behavior: FHRP failover moves the active gateway role to another device when the current active device becomes unavailable."
        },
        {
          "id": "d",
          "text": "Gateway of last resort is the best match when the IP connectivity scenario requires this behavior: The gateway of last resort is the default route used when no more specific route matches the destination."
        }
      ],
      "correct": "c",
      "explanations": {
        "a": "Administrative distance refers to Administrative distance ranks the trustworthiness of route sources when the same prefix is learned from multiple sources. FHRP failover is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "b": "Routing metric refers to A routing metric ranks paths within the same routing protocol or route source. FHRP failover is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "c": "FHRP failover moves the active gateway role to another device when the current active device becomes unavailable. This matches the IP connectivity scenario without confusing route selection, static routing, OSPF operation, or first-hop redundancy. This is the correct answer.",
        "d": "Gateway of last resort refers to The gateway of last resort is the default route used when no more specific route matches the destination. FHRP failover is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection."
      }
    },
    {
      "id": 519,
      "domain": "3.0",
      "objective": "3.5",
      "keyword": "FHRP failover",
      "difficulty": "medium-hard",
      "text": "Hosts must keep their default gateway after a router failure. Which scenario best matches FHRP failover?",
      "answers": [
        {
          "id": "a",
          "text": "A scenario points to Host route when the routing or OSPF evidence depends on this distinction: A host route points to one specific host address, typically using /32 for IPv4 or /128 for IPv6."
        },
        {
          "id": "b",
          "text": "A scenario points to Floating static route when the routing or OSPF evidence depends on this distinction: A floating static route uses a higher administrative distance so it is installed only when a better route is unavailable."
        },
        {
          "id": "c",
          "text": "A scenario points to Recursive static route when the routing or OSPF evidence depends on this distinction: A recursive static route points to a next-hop IP address that must itself be resolved through another route."
        },
        {
          "id": "d",
          "text": "A scenario points to FHRP failover when the routing or OSPF evidence depends on this distinction: FHRP failover moves the active gateway role to another device when the current active device becomes unavailable."
        }
      ],
      "correct": "d",
      "explanations": {
        "a": "Host route refers to A host route points to one specific host address, typically using /32 for IPv4 or /128 for IPv6. FHRP failover is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "b": "Floating static route refers to A floating static route uses a higher administrative distance so it is installed only when a better route is unavailable. FHRP failover is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "c": "Recursive static route refers to A recursive static route points to a next-hop IP address that must itself be resolved through another route. FHRP failover is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "d": "FHRP failover moves the active gateway role to another device when the current active device becomes unavailable. This distinction matters because the wrong concept would lead to the wrong route-table interpretation, forwarding decision, OSPF verification, or gateway-redundancy conclusion. This is the correct answer."
      }
    },
    {
      "id": 520,
      "domain": "3.0",
      "objective": "3.5",
      "keyword": "FHRP failover",
      "difficulty": "hard",
      "text": "A default-gateway redundancy design must continue forwarding during device failure. Which answer applies FHRP failover most accurately?",
      "answers": [
        {
          "id": "a",
          "text": "FHRP failover is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: FHRP failover moves the active gateway role to another device when the current active device becomes unavailable."
        },
        {
          "id": "b",
          "text": "OSPF network command is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: The OSPF network command enables OSPF on matching interfaces and assigns them to an area."
        },
        {
          "id": "c",
          "text": "OSPF passive interface is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: An OSPF passive interface advertises the connected network but does not send OSPF hello packets on that interface."
        },
        {
          "id": "d",
          "text": "OSPF router ID is the correct operational interpretation when the forwarding, route selection, OSPF, or FHRP decision depends on this exact meaning: An OSPF router ID uniquely identifies an OSPF router in the OSPF domain."
        }
      ],
      "correct": "a",
      "explanations": {
        "a": "FHRP failover moves the active gateway role to another device when the current active device becomes unavailable. This applies the concept at the point where a Cisco administrator must predict routing behavior from prefixes, administrative distance, metrics, OSPF state, or default-gateway redundancy. This is the correct answer.",
        "b": "OSPF network command refers to The OSPF network command enables OSPF on matching interfaces and assigns them to an area. FHRP failover is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "c": "OSPF passive interface refers to An OSPF passive interface advertises the connected network but does not send OSPF hello packets on that interface. FHRP failover is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection.",
        "d": "OSPF router ID refers to An OSPF router ID uniquely identifies an OSPF router in the OSPF domain. FHRP failover is different because the scenario depends on redundant default-gateway behavior rather than ordinary routing protocol selection."
      }
    }
  ]
}